# 问题清单

## 已解决

### 问题1：M1-7 端口分发路由前端缺失，无法在界面配置转发地址（已解决）
- **发现时间**：2026-03-25
- **问题描述**：M1-7 后端（`PortRouteController`、`PortRouteService`、`PortRoute` 实体）已完成，但前端页面、API 封装、路由、菜单均未创建，导致无法通过界面配置转发地址（`portAddress`）。
- **修复内容**：
  - 新增 `frontend/src/api/portRoute.js`：封装列表查询、新增/更新、删除、连通测试四个接口
  - 新增 `frontend/src/views/convert/PortRoute.vue`：含搜索栏、分页列表、新增/编辑弹窗（含转发地址、HTTP方法、超时、重试、请求/应答模板）、连通测试按钮
  - 更新 `frontend/src/router/index.js`：添加路由 `/convert/port-route`
  - 更新 `frontend/src/components/layout/SideMenu.vue`：在"渠道模板管理"后添加"端口分发路由"菜单项

---

### 问题3：字段映射"手动添加字段"缺少自定义名称输入
- **发现时间**：2026-03-25
- **问题描述**：`手动添加` 按钮直接生成 `field_1`、`field_2` 等无意义名称，用户无法指定字段名。
- **修复内容**：
  - 新增手动添加弹窗（`showAddFieldDialog`、`newFieldName`、`confirmAddField`）
  - 点击"手动添加"打开对话框，输入字段名后确认添加，含重复校验

---

### 问题4：字段加工"添加规则"触发无效 API 请求导致报错
- **发现时间**：2026-03-25
- **问题描述**：`addRule()` 内部调用 `runPreview()`，而 `runPreview()` 条件为 `&& rules.length===0`，即有规则但无输入时仍发 POST 请求，接口报错让用户误以为添加失败。
- **修复内容**：
  - `runPreview()` 条件由 `&&` 改为 `||`，无 `inputValue` 时直接跳过 API 调用
  - `addRule()` 中仅当 `inputValue` 非空时才触发预览，避免无意义请求

---

### 问题6：报文格式转换页面无法选择已配置的模板进行转换（已解决）
- **发现时间**：2026-03-25
- **问题描述**：报文格式转换（M1-1 前端）未提供选择已有模板按模板规则做转换的入口，页面只能做纯格式互转，无法复用已配置的字段映射和加工规则。
- **修复内容**：
  - `FormatConvert.vue` 顶部新增"使用模板（可选）"下拉选择器，`onMounted` 加载 `listTemplates` 填充选项（格式：`模板名（srcFmt → targetFmt）`）
  - 选中模板后：`srcFormat`/`targetFormat` 自动填充并锁定；右侧显示"含字段映射 + 字段加工"标签
  - `doConvert` 分支：有 `selectedTemplateId` 时调 `convertMessage({templateId, message, srcFormat})`（M1-6 全流程串联）；无模板保持原 `convertFormat + parseMessage` 并行逻辑
  - `reset()` 同步清除 `selectedTemplateId`

---

### 问题5：字段加工规则无唯一标识，无法被其他配置引用
- **发现时间**：2026-03-25
- **问题描述**：规则链中每条规则只有本地计数器 `id`，序列化时不携带，无法在渠道模板等其他配置中引用。
- **修复内容**：
  - 每条规则新增 `ruleName` 字段，默认值 `rule_1`、`rule_2`，用户可自定义
  - 规则列表行内增加可编辑输入框（占位 "规则标识"）
  - `saveRules()`、`buildPayload()`、`watch(modelValue)` 均同步携带 `ruleName`

---

### 问题2：报文头格式需支持渠道级别的适配配置（已解决）
- **发现时间**：2026-03-25
- **解决时间**：2026-03-27
- **问题描述**：报文头等格式要可以单独适配，需要在渠道级别提供配置选项（Content-Type、编码格式等）
- **影响范围**：M1-4 渠道模板管理、M1-7 端口分发路由
- **变更编号**：CHG-002
- **修复内容**：
  - 新增 `HeaderConfig` DTO（contentType、charset、requestHeaders、responseHeaders）
  - 新增 `CharsetConverter`：字节级编码转换（UTF-8/GBK/ISO-8859-1）
  - 新增 `HeaderConfigMerger`：两级合并，port_route 优先于 channel_config
  - `channel_config` 和 `port_route` 表各新增 `header_config TEXT` 列
  - `ChannelConfigService` / `PortRouteService` 实体双字段模式（Raw字符串存DB，对象供前端）
  - `PortRouteService.dispatch()` 插入两级 Header 合并 + charset 转码 + `forwardWithRetryBytes`
  - `PortRouteController.dispatch()` 将响应头写回 `HttpServletResponse`
  - 前端 `ChannelConfig.vue` / `PortRoute.vue` 各新增"报文头配置"折叠区（含继承提示）
  - 新增测试：`CharsetConverterTest`（8个）、`HeaderConfigMergerTest`（7个）、`M14ChannelHeaderTest`（2个）、`M17HeaderDispatchTest`（3个），全绿
- **设计文档**：`docs/superpowers/specs/2026-03-27-header-config-design.md`
- **实现计划**：`docs/superpowers/plans/2026-03-27-header-config.md`

---

## 根因分析与预防措施

### 根因：前后端开发缺少交叉验收机制

#### 直接原因
开发 M1-7 时，专注于后端逻辑实现（Controller/Service/Mapper），在后端通过测试后认为单元完成，未同步检查前端页面是否配套完成。前端的菜单、路由、页面、API 封装四个文件均依赖手动新增，属于容易遗漏的"无代码提示"工作。

#### 深层原因
1. **单元验收标准执行不彻底**：`需求拆分与最小实现方案.md` 中 M1-7 的验收标准明确要求前端配置页面，但验收时只运行了后端接口测试，未通过浏览器实际访问验证
2. **前后端是独立工程，没有强依赖关系**：后端测试通过不代表前端已完成，两者需独立核查
3. **没有"交付检查清单"**：每个单元应有标准化的交付清单（后端接口 + 前端页面 + 菜单入口 + 文档更新），现在缺少这个约束

### 预防措施

#### 措施一：每个单元完成时执行标准交付检查清单
每个含前端功能的单元，提交前必须逐项确认：

| 检查项 | 说明 |
|--------|------|
| ☐ 后端接口 | Controller 接口通过 Swagger 可访问 |
| ☐ 前端 API 封装 | `src/api/xxx.js` 已创建，方法名与后端接口一一对应 |
| ☐ 前端页面 | `src/views/xxx/Xxx.vue` 已创建，含列表/表单/操作 |
| ☐ 路由注册 | `src/router/index.js` 已添加对应路由 |
| ☐ 菜单入口 | `SideMenu.vue` 已添加菜单项，点击可跳转页面 |
| ☐ 浏览器验证 | 在 `localhost:5173` 实际操作一遍：打开菜单→新增→保存→列表刷新→删除 |
| ☐ 需求文档更新 | `需求拆分与最小实现方案.md` 和 `开发计划.md` 标记完成状态 |

#### 措施二：在 CLAUDE.md 中明确前端单元完成定义
约定凡是有前端交互的单元，"完成"的定义必须包括"浏览器可操作"，而不仅仅是"后端测试通过"。

#### 措施三：单元开发前先在前端创建占位页面
开发后端之前，先在前端把路由和菜单占位建好（哪怕用 `PlaceholderView`），这样在菜单上就能看到这个功能的入口，后续替换为真实页面时不会遗漏。

#### 措施四（问题3/4/5 补充）：用户交互必须经过 UX 验收，不能只看代码存在

| 易犯错误 | 正确做法 |
|---------|---------|
| 表单输入用自动生成默认值代替用户输入 | 凡用户需要命名的字段，必须提供对话框/输入框让用户填写 |
| 自动触发 API 的操作未检查前置条件 | 触发 API 前验证必要输入是否就绪（如预览接口需要 inputValue 非空） |
| 内部计数器 id 当作业务标识 | 需要被引用的实体必须设计用户可见、可自定义的 `code`/`name` 字段 |

---

## 待解决

### BUG-007：审计库 `powergateway_audit` 表结构缺失导致应用启动失败
- **发现时间**：2026-06-20
- **发现人**：TRAE AI（测试）
- **测试用例**：环境启动前置检查
- **问题描述**：
  1. `powergateway_config` 数据库表结构完整（已执行 `init.sql`）
  2. `powergateway_audit` 数据库存在但无任何表
  3. 应用启动时连接审计数据源，因 `sql_audit_log` 表不存在导致启动失败
  4. 错误信息：MyBatis 查询 `sql_audit_log` 时抛出 `Table 'powergateway_audit.sql_audit_log' doesn't exist`
- **根因分析**：`init.sql` 中审计表 DDL（第 144~157 行）虽然存在，但注释说明"生产环境需单独在审计库执行"。当前环境未在审计库执行该 DDL，导致表缺失。
- **影响范围**：应用完全无法启动，阻塞所有测试执行
- **修复建议**：
  - 方案 A（短期）：在 `powergateway_audit` 库手动执行 `CREATE TABLE sql_audit_log (...)`
  - 方案 B（长期）：应用启动时自动检测审计库表结构，缺失则自动创建（Flyway/Liquibase 或手动 init 逻辑）
- **修复人**：（待开发者 AI 修复）
- **修复状态**：待修复

### BUG-008：`init.sql` 中审计表 DDL 位置与注释不一致
- **发现时间**：2026-06-20
- **发现人**：TRAE AI（测试）
- **测试用例**：代码审查
- **问题描述**：`init.sql` 第 140 行注释说"在独立 powergateway_audit 库中建表"，但实际 DDL 写在配置库脚本内（第 144 行开始，没有 `USE powergateway_audit;` 切换语句）。这导致：
  1. 如果在配置库执行 `init.sql`，审计表会建在配置库而非审计库
  2. 如果在审计库执行，需要手动切库，容易遗漏
- **修复建议**：将审计表 DDL 拆分为独立文件 `init-audit.sql`，或在 `init.sql` 中显式添加 `USE powergateway_audit;` 切换语句
- **修复人**：（待开发者 AI 修复）
- **修复状态**：待修复

### BUG-009：PerfStatAspect 主线程同步查询接口配置增加 DB 开销
- **发现时间**：2026-06-20（代码审查发现，与开发计划 OPT-15 关联）
- **发现人**：TRAE AI（测试）
- **测试用例**：代码审查 / SYS2PerfStatAspectTest
- **问题描述**：`PerfStatAspect.java` 第 34 行在切面主线程中执行 `interfaceConfigMapper.selectById(interfaceId)` 查询数据库，用于获取 `opType`。这违反了审计架构"旁观者"原则，增加了主线程 DB 开销，在高并发场景下可能成为瓶颈。
- **根因分析**：性能统计切面需要在统计记录中写入 `opType`（SELECT/INSERT/UPDATE/DELETE），但当前实现是在主线程同步查询数据库获取，而非从事先缓存的配置中读取。
- **修复建议**（OPT-15 配置热缓存）：
  1. 接口配置发布时，将 `id -> opType` 映射预加载到 Caffeine 本地缓存
  2. `PerfStatAspect` 从 Caffeine 缓存读取 `opType`，避免主线程 DB 查询
  3. 缓存未命中时（新发布接口首次调用），异步回源查询并回填缓存
- **修复人**：（待开发者 AI 修复，关联 OPT-15）
- **修复状态**：待修复

### BUG-010：`sys_config` 表缺少 `value_type` 和 `group_name` 字段导致应用启动失败
- **发现时间**：2026-06-20
- **发现人**：TRAE AI（测试）
- **测试用例**：环境启动前置检查
- **问题描述**：
  1. 应用启动时 `SysConfigService` 查询 `sys_config` 表
  2. 抛出 `Unknown column 'value_type' in 'field list'` 错误
  3. 应用启动失败
- **根因分析**：CHG-005 变更中 `sys_config` 表新增了 `value_type`（VARCHAR(32)）和 `group_name`（VARCHAR(64)）字段，但 `init.sql` 已更新，现有数据库环境未执行 ALTER TABLE 添加这两个字段。
- **影响范围**：应用无法启动
- **修复建议**：
  - 短期：在现有数据库执行 `ALTER TABLE sys_config ADD COLUMN value_type VARCHAR(32) DEFAULT 'string', ADD COLUMN group_name VARCHAR(64) DEFAULT '其他'`
  - 长期：应用启动时自动检测表结构，缺失字段自动添加
- **修复人**：TRAE AI（测试环境已手动修复）
- **修复状态**：已修复（测试环境）/ 待开发者 AI 在代码中完善

### BUG-011：SYS 系统管理模块多个 API 返回 404 未实现
- **发现时间**：2026-06-20
- **发现人**：TRAE AI（测试）
- **测试用例**：SYS-T02~T07 API 测试
- **问题描述**：以下 5 个 SYS 模块 API 均返回 404，后端 Controller 未实现：
  | API | 预期功能 | 实际状态 |
  |-----|---------|---------|
  | GET /api/sys-config/list | 系统配置列表 | 404 |
  | GET /api/sys-log/list | 操作日志列表 | 404 |
  | GET /api/sql-audit-log/list | SQL 审计日志列表 | 404 |
  | GET /api/perf-stat/list | 性能统计列表 | 404 |
  | GET /api/perf-stat/stats | 统计数据汇总 | 404 |
- **根因分析**：这些功能在需求文档中有定义，AOP 切面（`SysLogAspect`、`SqlAuditAspect`、`PerfStatAspect`）已正确拦截并记录数据到数据库，但对应的查询/列表 Controller 接口尚未实现。
- **影响范围**：管理台无法查看日志、审计、性能统计等数据
- **修复建议**：补充实现以下 Controller：
  1. `SysConfigController` 增加列表查询接口
  2. `SysLogController` 增加操作日志列表查询
  3. `SqlAuditLogController` 增加审计日志列表查询
  4. `PerfStatController` 增加性能统计列表和汇总接口
- **修复人**：（待开发者 AI 修复）
- **修复状态**：待修复

### BUG-012：用户表 `create_time` 字段为 null
- **发现时间**：2026-06-20
- **发现人**：TRAE AI（测试）
- **测试用例**：SYS3UserTest / API 测试
- **问题描述**：`GET /api/user/list` 返回的用户数据中，`createTime` 字段为 null
- **根因分析**：`DataInitializer.java` 创建 admin 用户时未设置 `createTime` 字段，虽然数据库表定义了 `DEFAULT CURRENT_TIMESTAMP`，但 MyBatis-Plus 插入时可能传入了 null 值覆盖了默认值。
- **修复建议**：
  1. `DataInitializer.java` 中创建用户时显式设置 `createTime`
  2. 或在 `SysUser` 实体类中给 `createTime` 字段添加 `@TableField(fill = FieldFill.INSERT)` 注解
- **修复人**：（待开发者 AI 修复）
- **修复状态**：待修复

---

## 待验收（已修复，等待人工确认）

### CHG-005 F-4：空字符串固定值回填验证

- **关联变更**：CHG-005 / `FieldMapping.vue:524`
- **修复内容**：`fixedValue || null` 改为 `fixedValue ?? null`，防止空字符串被丢失
- **验收步骤**：
  1. 启动后端和前端
  2. 进入"字段映射配置"，新建或编辑一条模板
  3. 为某个映射规则勾选"固定值"，将输入框**清空**（空字符串）后保存模板
  4. 重新打开该模板的编辑弹窗
  5. **预期**：该规则仍显示为"固定值"模式，输入框为空（而非切换回"源字段"模式）

---

### CHG-005 F-7：损坏模板 JSON 给出用户提示验证

- **关联变更**：CHG-005 / `FieldMapping.vue:517`
- **修复内容**：JSON 解析失败时 catch 中加 `ElMessage.warning('模板映射规则解析失败，已清空规则，请重新配置')`
- **验收步骤**：
  1. 直接修改数据库，将任意一条模板的 `mapping_rule` 字段改为非法 JSON（如 `"{broken"`）
  2. 在字段映射页面加载该模板
  3. **预期**：页面弹出黄色 warning toast，内容包含"模板映射规则解析失败"，规则区显示为空

---

## 已规划（设计完成，待实现）

（暂无）
