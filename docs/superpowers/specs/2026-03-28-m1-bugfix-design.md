# M1 模块全量 Bug 修复批次 — 设计方案

- **日期**：2026-03-28
- **关联变更**：CHG-005
- **影响单元**：M1-2（字段映射配置）
- **修复策略**：方案 A（全量修复）

> **扫描结果修正（2026-03-28 代码核查）**：F-1/F-2/F-3 经实际读取代码确认均为**误报**，代码已正确使用可选链 `?.`。F-5/F-6/F-8/F-9 经核查为合理实现（有注释说明意图或语义正确），不需修复。实际修复的 Bug 只有 F-4 和 F-7，均在 `FieldMapping.vue`。

---

## 问题清单与修复方案

| ID | 文件 | 行号 | 问题描述 | 严重性 | 修复方案 |
|----|------|------|---------|--------|---------|
| F-1 | `FieldProcess.vue` | 326 | 网络错误时访问 `err.response` 未判空 | — | **误报**：代码已用 `err.response?.data?.message` |
| F-2 | `ChannelConfig.vue` | 257 | `formRef.value.validate()` 未做可选链 | — | **误报**：代码已用 `formRef.value?.validate()` |
| F-3 | `PortRoute.vue` | 394 | 同 F-2 | — | **误报**：代码已用 `formRef.value?.validate()` |
| **F-4** | `FieldMapping.vue` | 524 | `fixedValue \|\| null` 将空字符串固定值丢失 | 功能缺陷 | 改为 `fixedValue ?? null`（已修复） |
| F-5 | `PortRoute.vue` | 348 | `retryCount ?? 3` | — | **非问题**：`??` 语义正确，0 是有效值 |
| F-6 | `FormatConvert.vue` | 197 | catch 块静默 | — | **合理**：有注释说明不影响直接转换功能 |
| **F-7** | `FieldMapping.vue` | 517 | JSON 解析异常静默，规则清空无提示 | 体验问题 | catch 中加 `ElMessage.warning(...)` （已修复） |
| F-8 | `TemplateList.vue` | 113 | `latestOnly: true` 硬编码 | — | **合理**：查询语义明确，无需提取常量 |
| F-9 | 全文件 | 各处 | 错误处理风格不统一 | — | **非问题**：各文件 catch 均有合理处理或注释 |

---

## 变更记录对应

本批次所有修复归入 **CHG-005**，写入 `README/变更记录.md`。

- 变更类型：Bug 修复（不改变对外接口行为）
- 按变更规约，Bug 修复若不改变对外行为**不需要**更新需求拆分和开发计划，仅记录变更即可

---

## 测试策略

前端无自动化测试框架，采用手动验收。

| ID | 验收步骤 |
|----|---------|
| F-4 | 在字段映射中配置 `fixedValue = ""`（空字符串），保存模板后重新加载，确认空字符串正确回填（勾选框仍为"固定值"，输入框为空字符串而非切换成源字段模式） |
| F-7 | 直接修改数据库某条模板的 `mapping_rule` 字段为非法 JSON（如 `"{broken"`），在字段映射页面加载该模板，确认弹出 warning toast "模板映射规则解析失败，已清空规则，请重新配置" |

---

## 不在本批次范围内

- 后端代码无需修改（118 个测试全绿，后端逻辑无问题）
- 不涉及数据库结构变更
- 不涉及 API 接口变更
