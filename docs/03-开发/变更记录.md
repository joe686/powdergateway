# PowerGateway 范围变更记录

> 记录所有正式交付后的**范围增减、功能修改、行为变更**。
> 每次变更需同步更新 `需求拆分与最小实现方案.md` 和 `开发计划.md` 对应条目，并在此处存档。

---

## 变更记录模板

```
### [变更编号] 标题（简要描述）

- **日期**：YYYY-MM-DD
- **影响单元**：M1-1 / M2-3 / ...
- **变更类型**：范围新增 / 范围删减 / 功能修改 / Bug修复后行为变化
- **变更前**：原来的范围/行为描述
- **变更后**：修改后的范围/行为描述
- **影响文件**：修改了哪些源文件
- **需求文档更新**：`需求拆分` 第 X 节 / `开发计划` 阶段二
- **原因**：为什么做这个变更（问题单号 / 用户反馈 / 设计遗漏）
```

---

## 已归档变更

---

### CHG-051 · 2026-08-03 · v0.3.9 · Eureka selfRegister REST API 实装(Task 3)

- **日期**:2026-08-03
- **影响单元**:REG-1 注册中心集成(补齐 v0.3.0 SOCK-1 遗留占位)
- **变更前**:`EurekaRegistryClient.register` 只缓存 lastRegistered + log 占位(v0.3.0 Q8=A 拍板延到 v0.3.2 SOCK-5-B · v0.3.2 又延)
- **变更后**:
  - 用 RestTemplate 直接调 Eureka REST API `POST /eureka/apps/{APP}` 完成注册
  - deregister `DELETE /eureka/apps/{APP}/{instanceId}`
  - InstanceInfo JSON 完整字段(hostName/ipAddr/status/port/securePort/dataCenterInfo/vipAddress/metadata/homePageUrl/statusPageUrl/healthCheckUrl · Netflix 规范)
  - instanceId = ip:svc:port · Netflix 默认命名对齐
  - 不引入 spring-cloud-starter-netflix-eureka-client(避免与现有 netflix eureka-client 版本冲突)
  - 异常 catch · log warn 不阻塞主链路 · heartbeat(discover 已实装)兜底重试
  - 测试构造函数 `EurekaRegistryClient(config, eurekaClient, restTemplate)` 允许 mock RestTemplate
- **影响文件**:
  - `service/registry/eureka/EurekaRegistryClient.java`(+90 行)
  - `test/service/registry/eureka/REG1EurekaClientTest.java`(mock RestTemplate + register/deregister 断言)
- **未含**:pg-testkit MultiAppSelfRegister 顺延后续 minor(v0.3.9 只做 backend · pg-testkit 简化到 v0.3.11+)
- **原因**:v0.3.0 SOCK 阶段顺延项收官 · REG-1 完备 · 支持 PG 主动注册到部门 Eureka Server

---

### CHG-050 · 2026-08-03 · v0.3.9 · frontend nginx 镜像 + docker-compose 集成(Task 2)

- **日期**:2026-08-03
- **影响单元**:REL-1 打包发布形态(v0.3.7 CHG-047 后端 Docker 之后 · 补前端形态)
- **变更前**:v0.3.7 docker-compose 只有 mysql/redis/backend · 前端仍需手工 `npm run dev`
- **变更后**:
  - 新建 `frontend/Dockerfile`(多阶段 · node:18-alpine build → nginx:1.25-alpine runtime · CST 时区 · ~30MB)
  - 新建 `frontend/nginx.conf`(SPA history + /api /swagger-ui /v3/api-docs /webjars 全代理 backend:8080 · gzip · assets 长缓存 · 50m 请求体上限)
  - `frontend/.dockerignore` 排除 node_modules / dist / .git
  - `docker-compose.yml` 加 frontend service(image 0.3.9 · 5173:80 端口 · depends_on backend)
  - backend image tag 0.3.7 → 0.3.9 一并升
  - `docs/03-开发/Docker部署方案.md` 更新说明
- **影响文件**:
  - 新建 `frontend/Dockerfile` · `frontend/nginx.conf` · `frontend/.dockerignore`
  - `docker-compose.yml` 修改 header + 加 frontend service + tag 升级
  - `docs/03-开发/Docker部署方案.md` 修改
- **原因**:v0.3.7 顺延项"前端 nginx 镜像 · v0.3.8+ 视需求" · v0.3.9 收官 · REL-1 三形态(便携/标准/Docker)完备

---

### CHG-049 · 2026-08-03 · v0.3.9 · SocketInboundServerManager 自动 lifecycle hook(Task 1)

- **日期**:2026-08-03
- **影响单元**:SOCK-5-A 入站 Socket Server 生命周期(补齐 v0.3.2 遗留)
- **变更前**:INBOUND_SOCKET 接口 publish/disable/delete 时 · Manager 不感知 · 用户需重启后端才能端口占用/释放 · 或手动调 manager API
- **变更后**:
  - `InterfaceConfigService.publish` 后 · 若 type=INBOUND_SOCKET · 自动调 `manager.start(id)` 拉起 Netty ServerSocket
  - `InterfaceConfigService.disable` 后 · 若 type=INBOUND_SOCKET · 自动调 `manager.stop(id)` 释放端口
  - `InterfaceConfigService.delete` 前 · 兜底 stop(id)(draft/disabled 残留防御式)
  - Manager 通过 `@Autowired(required = false)` 注入 · 测试环境无 Bean 不阻塞主链路
  - publish 中 start 用 try/catch · 失败仅日志 · 不回滚已提交的 status=published(避免状态回滚导致后续管理困难)
  - `SocketInboundServerManager` javadoc 更新 · 移除"留后续 minor"标注
- **影响文件**:
  - `service/InterfaceConfigService.java`(+ 3 处 hook + 1 处 required=false 注入)
  - `socket/inbound/SocketInboundServerManager.java`(javadoc 注释更新)
- **原因**:v0.3.2 SOCK-5-A 遗留项收官 · 前端 InterfaceList 的发布/禁用按钮即时生效 · 无需重启

---

### CHG-048 · 2026-08-03 · v0.3.7 收尾 · JSON 骨架折叠树 + 3 页字段加工 tab 化(Task 4)

- **日期**:2026-08-03
- **影响单元**:M2-3/4/5(QueryConfig/InsertConfig/UpdateConfig)· 前端新增复用组件
- **变更前**:v0.3.5 分类型 4 页字段加工只有 JSON textarea 编辑 · 深度嵌套难看
- **变更后**:字段加工 el-form-item 内嵌 el-tabs · 双 tab 切换 · "编辑 (JSON)" textarea + "树预览 (折叠)" JsonTree · 手写 100 行递归组件 · 零新增依赖
- **影响文件**:
  - 新建 `frontend/src/components/JsonTree.vue` · `JsonNode.vue` 递归自引用(defineOptions name)
  - `QueryConfig.vue`/`InsertConfig.vue`/`UpdateConfig.vue` 各加 JsonTree import + processTab ref + el-tabs 结构
- **原因**:v0.3.2 Task 8 JSON 骨架折叠区顺延项 · v0.3.7 收尾交付 · 后续 minor 可换 vue-json-pretty 或加 diff / edit 能力

---

### CHG-047 · 2026-08-03 · v0.3.7 · Docker 部署形态补齐(REL-1 收尾 · Task 5)

- **日期**:2026-08-03
- **影响单元**:REL-1 打包发布形态(补充第三种交付形态)
- **变更前**:REL-1 仅便携版(内嵌 JRE)+ 标准版(依赖预装 JRE)· 无 Docker 编排
- **变更后**:新增 3 制品
  - `backend/Dockerfile`(openjdk-8 jre alpine + jar copy · 时区 CST · ~200MB)
  - `docker-compose.yml`(mysql 8.0.35 + redis 6.2.14-alpine + backend · healthcheck 依赖 · init.sql 自动装载)
  - `docs/03-开发/Docker部署方案.md`(定位/启动步骤/Oracle 场景/三形态对比)
- **Oracle**:pom.xml 已内置 `ojdbc8:21.9.0.0` scope=runtime · 无需 profile · 用户建 Oracle 类型 DB 连接即可
- **未含**:前端 nginx 镜像 · 顺延 v0.3.8+ 视需求补
- **原因**:v0.3.0 SOCK 阶段顺延项 · REL-1 完备 · 提供 demo / CI / 生产候选形态

---

### CHG-046 · 2026-08-03 · v0.3.7 · SOCKET 出站/入站独立配置页(SOCK-2/5 深度集成简化 · Task 2/3)

- **日期**:2026-08-03
- **影响单元**:M2 侧 · 新增 2 前端页(配合 v0.3.0 SOCK-1 后端 + v0.3.2 SOCK-5 后端)
- **变更前**:SOCKET 出站(SOCK-1 v0.3.0) + INBOUND_SOCKET 入站(SOCK-5 v0.3.2) 后端已就绪 · 但前端只能手写 config_json + 直接调 API · 无 UI
- **变更后**:
  - 新建 `SocketOutboundConfig.vue`(SOCKET 出站单页表单)· 基本 + 目标连接 + 请求模板 + 测试连接按钮
  - 新建 `SocketInboundConfig.vue`(INBOUND_SOCKET 入站单页表单)· 基本 + 监听 + 路由提取 + 顶部 info alert 解释链路
  - 加 2 路由 `/interface/socket-outbound` `/interface/socket-inbound` · MenuPermission ADMIN + USER 白名单加 2 项
  - SideMenu "接口定义" 分组加 2 入口
- **设计取舍**:选独立配置页 · 不塞进 InterfaceWizard(9 步 wizard 为 SQL 优化 · 硬塞 SOCKET 会大改) · 与 v0.3.5 分类型 4 页节奏对齐
- **影响文件**:
  - `frontend/src/views/interface/SocketOutboundConfig.vue`(新建 · 218 行)
  - `frontend/src/views/interface/SocketInboundConfig.vue`(新建 · 168 行)
  - `frontend/src/router/index.js`(+2 路由)
  - `backend/src/main/java/com/powergateway/config/MenuPermission.java`(ADMIN/USER 各 +2 项)
  - `backend/src/test/java/com/powergateway/config/UxBMenuPermissionOrderTest.java`(期望值 30 → 32)
  - `frontend/src/components/layout/SideMenu.vue`(+2 menu-item + INTERFACE_PATHS + hasGroupInterfaceDefine)
- **原因**:v0.3.2 Task 8 前端 INBOUND_SOCKET 3-step + SOCK-2 wizard 深度集成顺延项 · v0.3.7 简化交付(独立页 vs wizard 深度集成 · 更 KISS)

---

### CHG-045 · 2026-08-03 · v0.3.7 · CR-007 InterfaceWizard PG 功能号输入补齐(Task 1)

- **日期**:2026-08-03
- **影响单元**:SYS-5 InterfaceWizard + CR-007 双层路由前端接入
- **变更前**:v0.3.1 交付 CR-007 · `interface_config.function_id UNIQUE` 后端就绪 · 前端 InterfaceList 已显示 · 但 InterfaceWizard 无 functionId 输入(v0.3.1 Task 1.5 顺延)
- **变更后**:
  - wizard store 加 `functionId: ''` 字段
  - `SelectInterfaceSteps.vue` step:db 加 el-form-item "PG 功能号" · 提示语指向字典管理 scope=3 · 可选留空
  - `useInterfaceEdit.js` 加 functionId 回填 · edit 场景显示已保存值
  - `buildPayload` 加 functionId · trim + 空串转 undefined · 避免误覆盖
- **影响文件**:
  - `frontend/src/store/wizard.js`(+1 字段)
  - `frontend/src/composables/useInterfaceEdit.js`(+1 行回填)
  - `frontend/src/views/interface/SelectInterfaceSteps.vue`(step:db 加 el-form-item + buildPayload 加字段)
- **原因**:v0.3.1 Task 1.5 顺延项(v0.3.5 未做) · v0.3.7 收官 · CR-007 前端完备

---

### CHG-044 · 2026-08-02 · v0.2.7 分类型 4 页补齐 · 字段加工/分片/字典键/edit 回填(v0.3.5 minor · FB-057 B + FB-054 后半)

- **日期**:2026-08-02
- **影响单元**:M2-3 QueryConfig · M2-4 InsertConfig · M2-5 UpdateConfig · M2-6 DeleteConfig 4 页 UI 补齐
- **来源**:[FB-057 B 分类型 4 页保留 · 补齐新能力](../06-项目管理/反馈簿.md#fb-057) + [FB-054 wizard edit 后半 · 分类型 4 页 edit 回填](../06-项目管理/反馈簿.md#fb-054) · v0.3.1 Task 5 用户 2026-08-02 拍板拆下会话
- **变更前**:分类型 4 页仅覆盖 SYS-5 之前基础能力(SELECT 4 步 · INSERT/UPDATE 3 步 · DELETE 3 步)· 缺 SYS-5 后新增的字段加工/分片/字典能力 + edit 回填 · IA 一致性问题
- **变更后**:
  - **QueryConfig**(4→6 步):基本 → 字段(加字典键列)→ 字段加工(可选) → 分库分表(可选) → 条件 → 预览
  - **InsertConfig**(3→5 步):基本 → 插入表(加字典键列) → 字段加工(可选) → 分库分表(可选) → 保存
  - **UpdateConfig**(4→6 步):基本 → 修改字段(加字典键列) → 字段加工(可选) → 分库分表(可选) → 修改条件 → 保存
  - **DeleteConfig**(3→4 步):基本 → 多表条件 → 分库分表(可选) → 预览与保存(delete 无字段加工/字典键 · 场景无意义)
  - **4 页统一新增**:
    · header 编辑指示 pageTitle(editMode 时显示"编辑 {type}接口 #{id} · {name}")
    · onMounted 优先 edit 分支:route.query.id 有 → getInterface + 反解 form + tables + fields + conditions + processRules + shardConfigId
    · 字段加工 step(简化):processRulesText JSON textarea 双向 · 独立编辑器跳转 /convert/field-process(delete 除外)
    · 分库分表 step(简化):shardConfigId 下拉(listShardConfigs) + 跳转 /interface/shard
    · 字段配置区加"字典键"列:el-option-group 按 systemCode 分组显示 dictKeys(listDictMappings scope=2 前端聚合 · delete 除外)
    · buildConfigJson 加 field.dictKey + processRules · savePayload 加 id + shardConfigId
- **影响文件**(4 前端页):
  - `frontend/src/views/interface/QueryConfig.vue`(+201 -14)
  - `frontend/src/views/interface/InsertConfig.vue`(+152 -10)
  - `frontend/src/views/interface/UpdateConfig.vue`(+149 -19)
  - `frontend/src/views/interface/DeleteConfig.vue`(+81 -14)
- **测试**:前端 npm run build 通过(4 页 bundle 各更新)· 后端无改动(configJson 结构 v0.3.1 已支持 · fields[].dictKey / processRules / shardConfigId 均后端已有字段)
- **未含**:
  - 独立 FieldProcess 编辑器可视化嵌入(v0.3.5 用 JSON textarea 简化 · 独立编辑器跳转)· 后续 minor 可优化为组件内嵌
  - v0.3.2 Task 8 前端 INBOUND_SOCKET 3 step + JSON 骨架折叠区 · 拆 v0.3.7 单独 minor
- **原因**:v0.3.1 Task 5 拆下会话 · 补齐 IA 一致性 · FB-057 B 拍板"分类型 4 页作为高级用户直达路径"须与 InterfaceWizard 能力等价

---

### CHG-042 · 2026-08-02 · SOCK-5 lcpt-bank 入站场景 后端完整能力(v0.3.2 minor · SOCK-5-A/B/C/D + Task 7 整合)

- **日期**:2026-08-02
- **影响单元**:新增 com.powergateway.socket.inbound + socket.outbound + socket.route(4 大件) · 复用 v0.3.0 codec + v0.3.1 CR-007 双层路由 + v0.3.1 trace_id · 扩展 InterfaceConfigService.save + ExecDispatchService.dispatch
- **来源**:[FB-052](../06-项目管理/反馈簿.md#fb-052) · [CR-005 SOCK-5](../06-项目管理/待办与缺陷池.md#cr-005-socket--xml-报文接入闭环)
- **变更前**:v0.3.0 SOCK-1 只做出站 · PG 无入站 Socket 服务端 · 无法接收渠道 TCP XML
- **变更后**(4 子单元 + 整合):
  - **SOCK-5-A** 入站 Socket:SocketInboundConfig + Server(Netty ServerBootstrap 复用 v0.3.0 codec)+ Context(ThreadLocal + MDC.traceId)+ Handler + ServerManager(@PostConstruct 扫描已发布接口拉起)· Q20=C 仅 short 实装
  - **SOCK-5-B** HTTP 出站:HttpOutboundRequest + HttpOutboundExecutor(RegistryFacade.choose + RestTemplate + X-Trace-Id/X-Original-Function-Id header) · **Eureka selfRegister 未实装 · 独立 minor 补齐**(discover 已 verify · selfRegister 只影响联机反向发现 PG)
  - **SOCK-5-C** 编排 InboundSocketOrchestrator(核心):dom4j 手工递归提 //FunctionId(免 jaxen) · v0.3.1 双层路由 · 加载 config 校验 INBOUND_SOCKET + published · XML→Map 提 bizBody/bizHeader · wrapRequest 追加 _originalFunctionId · HttpOutboundExecutor.exec · unwrapResponse · buildResponseXml · Channel 回写 · finally close · AuditContext opType=INBOUND_SOCKET_EXEC · 传 traceId 给三 AOP · 异常 writeError XML
  - **SOCK-5-D** JsonSkeletonRenderer(手工简化 JSONPath $/[n]/.name 交替 · 免 com.jayway.jsonpath): wrapRequest flat/bank 骨架 · unwrapResponse list 优先/body fallback
  - **Task 7 整合**:Handler + Server 加 orchestrator 参数 · ExecDispatchService 加 case INBOUND_SOCKET(400) · InterfaceConfigService.save 校验(SOCKET/INBOUND_SOCKET 不强制 dbConnectionId · INBOUND_SOCKET 必填 inbound + outbound)
- **影响文件**(10 新增后端 · 4 修改后端):inbound/{Config,Context,Handler,Server,ServerManager} + outbound/{Request,Executor} + route/{JsonSkeletonConfig,JsonSkeletonRenderer,InboundSocketOrchestrator} + 修改 route/ExecDispatchService + service/InterfaceConfigService + inbound 现有 Handler/Server
- **测试新增**:SOCK5A/B/C/D 4 测试类 41 用例(9+5+13+14)
- **测试**:后端 mvn test 740 用例全绿(v0.3.1 基线 699 + 41 新增)
- **原因**:v0.3.1 tag 完成后 lcpt-bank 场景 PG 侧完整能力交付 · CR-007 已铺路 · trace_id 三表已就绪 · orchestrator 复用即可
- **未含**:Task 8 前端 INBOUND_SOCKET 3 step(拆下会话 · 归 v0.3.5/7 与分类型 4 页同批)· Task 10 端到端手工验收(需用户配合)· Eureka selfRegister 实装(独立 minor)

---

### CHG-043 · 2026-08-02 · pg-testkit HTTP Mock 联机(v0.3.2 minor · Task 5 · FB-053 简化)

- **日期**:2026-08-02
- **影响单元**:pg-testkit 新增 httpmock 子包 + mocks/http 资源
- **来源**:[FB-053](../06-项目管理/反馈簿.md) · Q21=A pg-testkit 并入 SOCK-5-B
- **变更前**:pg-testkit 只有 TCP Socket Mock(v0.3.0 SOCK-4)· 无 HTTP 联机 Mock · v0.3.2 SOCK-5-B HTTP 出站无法端到端联调
- **变更后**:
  - HttpMockController @Controller 挂 pg-testkit 主 web(端口 8081)· /mock/** 通配 · produces=application/json;charset=UTF-8(中文正确)
  - 递归提 body.head.FunctionId · 加载 mocks/http/{fnId}.json → fallback default.json
  - 日志记 X-Trace-Id + X-Original-Function-Id
  - mocks/http/180345.json:bank 报文样本([{head, body{list}}])· default.json:兜底
- **简化范围**:Eureka MultiApp SelfRegister 未实装 · 用户手工 Eureka 注册 pg-testkit 应用名 · 独立 minor 补齐
- **影响文件**:4 新增(controller + 2 json + 测试类 3 用例)
- **测试**:pg-testkit mvn test 3 用例全绿

---

### CHG-039 · 2026-08-02 · CR-007 双层功能号路由(v0.3.1 minor · Task 1)

- **日期**:2026-08-02
- **影响单元**:新增 com.powergateway.route(ChannelFunctionIdMapper + FunctionIdRouteService + RouteController + ExecDispatchService)· 扩展 interface_config schema + M2-7 ExecController · 复用 FN-12 字典
- **变更类型**:能力新增(渠道方走功能号路由)+ interface_config 加字段 + ExecController.dispatchByType 抽取共享
- **来源**:[CR-007](../06-项目管理/待办与缺陷池.md#cr-007-功能号路由-functionid-routing) + [FB-058](../06-项目管理/反馈簿.md#fb-058) · Q13=A 前置 · Q19=C 复用 interface_config
- **变更前**:渠道方只能直调 `/api/exec/{id}` · 需知内部 interfaceId · 金融行业惯例只知功能号不知 API 路径 · TCP XML 场景无 URL 概念
- **变更后**:
  - `interface_config.function_id VARCHAR(64) NULL` 唯一 · 建议 PG- 前缀区分渠道 functionId
  - `db/migration-v0.3.1-function-id.sql` 幂等 · 老库客户零破坏 · 老数据留空
  - `ChannelFunctionIdMapper.map`:走 FN-12 字典 scope=3 · systemCode=ROUTE · dictKey=channel_to_pg
    · 字典命中返 targetValue · 未命中/查询失败 fallback 返原值(兼容渠道直送 PG 功能号)
  - `FunctionIdRouteService.lookup`:PG 功能号 → interfaceId · Redis 缓存(命中 5min · negative caching 60s)· 缓存不可用透明 fallback DB
  - `RouteController POST /api/route`(免登):
    · 字段名从 sys_config[route.function.id.field] 读默认 functionId
    · 双层:ChannelMapper → RouteService → InterfaceConfig · 前置校验完整
    · **body 追加 _originalFunctionId=渠道原值 透传下游联机**(用户 Q1 明确要求)
    · 支持嵌套 params 或 body 直接 params 双协议
  - `ExecDispatchService`(@Service · 新):从 ExecController 抽 dispatchByType 逻辑 · 供 ExecController + RouteController 共享
  - `InterfaceConfigService.save`:functionId 唯一性校验(排除自身 · 空值允许多接口共存)
  - 前端 `InterfaceList.vue` 加"PG 功能号"列 · FN-09/FN-11 xlsx 概览 sheet 加"PG 功能号"列
- **影响文件**:
  - 后端新增(路由模块 4 + 迁移 SQL):`route/ChannelFunctionIdMapper.java` + `FunctionIdRouteService.java` + `RouteController.java` + `ExecDispatchService.java` + `db/migration-v0.3.1-function-id.sql`
  - 后端修改:`InterfaceConfig.java`(加 functionId)+ `InterfaceSaveRequest.java` + `InterfaceConfigService.java`(唯一校验)+ `ExecController.java`(delegate 到 dispatch service)+ `SaTokenConfig.java`(免登 /api/route/**)+ `InterfaceDocumentService.java` + `ExcelConfigCodec.java`(xlsx 加列)+ `db/init.sql` + `test/init-h2.sql`
  - 前端修改:`api/interface.js` 保留 · `views/interface/InterfaceList.vue` + `dist/*`
  - 测试新增:`CR007FunctionIdSaveTest.java`(6)+ `CR007ChannelFunctionIdMapperTest.java`(6)+ `CR007FunctionIdRouteServiceTest.java`(6)+ `CR007RouteControllerTest.java`(5)= 23 用例
- **测试**:后端 mvn test 全绿 · 前端 build 通过
- **原因**:2026-08-02 Q13=A 用户拍板 CR-007 前置到 v0.3.1(为 v0.3.2 SOCK-5-C lcpt-bank 入站编排铺路 · 让 SOCK-5-C 只复用不重造路由)
- **注**:前端 InterfaceWizard 元数据 step 加 functionId 输入延到 v0.3.7(与 SOCK-2 wizard 深度集成同批)· 用户 v0.3.1 可通过 Swagger POST /api/interface-config/save 传 functionId

---

### CHG-040 · 2026-08-02 · CR-003 版本显示扩展形态(v0.3.1 minor · Task 2)

- **日期**:2026-08-02
- **影响单元**:新增 sys_app_info 表 + AppInfoController + AppInfoBadge 组件 · 改造 MainLayout SideMenu + LoginView
- **变更类型**:新增能力(版本显示)
- **来源**:[CR-003](../06-项目管理/待办与缺陷池.md#cr-003-程序版本显示) + [FB-042](../06-项目管理/反馈簿.md#fb-042) · Q14=A 前置到 v0.3.1
- **变更前**:PG 无对外可见版本信息 · 用户运维/演示时不知当前跑的版本
- **变更后**:
  - `sys_app_info` 表(H2 + MySQL 双兼容):version/build_time/git_sha/author(默认光斓)/release_note
  - `SysAppInfoInitializer @PostConstruct` 启动 upsert:若无当前版本记录则插入 · 已有跳过(保留"防篡改")
  - `AppInfoController GET /api/app-info`(免登):
    · 返 {version, buildTime, gitSha, author, releaseNote, currentDate}
    · currentDate 走 SimpleDateFormat("yyyy年M月d日", Locale.CHINA) · 中文格式
    · 表未初始化 fallback 到 Initializer 常量
  - 前端 `AppInfoBadge.vue` 通用微小徽章组件:图标 + version · el-popover 弹版本卡片(程序/作者/日期/Build/构建时间/note)
  - `SideMenu.vue` 底部挂 AppInfoBadge(.side-footer 绝对定位)
  - `LoginView.vue` 登录框下方挂 AppInfoBadge(text-align center)
- **影响文件**:
  - 后端新增:`model/SysAppInfo.java` + `dao/SysAppInfoMapper.java` + `config/SysAppInfoInitializer.java` + `controller/AppInfoController.java` + `db/migration-v0.3.1-app-info.sql`
  - 后端修改:`SaTokenConfig.java`(免登 /api/app-info)+ `db/init.sql` + `test/init-h2.sql`
  - 前端新增:`api/appInfo.js` + `components/layout/AppInfoBadge.vue`
  - 前端修改:`components/layout/SideMenu.vue` + `views/LoginView.vue` + `dist/*`
  - 测试新增:`CR003AppInfoTest.java`(3)
- **测试**:后端 mvn test 全绿 · 前端 build 通过
- **原因**:2026-08-02 Q14=A 用户明确"版本号 + 作者(光斓)+ 中文日期 + 测试版本注 · 存 H2 表防篡改 · 前端展示不起眼"

---

### CHG-041 · 2026-08-02 · trace_id 三表补齐 + TraceIdFilter + AOP MDC(v0.3.1 minor · Task 6 · Q23=A)

- **日期**:2026-08-02
- **影响单元**:扩展 sys_log/sql_audit_log/perf_stat 三表 + 三 AOP · 新增 TraceIdFilter · 前端 LogList 加列
- **变更类型**:能力扩展(跨表追溯)
- **来源**:[GAP 梳理维度 6](../06-项目管理/复盘诊断/能力GAP梳理-2026-07-28.md) · Q23=A 前置到 v0.3.1
- **变更前**:sys_log/sql_audit_log/perf_stat 三表独立 · 无 trace_id 关联 · SOCK/exec 场景一次请求跨表排查困难
- **变更后**:
  - 三表加 `trace_id VARCHAR(64) NULL + 索引` · 幂等迁移
  - `TraceIdFilter`(@Order HIGHEST):请求进入生成 UUID(去 -)或沿用上游 X-Trace-Id · MDC.traceId + 响应头 X-Trace-Id · finally 清理
  - `SqlAuditAspect / SysLogAspect / PerfStatAspect`:log.setTraceId(MDC.get) · AuditContext.traceId 允许 dispatch 覆盖
  - `AuditContext.traceId` 字段
  - 前端 `LogList.vue` 操作日志 tab + SQL 审计 tab 各加"Trace"列(前 8 字符 + title 全值 · monospace 紫色)
- **影响文件**:
  - 后端新增:`config/TraceIdFilter.java` + `db/migration-v0.3.1-trace-id.sql`
  - 后端修改:`SysLog.java` + `SqlAuditLog.java` + `PerfStatRecord.java` + `AuditContext.java` + `SqlAuditAspect.java` + `SysLogAspect.java` + `PerfStatAspect.java` + `db/init.sql` + `test/init-h2.sql`
  - 前端修改:`views/system/LogList.vue` + `dist/*`
  - 测试新增:`Task6TraceIdTest.java`(5)
- **测试**:后端 mvn test 全绿 · 前端 build 通过
- **原因**:v0.3.0 SOCK-1 交付后 · SOCK 场景排查跨表需求凸显 · Q23=A 拍板前置 v0.3.1(v0.5.0 只补 business_op_log 第四表)
- **注**:后端 GET /api/logs/trace/{traceId} 跨表关联查询 API + 前端追溯按钮延后(用户可用 SQL 手工查 · minor 补齐)

---

### CHG-037 · 2026-08-02 · SOCK-1 后端出站 Socket + XML 报文接入(v0.3.0 minor · CR-005)

- **日期**:2026-08-02
- **影响单元**:新增 SOCK-1(Netty TCP Client + 三分帧编解码 + 双编码) · 扩展 M2-7 ExecController.dispatchByType(加 SOCKET 分支) · 复用 M1-1 FormatConverter.flattenMap
- **变更类型**:能力新增(出站 TCP Socket 通道)+ interface_config.type 枚举扩展 + FormatConverter.flattenMap 升级为 public static
- **来源**:[FB-052](../06-项目管理/反馈簿.md#fb-052) · [CR-005](../06-项目管理/待办与缺陷池.md#cr-005-socket--xml-报文接入闭环)
- **变更前**:PG 出口通道仅 DB CRUD(SELECT/INSERT/UPDATE/DELETE) · 无 TCP Socket + XML 报文接入 · 无法覆盖金融/银行 lcpt-host 场景
- **变更后**:
  - **新增 com.powergateway.socket** 后端模块:
    - `SocketClient`:Netty Bootstrap · 短连接(SO_KEEPALIVE=false)· 全局共享 NioEventLoopGroup(4 worker daemon · JVM shutdownHook)· 同步 send-receive · IdleStateHandler 触发超时
    - `SocketExecutor`(@Service):解析 config_json.socket → renderTemplate 占位 → send → parseXml + flattenMap → SocketExecResponse
    - `SocketExecRequest / SocketExecResponse` DTO
    - `codec.FramingType` enum · 三值全支持:XML_BOUNDARY / LENGTH_PREFIX_BE4 / LENGTH_PREFIX_BE8(用户 2026-08-02 Q5=C 拍板)
    - `codec.CharsetSupport`:UTF-8 + GBK 白名单(Q6=B)
    - `codec.XmlBoundaryCodec`:ByteToMessageDecoder 识别 &lt;/root&gt; 边界 · 支持 &lt;?xml 属性顺序变体 + 无声明 + 中文根标签
    - `codec.LengthPrefixCodec`:4/8 字节大端头 · 剥头输出纯 payload · 默认最大帧 10MB
    - `exception.SocketConnectException(502)` / `SocketTimeoutException(504)`
  - **改造 ExecController.dispatchByType**:加 `case "SOCKET"` 分支 · AuditContext opType=SOCKET_EXEC · 返 SocketExecResponse(rawXml + flattened Map + latencyMs)
  - **升级 FormatConverter.flattenMap 为 public static**:供 SocketExecutor 复用(backend/CLAUDE.md 关键代码地标已声明)
  - **数据库 schema**:`db/migration-v0.3.0-socket.sql` 幂等 · `interface_config.type` COMMENT 扩 "SELECT/INSERT/UPDATE/DELETE/SOCKET" · 老库客户零破坏(字段类型 VARCHAR(32) 已够 · 仅注释更新)
  - **接口配置 config_json.socket 段结构**:{ip, port, framing, charset, connTimeoutMs?, readTimeoutMs?, connectionMode?, requestTemplate, responseFlattenPrefix?}
  - **connectionMode 契约**:仅 short 实装 · long/pooled 预留 · SocketClient.checkConnectionMode 静态拦截
  - **Eureka selfRegister 注释更新**:占位改说明"归 v0.3.2 SOCK-5-B 补齐"(Q8=A 拍板延后)
  - **依赖新增**:pom `io.netty:netty-all:4.1.100.Final`(Q9=A)
- **影响文件**(11 新增 · 4 修改):
  - 后端新增:`socket/SocketClient.java` + `SocketExecutor.java` + `SocketExecRequest.java` + `SocketExecResponse.java` + `codec/FramingType.java` + `codec/XmlBoundaryCodec.java` + `codec/LengthPrefixCodec.java` + `codec/CharsetSupport.java` + `exception/SocketConnectException.java` + `SocketTimeoutException.java` + `db/migration-v0.3.0-socket.sql`
  - 后端修改:`pom.xml`(netty-all) + `controller/ExecController.java`(SOCKET 分支) + `utils/FormatConverter.java`(flattenMap public static) + `service/registry/eureka/EurekaRegistryClient.java`(注释更新) + `db/init.sql`(type 注释)
  - 测试新增:`SOCK1SocketClientTest.java` + `SOCK1XmlBoundaryCodecTest.java` + `SOCK1LengthPrefixCodecTest.java` + `SOCK1SocketClientIntegrationTest.java` + `SOCK1SocketExecRequestParseTest.java` + `SOCK1SocketExecutorTest.java`(6 测试类 · 共 57 用例)
- **测试**:后端 668 用例全绿(v0.2.5 基线 603 + SOCK 65)· 集成测试用 Netty ServerBootstrap 本地打桩验证三 framing × 双 charset 矩阵
- **需求文档更新**:`需求拆分与最小实现方案.md` 新增 SOCK-1 单元
- **原因**:2026-07-28 用户 FB-052 拍板方案 A + 2026-08-02 24 项决策(Q1/Q5/Q6/Q7/Q8/Q9/Q10/Q11/Q12/Q20/Q22 全定) · lcpt-host 出站场景闭环 · v0.3.0 交付

---

### CHG-038 · 2026-08-02 · SOCK-2/3/4 前端 tab + testConnect + pg-testkit Mock + host 指南(v0.3.0 minor · CR-005)

- **日期**:2026-08-02
- **影响单元**:AUX-1 MessageDebug.vue + 新增 SocketController + pg-testkit SocketMockServer + 制品发布指南
- **变更类型**:调试工具扩展(MessageDebug 加扁平化 tab)+ 后端辅助 API(testConnect)+ pg-testkit 新增独立模块 + 用户文档
- **来源**:[FB-052](../06-项目管理/反馈簿.md#fb-052) · [CR-005](../06-项目管理/待办与缺陷池.md#cr-005-socket--xml-报文接入闭环)
- **变更前**:MessageDebug 只有 JSON↔XML 双向 · 无 XML 扁平化预览 · 无 SOCKET 测试连接 · pg-testkit 无 TCP Mock 兜底 · 无 host 场景演示指南
- **变更后**:
  - **SOCK-3 · MessageDebug XML→JSON 扁平化 tab**(Q10=A):
    - 顶部 radio 新增"XML 扁平化(SOCK)"tab · 配置栏加"扁平化前缀"输入框
    - 前端 `api/tools.js` 新增 `xmlFlatten(xml, prefix)`
    - 后端 `MessageToolsController` `POST /api/tools/xml-flatten` · 免登内部工具 · 返 { flattened, keyCount }
  - **SOCK-2 · SocketController.testConnect 后端 API**:
    - `POST /api/socket/test-connect` · 入 { ip, port, connTimeoutMs? } · 出 { reachable, latencyMs, message }
    - 走 java.net.Socket + InetSocketAddress · 仅探测 TCP 三次握手
    - 校验 port 1~65535 · ip 非空
    - **前端 InterfaceWizard SOCKET 目标类型深度集成延到 v0.3.7**(SelectInterfaceSteps 714 行改动大 · v0.3.0 后端能力已完备 · 用户可通过 Swagger POST /api/interface-config/save 手工创建 SOCKET 接口)
  - **SOCK-4 · pg-testkit TCP Mock 简化版**(Q11=A):
    - 新模块 `com.powergateway.testkit.socket`:SocketMockConfig / SocketMockRule / SocketMockServer
    - 纯 java.net.ServerSocket 阻塞 IO · 避免引入 Netty 到 pg-testkit
    - 仅支持 XML_BOUNDARY + UTF-8(host 场景足够)· length_prefix + GBK 留 v0.3.7
    - YAML 配 socket-mock.rules[functionId → responseFile] · 默认 enabled=false
    - `mocks/181345-response.xml` 示例应答报文(host 场景 · 货币组合产品查询)
  - **Task 12 · host 使用指南文档**(Q12=B · 替代内置 demo):
    - 新建 `docs/05-制品发布/SOCK-lcpt-host-替代指南.md`
    - Swagger UI 一 POST 创建 SOCKET 接口 · 一 POST 发布 · curl exec 触发
    - 5 分钟客户演示脚本 + 故障排查表
- **影响文件**(11 新增 · 3 修改):
  - 后端新增:`controller/MessageToolsController.java` + `controller/SocketController.java`
  - 后端测试新增:`SOCK3XmlFlattenTest.java` + `SOCK2SocketControllerTest.java`(8 用例)
  - 前端新增:`api/tools.js`
  - 前端修改:`views/tools/MessageDebug.vue`(第 3 tab + 前缀输入 + xmlFlatten 调用)
  - pg-testkit 新增:`socket/SocketMockConfig.java` + `SocketMockRule.java` + `SocketMockServer.java` + `resources/mocks/181345-response.xml` + `application.yml`(socket-mock 段)
  - pg-testkit 测试新增:`SocketMockServerTest.java`(4 用例)
  - 文档新增:`docs/05-制品发布/SOCK-lcpt-host-替代指南.md`
- **测试**:后端 668 用例全绿(含 SOCK3XmlFlattenTest 4 · SOCK2SocketControllerTest 4)· 前端 build 通过 · pg-testkit mvn test 4 用例全绿 · pg-testkit compile 通过
- **原因**:CHG-037 后端能力就位后 · 补齐用户使用面(调试 tool + 测试连接 + Mock 兜底 + 使用指南)· Q10/Q11/Q12 拍板归 v0.3.0 · 前端 wizard 深度集成因 SelectInterfaceSteps 改动大延到 v0.3.7 与 4 分类型页补齐同批

---

### CHG-034 · 2026-07-29 · FN-12 v2 scope 拆分 + batch API（v0.2.5 minor · CR-004 ①）

- **日期**：2026-07-29
- **影响单元**：FN-12（v0.2.0 交付后功能补完 · 加 scope 维度分场景 · 加批量 API）
- **变更类型**：范围新增（表结构 + API 新增 + 唯一键扩展 + Redis 缓存 key 加 scope 维度）
- **来源**：[FB-047](../06-项目管理/反馈簿.md#fb-047) + [FB-048](../06-项目管理/反馈簿.md#fb-048) · [CR-004](../06-项目管理/待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转)
- **变更前**:`dict_mapping` 表所有字典条目混挂 · 唯一键 `(system_code, dict_key, direction, source_value)` · 前端所有场景共用同一批数据
- **变更后**:
  - 表加字段 `scope TINYINT NOT NULL DEFAULT 3`（1=M1 转换侧 / 2=M2 可视化侧 / 3=通用共享）
  - 唯一键扩展为 `(scope, system_code, dict_key, direction, source_value)` · 同键值不同 scope 允许并存
  - 迁移 SQL `migration-fn-12-scope.sql` 幂等 · 老库客户 scope=3 兜底 · 不破坏 v0.2.0 已配置字典
  - `DictMappingController` 全部端点(list/systems/save/update/delete/import/export/lookup)加 scope 参数(可选,默认 3)
  - `DictMappingService.lookup(scope, ...)`:DB 查询走 `IN(scope, 3)` fallback · 缓存 key 从 `dict:{sys}:{key}:{dir}` 改为 `dict:{scope}:{sys}:{key}:{dir}`
  - 保留 v0.2.0 老签名重载(scope 默认 3 或 null),向后兼容
  - `DictMappingProcessor` params 中读取 `scope` · 传给 service.lookup 让 M1/M2 集成时有 scope 视角
  - **新增** `POST /api/dict-mapping/batch`:收 `{scope, systemCode, dictKey, direction, items[]}` · 整批事务 · `@Max(200)` 上限 · 超限提示走 FN-11 Excel 导入
- **影响文件**：
  - 后端:`db/init.sql` + `db/migration-fn-12-scope.sql`(新增) + `DictMapping.java` + `DictMappingSaveRequest.java` + `DictMappingVO.java` + `DictMappingBatchSaveRequest.java`(新增) + `DictMappingMapper.java` + `DictMappingService.java` + `DictMappingController.java` + `DictMappingProcessor.java` + `InterfaceDocumentService.java`(适配 mapper 签名) + `test/init-h2.sql`
  - 测试:`FN12DictMappingTest.java` 加 5 用例(scope 拆分 + batch API + M1/M2 fallback) · `M13DictMappingProcessorTest.java` 加 1 用例
- **测试**:后端 598 用例全绿(比 v0.2.0 的 555 多 43 · 含本 CHG 新增 6 用例)
- **需求文档更新**：`需求拆分与最小实现方案.md` FN-12 单元
- **原因**:v0.2.0 FN-12 交付后用户暴露 M1/M2 场景混挂问题 · 用户 2026-07-27 拍板走 v0.2.5 minor 补完

---

### CHG-035 · 2026-07-29 · FN-12 v2 前端菜单拆分 + 批量弹窗 + 跳转联动（v0.2.5 minor · CR-004 ②③④）

- **日期**：2026-07-29
- **影响单元**：FN-12 前端(v0.2.0 CHG-031 补完) · SYS-3 菜单白名单联动
- **变更类型**：IA 结构变更（拆菜单）+ 交互形态变更（弹窗重构）+ 状态跨页保留（新交互）
- **来源**：[FB-047](../06-项目管理/反馈簿.md#fb-047) + [FB-048](../06-项目管理/反馈簿.md#fb-048) + [FB-049](../06-项目管理/反馈簿.md#fb-049) · [CR-004](../06-项目管理/待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转)
- **变更前**:
  - 单一菜单 `/tools/dict` 挂在"辅助工具"下 · M1/M2 混用
  - 新增/编辑对话框每次只能存一条 · 顶部字段每条都要重填
  - 字段选择器无跳转 · 用户找不到字典键就得离开向导手工新建再回来
- **变更后**:
  - **拆两菜单**:`/transform/dict`(scope=1 · 挂"接口转换配置 → 转换规则"分组) + `/interface/dict`(scope=2 · 挂"可视化接口开发 → 接口定义"分组) · 都指向 `DictMappingList.vue` · props 传 scope
  - **下线** `/tools/dict` 路由和辅助工具菜单条目
  - `DictMappingList.vue` 抽通用组件接收 `scope` prop · 顶部显示当前场景标题
  - **批量键值对弹窗**:顶部锁 systemCode/dictKey/direction 三字段 · 下方 `+ 添加一行` · 支持删除行 · 支持粘贴 TSV(Excel 复制) · 超 200 条前端阻断 + 提示走 FN-11 · 提交调 batch API · 错误精准到行号
  - **字段选择器"去新建"跳转**:`DictMappingParamDialog.vue` 底部加"+ 新建字典键"链接 · 点击 → `router.push` 到对应侧字典菜单(按 scope 走 /transform/dict 或 /interface/dict) · URL query 预填 `?system=X&dictKey=Y&direction=Z&returnTo=<原页 fullPath>` · wizardStore 已有 localStorage 自动持久化 · 状态自动保留
  - **回跳 auto-select**:`DictMappingList.vue` mount 时检测 `route.query` 预填批量弹窗 · 保存后写 `sessionStorage['pg-dict-return-value']={system,dictKey,direction}` · `router.push(returnTo)` · 原页 mount 时读 sessionStorage · store.invalidate + auto-select
  - **scope 视角过滤**:`dictMapping` store 加 `groupedForScope/systemsForScope/dictKeysForScope` getter · scope=1 显示 (1,3) · scope=2 显示 (2,3) · scope=3 只显示 3
  - **后端菜单白名单** `MenuPermission.java` 更新 ADMIN/USER/READONLY · 移除 `/tools/dict` · 加 `/transform/dict`(admin+user)、`/interface/dict`(admin+user+readonly)
- **影响文件**：
  - 前端:`api/dictMapping.js`(加 batch + scope 参数) + `stores/dictMapping.js`(加 scope 过滤 getter) + `views/tools/DictMappingList.vue`(完全重写:接 scope prop + 批量弹窗 + returnTo 回跳) + `components/dict/DictMappingParamDialog.vue`(加 scope prop + 跳转链接 + auto-select) + `views/convert/FieldProcess.vue` + `views/convert/TransformInterfaceSteps.vue`(scope=1) + `views/interface/SelectInterfaceSteps.vue`(scope=2) + `router/index.js`(拆两条路由 · 下线 tools/dict) + `components/layout/SideMenu.vue`(挪位置)
  - 后端:`MenuPermission.java`
  - 测试:`tests/views/TransformInterfaceStepsPart2.spec.js`(补 useRoute mock)
- **测试**:前端 npm run build 通过 + vitest 54 用例全绿
- **原因**:v0.2.0 交付后暴露 IA/效率/流程割裂三点体验瓶颈 · 与 CHG-034 同批交付

---

### CHG-036 · 2026-07-29 · 接口调用体验综合专题(v0.2.5 minor · CR-008)

- **日期**：2026-07-29
- **影响单元**：M2-7 接口发布 · InterfaceList.vue · AUX-2 首页 DashboardView
- **变更类型**：交互扩展(接口列表加 3 按钮) + 组件新增(4 tab 调用示例侧抽屉) + UX 引导补齐(首页指引卡)
- **来源**：[FB-055](../06-项目管理/反馈簿.md#fb-055) + [FB-056](../06-项目管理/反馈簿.md#fb-056) · [CR-008](../06-项目管理/待办与缺陷池.md#cr-008-接口调用体验综合专题fb-055--fb-056)
- **变更前**:接口发布后用户不知如何用外部工具调用 · Swagger UI 顶部 URL 栏容易被误用输入接口路径 · 报"Unable to render this definition"
- **变更后**:
  - `InterfaceList.vue` 每行加 3 按钮:
    - **调用示例**:打开侧抽屉展示 4 tab(curl / Postman JSON / HTTP Raw / Python requests) · 一键复制 · 未发布接口顶部提示"⚠️ 需先发布"
    - **📋 curl**:一键复制 curl 命令到剪贴板(单行 · 无 backslash)
    - **🔗 Swagger**:深链跳 `/swagger-ui/index.html#/接口执行/execute_{id}` · 未发布时 disabled + 提示
  - **新增** `components/interface/InterfaceCallSampleDrawer.vue`(4 tab · 参数样本按接口类型 SELECT/其他 自动生成)
  - **首页 Dashboard 引导卡**:说明 3 步(发布 → 获取示例 → Swagger UI 用法) · 明确警示"不要输入接口路径到 Swagger 顶部 URL 栏"
- **影响文件**：
  - 前端:`views/interface/InterfaceList.vue`(加 3 按钮 + 抽屉引用 + copyCurl/openSwagger/openCallSample 方法) + `components/interface/InterfaceCallSampleDrawer.vue`(新增) + `views/DashboardView.vue`(加 guide-card + 样式)
- **测试**:前端 npm run build 通过 + vitest 54 用例全绿
- **原因**:2026-07-28 用户反馈 Swagger 报错 + 不知怎么用外部工具调用 · CR-008 综合专题 · 与 CR-004 合并 v0.2.5 一起 tag

### CHG-001 M1-1 前端新增模板选择功能

- **日期**：2026-03-25
- **影响单元**：M1-1（格式转换引擎）
- **变更类型**：范围新增
- **变更前**：
  - M1-1 规格明确"不含前端页面"；`FormatConvert.vue` 虽已存在，但只提供直接格式互转（选格式 → 输入报文 → 转换），无法复用已配置的转换模板
- **变更后**：
  - `FormatConvert.vue` 顶部新增"使用模板（可选）"下拉选择器，加载所有已配置模板（`GET /api/template/list`）
  - 选中模板后：源格式/目标格式自动填充并锁定，转换时调用 M1-6 全流程接口 `POST /api/convert`（携带 `templateId`），串联格式转换 + 字段映射 + 字段加工
  - 未选模板时：保持原有直接转换逻辑（`POST /api/format-convert/convert` + `/parse` 并行）
  - `reset()` 同步清除模板选择
- **影响文件**：
  - `frontend/src/views/convert/FormatConvert.vue`
- **需求文档更新**：
  - `需求拆分与最小实现方案.md` → M1-1 范围、不含、实现方案、验收标准全部更新
  - `开发计划.md` → 阶段二 M1-1 行补充前端说明
- **原因**：问题清单 问题6（报文格式转换页面无法选择已配置的模板进行转换）

---

### CHG-002 渠道级别报文头适配配置

- **日期**：2026-03-27
- **影响单元**：M1-4（渠道模板管理）、M1-7（端口分发路由）
- **变更类型**：范围新增
- **变更前**：
  - `PortRouteService.forwardWithRetry()` 中 HTTP 请求头硬编码为 `Content-Type: text/plain`
  - `channel_config` 和 `port_route` 表均无报文头相关字段
- **变更后**：
  - `channel_config` 新增 `header_config TEXT` 列（JSON），存渠道级别默认报文头配置
  - `port_route` 新增 `header_config TEXT` 列（JSON），存端口路由级别覆盖配置
  - 新增 `HeaderConfig` DTO：含 `contentType`、`charset`、`requestHeaders`（KV map）、`responseHeaders`（KV map）
  - 新增 `HeaderConfigMerger` 工具类：两级合并，port_route 优先于 channel_config
  - 新增 `CharsetConverter` 工具类：字节级编码转换（UTF-8/GBK/ISO-8859-1），转发前后自动转码
  - `PortRouteService.dispatch()` 新增 Header 构造步骤：合并配置 → charset 转码 → 注入 HttpHeaders → 返回时注入响应头
  - 前端：`ChannelConfig.vue` 和 `PortRoute.vue` 弹窗各增加"报文头配置"折叠区（Content-Type 选择、Charset 选择、自定义 KV 表格）
- **影响文件**：
  - `backend/src/main/resources/db/init.sql`（两张表各加一列）
  - `backend/src/main/java/com/powergateway/model/ChannelConfig.java`
  - `backend/src/main/java/com/powergateway/model/PortRoute.java`
  - `backend/src/main/java/com/powergateway/model/dto/HeaderConfig.java`（新增）
  - `backend/src/main/java/com/powergateway/utils/HeaderConfigMerger.java`（新增）
  - `backend/src/main/java/com/powergateway/utils/CharsetConverter.java`（新增）
  - `backend/src/main/java/com/powergateway/service/ChannelConfigService.java`
  - `backend/src/main/java/com/powergateway/service/PortRouteService.java`
  - `backend/src/main/java/com/powergateway/model/dto/ChannelSaveRequest.java`
  - `backend/src/main/java/com/powergateway/model/dto/PortRouteSaveRequest.java`
  - `frontend/src/views/convert/ChannelConfig.vue`
  - `frontend/src/views/convert/PortRoute.vue`
- **需求文档更新**：
  - `需求拆分与最小实现方案.md` → M1-4、M1-7 范围、实现方案、验收标准更新
  - `开发计划.md` → 阶段二 M1-4、M1-7 行描述更新
- **原因**：问题清单 问题2（报文头格式需支持渠道级别的适配配置）
- **设计文档**：`docs/02-设计/详细设计/2026-03-27-header-config-design.md`

---

### CHG-004 字段映射 srcField 支持点号路径访问 XML 嵌套字段

- **日期**：2026-03-27
- **影响单元**：M1-2（字段映射配置 — `srcField` 格式约定）、M1-6（报文转换调用接口 — `applyMapping` 实现）
- **变更类型**：范围新增（新增功能点）
- **变更前**：
  - `applyMapping()` 对 `srcField` 只做顶层平铺查找（`srcMap.containsKey(srcField)`）
  - XML 报文被解析为嵌套 Map（如 `{head: {FunctionId: "170350"}, body: {...}}`），`srcField: "FunctionId"` 无法命中 `head` 下的字段，仅能映射顶层 key（`head`、`body`）
- **变更后**：
  - `applyMapping()` 新增 `getNestedValue(map, path)` 方法，支持点号路径递归取值
  - `srcField` 可配置为多级路径，如 `head.FunctionId`、`body.OffSet`，无点号时退化为原有平铺查找（向后兼容）
  - `mapping_rule` 示例：`[{"srcField": "head.FunctionId", "targetField": "functionId"}]`
- **影响文件**：
  - `backend/src/main/java/com/powergateway/service/ConvertService.java`（`applyMapping` + 新增 `getNestedValue`）
  - `backend/src/test/java/com/powergateway/M16ConvertApiTest.java`（新增验收测试 `convert_xmlNestedPath_dotNotationMapping`）
- **需求文档更新**：
  - `需求拆分与最小实现方案.md` → M1-2 实现方案（`srcField` 格式说明）、M1-6 实现方案（`applyMapping` 逻辑）
  - `开发计划.md` → M1-6 行描述
- **原因**：XML 报文存在 `<root><head>...</head><body>...</body></root>` 层级结构，平铺查找无法访问嵌套字段，需支持路径配置

---

### CHG-006 SYS-1 日志归档策略：物理删除改为归档到历史表

- **日期**：2026-05-13
- **影响单元**：SYS-1（日志管理）
- **变更类型**：范围新增（设计阶段调整，实现前变更）
- **变更前**：
  - `SysLogCleanupJob` 每天凌晨读取 `sys.log.retention.days`，将 `sys_log` 中超期记录**物理删除**
- **变更后**：
  - 改为**归档策略**：`SysLogArchiveJob` 将超期记录 `INSERT INTO sys_log_history SELECT ... WHERE op_time < ?`，插入完成后再从 `sys_log` 删除
  - 新增 `sys_log_history` 表（结构与 `sys_log` 相同，额外增加 `archived_time` 字段）
  - 新增后端接口 `GET /api/log/history/list`，参数与 `list` 一致，查历史表
  - 前端"操作日志" Tab 右上角增加「查历史数据」`el-switch`，关闭查 `sys_log`，开启查 `sys_log_history`
- **影响文件**（设计阶段，尚未实现）：
  - `backend/src/main/resources/db/init.sql`（新增 `sys_log_history` 表）
  - `backend/src/main/java/com/powergateway/model/SysLogHistory.java`（新增）
  - `backend/src/main/java/com/powergateway/dao/SysLogHistoryMapper.java`（新增）
  - `backend/src/main/java/com/powergateway/job/SysLogArchiveJob.java`（取代原 CleanupJob）
  - `backend/src/main/java/com/powergateway/controller/SysLogController.java`（新增 history/list 端点）
  - `frontend/src/api/log.js`（新增 `listHistoryLogs`）
  - `frontend/src/views/system/LogList.vue`（新增「查历史数据」开关）
- **需求文档更新**：
  - `需求拆分与最小实现方案.md` → SYS-1 实现方案、验收标准更新
  - `开发计划.md` → 阶段四 SYS-1 行描述更新
- **原因**：物理删除不可逆，归档策略保留历史数据且不影响当前表查询性能
- **设计文档**：`docs/02-设计/详细设计/2026-05-13-sys1-design.md`

---

### CHG-003 修复字段映射/加工页面源字段不显示问题

- **日期**：2026-03-27
- **影响单元**：M1-2（字段映射配置）、M1-3（字段加工引擎前端）
- **变更类型**：Bug 修复（不改变对外行为）
- **根因**：`vue-draggable-next` v2.x 的 `render()` 函数仅读取 `$slots.default`，从不读取 `$slots.item`。代码使用的 `<template #item="{ element }">` 是 `vuedraggable` v4.x（官方包）的 API，在 `vue-draggable-next` 中该 slot 被完全忽略，组件渲染空 div，导致所有手动添加或解析报文得到的源字段均不显示。
- **变更前**：`FieldMapping.vue` 和 `FieldProcess.vue` 中 `<draggable>` 内使用 `<template #item="{ element }">` 渲染列表项
- **变更后**：改为 `vue-draggable-next` 兼容的 default slot + `v-for` 写法，每个列表项通过 `v-for="element in list"` 直接渲染
- **影响文件**：
  - `frontend/src/views/convert/FieldMapping.vue`（srcFields 和 mappingRules 两处 draggable）
  - `frontend/src/views/convert/FieldProcess.vue`（rules draggable）

---

### CHG-005 M1 前端 Bug 修复批次

- **日期**：2026-03-28
- **影响单元**：M1-2（字段映射配置）
- **变更类型**：Bug 修复（不改变对外接口行为）
- **修复内容**：

| ID | 文件 | 问题 | 修复 |
|----|------|------|------|
| F-4 | `FieldMapping.vue:524` | `fixedValue \|\| null` 将空字符串固定值转为 null，回填时丢失，导致空字符串固定值保存后无法还原 | 改为 `fixedValue ?? null`（`??` 只在 null/undefined 时取默认值，空字符串被保留） |
| F-7 | `FieldMapping.vue:517` | 模板 `mappingRule` JSON 解析异常被静默吞掉，规则区清空但用户毫无感知，无法区分"空规则"和"数据损坏" | catch 中加 `ElMessage.warning('模板映射规则解析失败，已清空规则，请重新配置')` |

- **影响文件**：
  - `frontend/src/views/convert/FieldMapping.vue`
- **需求文档更新**：无（Bug 修复不改变对外行为，按变更规约无需更新需求拆分和开发计划）
- **设计文档**：`docs/02-设计/详细设计/2026-03-28-m1-bugfix-design.md`
- **原因**：代码扫描发现，F-4 会导致空字符串固定值无法持久化；F-7 会导致模板数据损坏时用户无感知

---

### CHG-007 AUX-2 首页报表范围扩展

- **日期**：2026-05-20
- **影响单元**：AUX-2
- **变更类型**：范围扩展（设计阶段调整，实现前变更）
- **变更前**：4 数字卡片 + 1 折线图 + 告警列表 + 快捷入口（共 4 个区块）
- **变更后**：5 数字卡片（新增缓存命中率） + 调用趋势折线 + opType 柱图 + 接口状态饼图 + TOP5 慢接口表 + 未处理告警列表 + 快捷入口 + today/week/month 维度切换（共 8 个区块）
- **原因**：原范围仅 4 个区块对运营价值不足，扩展为可指导运维决策的综合报表中心；底层数据均来自已建模块（SYS-2/M2-10/M2-7），不新增数据采集逻辑
- **后端 API**：新增 `GET /api/home/overview?dimension=today|week|month`（聚合接口）
- **前端**：改造 `frontend/src/views/DashboardView.vue`（路由 `/dashboard` 不变）
- **需求文档更新**：`需求拆分与最小实现方案.md` → AUX-2 节整体替换；`开发计划.md` → 阶段五 AUX-2 行描述更新

---

### CHG-008 测试工具独立为 pg-testkit 项目（与项目本体分离）

- **日期**：2026-06-20
- **影响单元**：测试工具计划（不属于 28 个交付单元，独立测试基础设施）
- **变更类型**：范围新增 + 架构调整（测试工具从内嵌 backend 改为独立项目）
- **变更前**：
  - `测试工具计划.md` 第六节规划测试工具位于 `backend/src/test/java/com/powergateway/testkit/`，内嵌在 backend 项目内
  - 测试工具与 backend 共享 pom.xml 依赖、共享 Spring 上下文
  - 未实际创建任何测试工具代码
- **变更后**：
  - 测试工具独立为仓库根目录的 `pg-testkit/` 项目，与 `backend/`、`frontend/` 平级
  - 独立 `pom.xml`（Spring Boot 2.7.18 + spring-boot-starter-web + spring-boot-starter-jdbc + mysql-connector-java + lombok），**不依赖 backend jar**
  - 独立 `PgTestKitApplication` 启动类，默认端口 8081（不与 backend 8080 冲突）
  - 通过 HTTP REST 调用 PG 后端、通过 JDBC 直连配置库/审计库完成校验，**不引用 backend 任何内部类**
  - 实现阶段一全部类：
    | 类 | 位置 | 说明 |
    |----|------|------|
    | `PgTestKitApplication` | `src/main/java` | 独立 Spring Boot 启动类 |
    | `MockServerService` | `src/main/java/.../mock` | 内嵌 HTTP Mock 服务器（JDK HttpServer，默认 9999 端口） |
    | `MockServerController` | `src/main/java/.../mock` | Mock 管理 API（start/stop/reset/configure/requests/verify） |
    | `MockResponseRule` | `src/main/java/.../mock` | 响应规则配置（路径/方法/头/体匹配 + 延迟模拟） |
    | `RequestRecord` | `src/main/java/.../mock` | 请求记录 |
    | `TestDataFactory` | `src/main/java/.../data` | 测试数据生成工厂（登录/模板/渠道/路由/接口/转换/分发） |
    | `TestScenario` | `src/main/java/.../scenario` | 测试场景定义（步骤 + 预期） |
    | `TestScenarioRunner` | `src/main/java/.../scenario` | 场景执行器（login/createTemplate/convert/dispatch/verifyDb/verifyMock） |
    | `TestApiController` | `src/main/java/.../api` | `/test/*` API（health/db/query/run-scenario/results） |
    | `PgAssertions` | `src/test/java/.../assertkit` | PG 专用断言工具（依赖 JUnit，测试期可用） |
    | `IntegrationTestBase` | `src/test/java/.../base` | 全链路测试基类（依赖 spring-boot-test） |
    | `PgTestKitApplicationTest` | `src/test/java` | 上下文加载冒烟测试 |
  - `assertkit` 包名避开 Java 保留字 `assert`
  - `PgAssertions` 和 `IntegrationTestBase` 因依赖 JUnit / spring-boot-test（test scope），放在 `src/test/java/` 下
- **影响文件**（新增）：
  - `docs/04-测试/测试工具计划.md`（更新第 2.2、5.1、六节，反映独立项目结构）
  - `pg-testkit/pom.xml`
  - `pg-testkit/README.md`
  - `pg-testkit/src/main/resources/application.yml`
  - `pg-testkit/src/main/java/com/powergateway/testkit/PgTestKitApplication.java`
  - `pg-testkit/src/main/java/com/powergateway/testkit/mock/{MockServerService,MockServerController,MockResponseRule,RequestRecord}.java`
  - `pg-testkit/src/main/java/com/powergateway/testkit/data/TestDataFactory.java`
  - `pg-testkit/src/main/java/com/powergateway/testkit/scenario/{TestScenario,TestScenarioRunner}.java`
  - `pg-testkit/src/main/java/com/powergateway/testkit/api/TestApiController.java`
  - `pg-testkit/src/test/java/com/powergateway/testkit/assertkit/PgAssertions.java`
  - `pg-testkit/src/test/java/com/powergateway/testkit/base/IntegrationTestBase.java`
  - `pg-testkit/src/test/java/com/powergateway/testkit/PgTestKitApplicationTest.java`
- **需求文档更新**：无（测试工具不属于 28 个交付单元，不影响需求拆分和开发计划中的功能范围）
- **原因**：用户要求"测试工具要跟项目本体分开"。独立项目避免测试工具与 backend 耦合，便于独立维护和部署，且不污染 backend 的依赖和构建产物
- **验证**：`mvn compile` + `mvn test-compile` 均通过

---

### CHG-009 使用 pg-testkit 测试工具实测项目 + 修复启动阻塞问题

- **日期**：2026-06-20
- **影响单元**：测试工具验证（覆盖 M1-3 报文转换、M1-7 端口分发、SYS-1 操作日志、SYS-2 系统配置）
- **变更类型**：缺陷修复（数据库初始化）+ 测试工具缺陷修复 + 实测验证

#### 1. 修复的阻塞问题

| 问题 | 原因 | 修复 |
|------|------|------|
| 配置库缺少 `sys_log`/`sys_log_history`/`perf_stat`/`perf_alert` 表 | init.sql 未完整执行 | 手动补建 4 张表（DDL 对齐 init.sql） |
| `sys_config` 只有 4 条配置（应 8 条） | init.sql 的 INSERT 未完整执行 | 补插 4 条缺失配置（sys.log.retention.days/log_menu_enabled/alert_fail_rate/alert_response_ms） |
| pg-testkit 启动失败：`Failed to determine a suitable driver class` | 引入 spring-boot-starter-jdbc 触发 DataSource 自动配置，但测试工具用手动 JdbcTemplate | `PgTestKitApplication` 添加 `@SpringBootApplication(exclude = {DataSourceAutoConfiguration.class})` |

#### 2. 测试工具自身发现的问题（待后续改进）

| 问题 | 说明 | 影响 |
|------|------|------|
| `TestScenarioRunner` 缺少步骤间数据传递 | `createTemplate` 返回的模板 ID 无法自动注入到后续 `convert` 步骤的 `templateId` 参数，需手动硬编码 | 中：影响自动化场景编排，当前需分步执行或硬编码 ID |
| `TestScenarioRunner` 缺少 `createChannel`/`createPortRoute` 动作 | 端口分发场景无法纯靠 run-scenario 完成，需额外调用后端 API | 中：端口分发全链路场景需混合使用 run-scenario + 直接 API 调用 |

#### 3. 实测结果

测试环境：MySQL80 + Redis + PG 后端(8080) + pg-testkit(8081) + Mock(9999)

| 测试项 | 方式 | 结果 |
|--------|------|------|
| pg-testkit 健康检查 | `GET /test/health` | ✅ UP |
| PG 后端健康检查 | `GET /api/health` | ✅ code=200 |
| 配置库连接 + 数据查询 | `POST /test/db/query`（sys_user/sys_config） | ✅ admin 用户 + 8 条配置 |
| 报文转换全链路 | `POST /test/run-scenario`（登录→建模板→转换→验证审计库） | ✅ 3/3 通过，JSON→XML 转换正确（耗时 28ms） |
| Mock 服务器配置 | `POST /test/mock-server/configure` | ✅ 规则配置成功 |
| 端口分发全链路 | 登录→建渠道→建路由→`POST /api/dispatch`→验证 Mock | ✅ PG→Mock 双向通信正常（耗时 49ms），Mock 收到 1 条请求 |
| Mock 请求自动验证 | `POST /test/mock-server/verify` | ✅ matched=1, passed=true |
| 操作日志验证 | `POST /test/db/query`（sys_log） | ✅ 5 条日志（登录/模板/渠道/路由），SYS-1 正常 |

#### 4. 影响文件（修改）

- `pg-testkit/src/main/java/com/powergateway/testkit/PgTestKitApplication.java`（添加 exclude DataSourceAutoConfiguration）
- `docs/03-开发/变更记录.md`（本条 CHG-009）

#### 5. 数据库变更（手动执行，非代码文件）

- `powergateway_config` 库：新建 `sys_log`、`sys_log_history`、`perf_stat`、`perf_alert` 表
- `powergateway_config.sys_config` 表：插入 4 条缺失配置

- **验证**：报文转换 + 端口分发 + 操作日志全链路实测通过

---

### CHG-010 问题清单 BUG-007~012 批次修复

- **日期**：2026-06-20
- **影响单元**：M2-9（SQL 审计）、SYS-1（日志管理）、SYS-2（性能统计）、SYS-3（用户管理）、SYS-4（系统配置）、M2-7（接口发布）
- **变更类型**：Bug 修复（含行为变化与新增端点）
- **修复内容**：

| BUG ID | 问题 | 修复方案 |
|--------|------|---------|
| BUG-007 | 审计库 `powergateway_audit` 表结构缺失导致应用启动失败 | 审计表 DDL 拆分到独立文件 `init-audit.sql`，显式 `USE powergateway_audit;` |
| BUG-008 | `init.sql` 中审计表 DDL 位置与注释不一致 | `init.sql` 移除审计表 DDL，添加注释指向 `init-audit.sql` |
| BUG-009 | `PerfStatAspect` 主线程同步查询 DB 获取 opType | 新增 `InterfaceOpTypeCache`（Caffeine 缓存），发布时预加载，禁用/删除时清除 |
| BUG-010 | `sys_config` 表缺少 `value_type`/`group_name` 字段 | 新增幂等迁移脚本 `migration-sys-config.sql`（存储过程检查字段存在性） |
| BUG-011 | SYS 模块 5 个 API 返回 404 | 新增 4 个 Controller 提供期望路径端点，复用现有 Service |
| BUG-012 | 用户表 `create_time` 为 null | `DataInitializer` 显式设置时间 + 新增 `MyMetaObjectHandler` 自动填充 |

- **变更前**：
  - 审计表 DDL 混在配置库 `init.sql` 中，无 `USE` 切换语句
  - `PerfStatAspect` 每次调用都同步查询 `interface_config` 表
  - `/api/sys-config/list`、`/api/sys-log/list`、`/api/sql-audit-log/list`、`/api/perf-stat/list`、`/api/perf-stat/stats` 返回 404
  - admin 用户 `createTime` 为 null
- **变更后**：
  - 审计表 DDL 独立到 `init-audit.sql`，配置库与审计库初始化脚本分离
  - `PerfStatAspect` 从 Caffeine 缓存读取 opType，命中率接近 100%，主线程无 DB 查询
  - 5 个 API 端点可正常访问，与现有 `/api/config/*`、`/api/log/*`、`/api/stats/*` 共存
  - admin 用户 `createTime` 正确填充，`MyMetaObjectHandler` 自动填充所有实体的时间字段
- **影响文件**：
  - `backend/src/main/resources/db/init.sql`（移除审计表 DDL，添加迁移注释）
  - `backend/src/main/resources/db/init-audit.sql`（新增，审计库初始化脚本）
  - `backend/src/main/resources/db/migration-sys-config.sql`（新增，sys_config 幂等迁移脚本）
  - `backend/src/main/java/com/powergateway/config/DataInitializer.java`（显式设置 createTime）
  - `backend/src/main/java/com/powergateway/config/MyMetaObjectHandler.java`（新增，自动填充处理器）
  - `backend/src/main/java/com/powergateway/service/InterfaceOpTypeCache.java`（新增，opType 缓存）
  - `backend/src/main/java/com/powergateway/aop/PerfStatAspect.java`（改用缓存）
  - `backend/src/main/java/com/powergateway/service/InterfaceConfigService.java`（publish/disable/delete 集成缓存）
  - `backend/src/main/java/com/powergateway/service/PerfStatService.java`（新增 listRecords 方法）
  - `backend/src/main/java/com/powergateway/controller/SysConfigQueryController.java`（新增）
  - `backend/src/main/java/com/powergateway/controller/SysLogQueryController.java`（新增）
  - `backend/src/main/java/com/powergateway/controller/SqlAuditLogController.java`（新增）
  - `backend/src/main/java/com/powergateway/controller/PerfStatController.java`（新增）
  - `backend/src/test/java/com/powergateway/BUG011_012_FixTest.java`（新增验证测试）
- **需求文档更新**：无（Bug 修复为主，新增端点为测试用例期望路径的补充实现）
- **原因**：问题清单 BUG-007~012，阻塞应用启动与系统管理功能验证


---

### CHG-011 2026-06-29 全站 E2E 自主验证 + 6 处 BUG 修复

- **日期**：2026-06-29
- **影响单元**：M2-10（缓存）、SYS-3（菜单白名单）、SYS-5（接口配置向导）、AUX-2（首页）、TopBar、InterfaceList
- **变更类型**：Bug 修复（无对外接口/范围变化）
- **背景**：用户要求"启动浏览器完成全站 E2E 自动化测试，自动修复所有页面 BUG/控制台报错/跳转异常"，一次性闭环执行。本轮逐单元跑通登录 → 报文转换 → 接口配置 → 执行 → 系统管理 → 首页全链路，期间发现 6 处问题并修复。
- **修复内容**：

| BUG | 位置 | 现象 | 根因 | 修复 |
|-----|------|------|------|------|
| E2E-1 | `interface_config` 表 | 登录后 dashboard 弹 "系统异常，请稍后重试"；`/api/home/overview` 500 | 表缺 M2-10 引入的 `cache_enabled`/`cache_ttl_seconds`/`cache_key_template` 三列（用户本地库为更早版本建库） | `ALTER TABLE interface_config ADD COLUMN ...` 补齐三列 |
| E2E-2 | `TopBar.vue:19` | 控制台 3 条 Vue warning：`Property "UserFilled" was accessed during render but is not defined on instance` | `<el-avatar :icon="UserFilled" />` 通过 prop 传值的图标必须显式 import，全局注册的图标只对模板标签生效 | 新增 `import { UserFilled } from '@element-plus/icons-vue'` |
| E2E-3 | `frontend/index.html:5` | `GET /favicon.ico 404` 控制台报错 | 引用了不存在的 favicon | 改为 inline SVG（⚡ emoji）的 data:URI |
| E2E-4 | `InterfaceList.vue:78` | 控制台 3 条 Vue warning：`Component inside <Transition> renders non-element root node that cannot be animated` | 模板有两个根节点（`.interface-list` div + `el-dialog`），与 `<Transition>` 动画要求的单根节点冲突 | 把 `el-dialog` 移到 `.interface-list` div 内 |
| E2E-5 | `MenuPermission.java:14-34` | admin 角色访问 `/interface/wizard` 被路由守卫踢回 dashboard | `ADMIN_MENUS` 和 `USER_MENUS` 白名单都漏了 `/interface/wizard` 路径（路由存在但未授权） | 两个白名单各补一项 `/interface/wizard`（需 backend 重启生效） |
| E2E-6 | `InterfaceWizard.vue:139` | 进入向导页面立即崩溃：`TypeError: Cannot read properties of undefined (reading 'tableName')` | `<el-select v-model="wizard.tables[0].tableName" ...>` 在 `interfaceType=''`（初始态）走 v-else 分支，但 `wizard.tables` 还是空数组 | 给 `el-form-item` 加 `v-if="wizard.tables[0]"` 保护 |

- **影响文件**：
  - `backend/src/main/java/com/powergateway/config/MenuPermission.java`（补 `/interface/wizard`）
  - `frontend/src/components/layout/TopBar.vue`（import UserFilled）
  - `frontend/src/views/interface/InterfaceList.vue`(改根节点结构)
  - `frontend/src/views/interface/InterfaceWizard.vue`(加 v-if 保护)
  - `frontend/index.html`（inline SVG favicon）
  - DB 改动：`ALTER TABLE interface_config ADD COLUMN cache_enabled ...`（本地 mysql 已执行）

- **未修项（已记录到 E2E 报告，留待后续判断）**：
  - 表结构 `column.remarks` 中文乱码（JDBC INFORMATION_SCHEMA 编码）— 建议在 db_connection 默认 URL 加 `useInformationSchema=true&useUnicode=true`，触及面较广，单独处置
  - `el-radio` 的 `label` 属性 v3 deprecation warning（element-plus 上游软提醒，3 条/页）
  - 模板列表脏数据 `????_e2e`、`sys_config` 默认分组错位（历史数据，非代码问题）
  - CSV 多行 / JSON 数组互转 — 路线图 OPT-16 已标记为增强项，当前为单 Map 设计约束
  - SQL 审计未记录 SELECT — 实现设计选择
  - SYS-5 实现 10 步（路线图描述 9 步）— 已交付实现差异，不改

- **验收**：登录 → 22 业务页面（含 5173 全部路由）逐项控制台均 0 错 0 警告（除 SYS-2 性能页 3 条 element-plus deprecation 软提醒外）；M1-1/M1-6 报文转换 + M2-3/M2-7 接口配置-发布-执行链路 API 验证通过。
- **报告**：`docs/04-测试/E2E验证报告-2026-06-29.md`

---

### CHG-012 2026-06-29 第二轮深度 E2E + 3 处审计/缓存 BUG 修复

- **日期**：2026-06-29
- **影响单元**：M2-4 INSERT、M2-9 SQL 审计、M2-10 双层缓存、M2-2 表结构、SYS-2 性能页、SYS-4 系统配置
- **变更类型**：Bug 修复 + 文档同步
- **背景**：用户授权进入自主连续模式。第二轮跑了 M2-4/5/6 真实执行链路、M2-10 缓存命中实测、M2-8 分库分表路由验证，期间发现 3 处实际 BUG（其中 1 处是 INSERT 完全没审计的真隐患），并修复 OBS-1/2/5/6 列出的 4 项遗留观察。

- **新发现并已修 BUG**：

| BUG | 位置 | 现象 | 根因 | 修复 |
|-----|------|------|------|------|
| E2E-7 | `config/CacheConfig.java:27` | M2-10 缓存命中统计永远 0（hitCount/missCount=0），所有 cache_hit:*/cache_miss:* Redis key 不存在 | `@ConditionalOnBean(RedisConnectionFactory.class)` 在 backend 启动顺序敏感情况下判定为 false，`cacheRedisTemplate` Bean 不创建，`QueryCacheManager.redisTemplate` 注入 null；`incrHit`/`incrMiss`/`getStats` 全部依赖 redisTemplate，于是永久跳过 | 去掉 `@ConditionalOnBean` — Spring 容器中 RedisConnectionFactory 真不存在时 Bean 方法不会被调用，无需条件守护 |
| E2E-8 | `service/InterfaceConfigService.executeInsert` + `controller/ExecController.INSERT` | INSERT 接口 exec 后 `audit_sql_log` 完全没记录（UPDATE/DELETE 都有） | `executeInsert` 漏标 `@AuditLog` 注解；`ExecController` 的 INSERT 分支没设置 `AuditContextHolder`（UPDATE/DELETE 都设了） | `executeInsert` 加 `@AuditLog`；ExecController 的 INSERT 分支补 `AuditContextHolder.set(new AuditContext().setInterfaceId(...).setOpType("INSERT").setTargetDb(...))` |
| E2E-9 | `service/InterfaceConfigService.executeInsert:359` | INSERT 审计虽然落了，但 `target_table` 字段始终为 NULL | UPDATE/DELETE 在方法内部 set targetTable，INSERT 没有相应代码 | 在 INSERT 路径加 `auditCtx.setTargetTable(tables.stream().map(TableInsertConfig::getTableName).collect(joining(",")))` |

- **完成 OBS 项修复**：

| OBS | 处理结果 | 实际改动 |
|-----|---------|---------|
| OBS-1 表结构注释中文乱码 | **部分缓解**（10% 列已正确，多数仍乱） | `TableMetaService.normalizeUrlForMetaQuery` 在元数据查询时为 URL 注入 `useInformationSchema=true&useUnicode=true&characterEncoding=utf8`；剩余乱码根因在 MySQL 服务端字符集配置（双重 latin1↔utf8 编码错误，注释字节本身已是错的），需客户改 my.ini 加 `character-set-server=utf8mb4` 并重建表，**非代码层可完整修复** |
| OBS-2 el-radio v3 deprecation | ✅ 已修 | `Stats.vue:10-12` 三处 `<el-radio-button label=>` 改 `value=` |
| OBS-5 sys_config 历史分组 | ✅ 已修 | SQL UPDATE 把 `cache.*` 归位"缓存配置"，`audit.log.retention.days`/`sql.log.retention.days` 归位"日志配置" |
| OBS-6 SYS-5 9 vs 10 步 | ✅ 已修 | `开发计划.md` SYS-5 描述改为 10 步并枚举 |

- **新验证（之前只验证了 SELECT exec 和页面渲染）**：

| 单元 | 操作 | 结果 |
|---|---|---|
| M2-4 INSERT | 建接口 / 发布 / exec writing `sys_user` | 成功插入 id=4 + `sql_audit_log` 落 INSERT target_table=sys_user SUCCESS |
| M2-5 UPDATE | 建接口 / 发布 / exec 改 role=readonly | 成功 + `sql_audit_log` 落 UPDATE target_table=sys_user SUCCESS |
| M2-6 DELETE | 建接口 / 发布 / exec 按 id 删（含 configJson conditions 嵌入 table 内的格式约束）| 成功 + `sql_audit_log` 落 DELETE target_table=sys_user SUCCESS |
| M2-8 ShardRouter | 配 MODULO divisor=4，2 段，路由预览 userId=1/7/10 | orders_1/orders_3/orders_2 路由正确 |
| M2-10 双层缓存 | 启用缓存后 5 次 exec：第 1 次 691ms（冷启动+DB），第 2-5 次 10-12ms（命中）| hitCount=4 missCount=1，Redis 中 `cache_hit:1`/`cache_miss:1`/`query_cache:1:` 都有 |

- **影响文件**：
  - `backend/src/main/java/com/powergateway/config/CacheConfig.java`（去 `@ConditionalOnBean`）
  - `backend/src/main/java/com/powergateway/service/InterfaceConfigService.java`（INSERT 加 @AuditLog + setTargetTable）
  - `backend/src/main/java/com/powergateway/controller/ExecController.java`（INSERT 分支 set AuditContextHolder）
  - `backend/src/main/java/com/powergateway/service/TableMetaService.java`（OBS-1 normalizeUrlForMetaQuery 注入 utf-8 参数）
  - `frontend/src/views/system/Stats.vue`（OBS-2 el-radio label→value）
  - `docs/03-开发/开发计划.md`（OBS-6 SYS-5 10 步描述）
  - DB 改动：sys_config group_name UPDATE（OBS-5）

- **未完整修复（已记录）**：
  - OBS-1 表结构注释多数列仍乱码 — 根因在用户本地 MySQL 服务端字符集配置 + 注释字节双重编码，建议客户在 my.ini 加 `[mysqld] character-set-server=utf8mb4` 并重建表
  - 中间 trace 日志已清理

- **验收**：M2-4/5/6 真实写入数据库 + SQL 审计落 3 条；M2-10 缓存命中前后耗时 60-100× 差异；M2-8 路由计算正确。

- **报告**：`docs/04-测试/E2E验证报告-2026-06-29.md` § 8、§ 9

---

### CHG-013 2026-06-29 第三轮复杂场景深度验证（无新 BUG）

- **日期**：2026-06-29
- **影响单元**：M2-4 多表事务、M2-3 多表 JOIN、M1-6 映射加工、M1-7 端口分发、AUX-2 首页
- **变更类型**：测试验证（无代码改动）
- **背景**：第二轮 9 处 BUG 修完后，跑前两轮未覆盖的复杂用例。**5 个场景全部一次通过**，证明第二轮修复后系统进入稳定状态。

- **测试结果**：

| 场景 | 输入 | 结果 |
|---|---|---|
| 多表 INSERT + 事务回滚 | sys_user + channel_config 两表 INSERT，重复 channel_code 触发 UNIQUE 冲突 | data=2 正常；冲突报 "Duplicate entry...已回滚" 且 sys_user 无残留 ✅ |
| 多表 LEFT JOIN 查询 | sys_user u LEFT JOIN sys_log sl ON u.username = sl.operator | 2 行返回，无匹配行 lastModule=null（LEFT JOIN 语义正确）✅ |
| M1-6 含映射+加工 | mapping userId→USER_ID + process_rule {NAME:[{CASE/UPPER}]} | `<USER_ID>u001</USER_ID><NAME>ALICE</NAME>` 双重生效 ✅ |
| M1-7 ↔ pg-testkit mock | port_route → 9999/api/echo, mock 返回 body | dispatch 拿到 mock 响应，47ms ✅ |
| AUX-2 首页 维度切换 | 点击本周 → 触发 /api/home/overview?dimension=week | KPI 6/6/43/88.4% 与 DB 一致 ✅ |

- **新观察（OBS-8）**：测试中发现 INSERT/UPDATE/DELETE 三处 configJson 字段命名不统一：
  - INSERT: `{tableName, paramKey, constValue}`
  - DELETE: conditions **嵌入 table 内**，UPDATE 是同级
  - SELECT: `{name, alias}`（与 INSERT 的 tableName 不一致）
  - Template: 入参 `mappingRules` 复数，DB 列 `mapping_rule` 单数
  - Template: `TemplateSaveRequest` 不支持 `processRules` 字段，需直接 SQL UPDATE
  
  建议加文档明示三种入参契约，长线可考虑统一收敛但不破坏向后兼容。

- **报告**：`docs/04-测试/E2E验证报告-2026-06-29.md` § 12

---

### CHG-016 2026-07-20 UX-A 全站视觉重塑 + 双主题切换

- **日期**：2026-07-20
- **影响单元**：UX-A（阶段六第 1 波）+ CacheConfig 回归修复
- **变更类型**：范围新增（全站视觉重塑 + 主题偏好持久化）
- **变更内容**：
  - **后端**：`sys_user` 加 `theme_pref TEXT` 列 + 幂等迁移 `migration-sys-user-theme.sql`；`SysUserService.getThemePref/setThemePref` + `SysUserThemePrefController` 2 端点（`GET/PUT /api/user/theme-pref`）+ MockMvc/Service 5 测试全绿
  - **前端 CSS 底座**：新建 `frontend/src/styles/tokens.css`（双主题 Aurora/Nebula CSS 变量）+ `element-plus-override.css`（Element Plus `--el-*` 桥接）；`main.js` 引入；`App.vue` 加背景 blob + 星空
  - **前端主题机制**：新建 `frontend/src/composables/useTheme.js`（4 模式：manual/schedule/system/sun，内置北京 NOAA 日出日落算法，无需联网）；`ThemeToggle.vue` 圆形滑块；`ThemeDrawer.vue` 380px 抽屉（4 模式 radio + 定时时间 + 应用范围）；`TopBar.vue` 挂接（`<!-- UX-A THEME START/END -->` 注释锁供 UX-B 合并契约）
  - **前端页面美化**：`SideMenu.vue` `<style>` 末尾追加 active 光带 + 分组标签 CSS（UI-02）；`DashboardView.vue` KPI 5 卡改 `grid-template-columns: repeat(5, minmax(0,1fr))` 保证与下方模块严格右对齐（UI-03）；抽查 5 页替换 24 处硬编码色为 `var(--pg-*)`（UI-04/05）
  - **附带修复**：撤销 Sub-A-BE 为过测试引入的 `CacheConfig @ConditionalOnBean` 回归（该注解在启动顺序敏感时判定 false，撤销了 CHG-012 E2E-7 生产修复），改用 `@Profile("!test")` 达到"测试环境不注册 / 生产环境正常注册"效果
- **影响文件**：详见任务计划 `docs/03-开发/任务计划/2026-07-19-UX-A-visual-refresh.md`
- **需求文档更新**：`需求拆分与最小实现方案.md` 追加阶段六 UX-A 节；`开发计划.md` UX-A 行标 ✅
- **原因**：问题清单 UI-01 ~ UI-06（源自 111.txt 全站视觉反馈 + 用户增补的"双主题可切换 + 定时"需求）
- **设计文档**：`docs/02-设计/详细设计/2026-07-19-UX-A-visual-refresh-design.md`
- **视觉参考**：`mockups/v3-combined-switchable/index.html`（用户已确认）
- **验证**：UXAThemePref*Test 5/5 + M210CacheTest 11/11 + SYS4SysConfig*Test 10/10 全绿；mockup 与最终代码 CSS 变量逐条对齐

---

### CHG-019 2026-07-20 UX-D 接口转换配置流程重构 + 转换向导

- **日期**：2026-07-20
- **影响单元**：新增阶段六 UX-D；不改动 M1-1 ～ M1-7 对外行为；SYS-5 InterfaceWizard 由单文件重构为 WizardShell + SelectInterfaceSteps 组合，行为完全等价
- **变更类型**：功能新增（FN-04 流程重构 + FN-05 转换向导）+ 架构收敛（抽 WizardShell）
- **变更前**：接口转换配置六个页面平铺，无引导流程；`/convert/wizard` 不存在
- **变更后**：
  - 新增 `WizardShell.vue` + `useWizardShell` composable，两个向导共用骨架
  - 新增 `TransformInterfaceSteps.vue` 7 步向导：系统 → 功能号 → 端口 → 路由 → 模板 → 测试 → 发布
  - 原 `InterfaceWizard.vue` 迁到 `SelectInterfaceSteps.vue`（行为不变）
  - `port_route` 新增 `function_code`(64)/`function_name`(128) 可空字段
  - `convert_template` 新增 `function_code`(64) 可空字段
  - `PortRouteService.listRoutes` / `TemplateService.listTemplates` 支持 `functionCode` 过滤
  - `MenuPermission` ADMIN/USER 白名单加 `/convert/wizard`
- **原因**：用户反馈接口转换配置流程割裂（111.txt #12），需要仿 SYS-5 提供向导（111.txt #13）
- **验收**：
  - 原 `/interface/wizard` SELECT/INSERT/UPDATE/DELETE 全流程回归通过（Task 3，代码层验证）
  - 新 `/convert/wizard` E01-E07 + P01-P04 等效验证通过（Task 10）
  - `mvn test` 全绿（UXD 新增 8 用例）；`npm run test` 全绿（新增 12 用例）
- **保护点**：
  - `WizardShell.currentStepDef` 默认对象兜底（延续 CHG-011 E2E-6）
  - `SelectInterfaceSteps.vue` 保留 `wizard.tables[0]?.tableName` 的 `?.` + `v-if`
  - Task 5 提前处理 `MenuPermission` 白名单（防 CHG-011 E2E-5 重演）

---

### CHG-017 2026-07-20 UX-B · 信息架构重整

- **日期**：2026-07-20
- **影响单元**：SYS-3（菜单白名单顺序）、SideMenu.vue、TopBar.vue
- **变更类型**：UX 优化（无功能新增，无接口签名变化）
- **背景**：CHG-015 UX 批次 B 组 4 项（NAV-01 ~ NAV-04）
- **变更前 → 变更后**：
  - 「接口转换配置」6 条平铺 → 3 个小节（基础配置 / 转换规则 / 发布测试）
  - 「报文格式转换」分组首位 → 分组末位
  - 「转换模板管理」分组末位 → 分组第一位
  - TopBar「个人信息」空按钮 → 跳转 `/system/user`；readonly 角色下不渲染此项
  - `MenuPermission.java` 三个 List 顺序刷新对齐前端（集合成员零变）
- **影响文件**：
  - `frontend/src/components/layout/SideMenu.vue`
  - `frontend/src/components/layout/TopBar.vue`
  - `backend/src/main/java/com/powergateway/config/MenuPermission.java`
  - 新增 `frontend/src/components/layout/__tests__/UX-B_SideMenu.spec.js`（7 用例）
  - 新增 `frontend/src/components/layout/__tests__/UX-B_TopBar.spec.js`（4 用例）
  - 新增 `backend/src/test/java/com/powergateway/config/UxBMenuPermissionOrderTest.java`（5 用例）
  - 新增 `frontend/vitest.config.js` / `frontend/src/tests/setup.js` / `frontend/src/tests/smoke.spec.js`
  - 修改 `frontend/package.json`：新增 vitest / @vue/test-utils / happy-dom devDeps
- **需求文档更新**：
  - `docs/01-需求/需求拆分与最小实现方案.md` 追加阶段六 UX-B 单元段
  - `docs/03-开发/开发计划.md` 阶段六表格 UX-B 行标记完成
- **回归验证**：`SYS3UserTest` 集合断言通过，`GET /api/auth/menu` 返回集合成员完全一致
- **原因**：问题清单 2026-07-19 批次 B 组 NAV-01 ~ NAV-04

---

### CHG-018 UX-C 字段映射/加工回滚复发修复 + 字段公式管理落地

- **日期**：2026-07-19
- **影响单元**：UX-C（阶段六），涉及 M1-2、M1-3 前端 draggable 用法修复；P0-3 `field_formula` 表补齐并新建全套 Service/Controller/前端页面
- **变更类型**：Bug 修复（FN-01/02）+ 范围新增（FN-03 字段公式管理）
- **变更前**：
  - `FieldMapping.vue` 与 `FieldProcess.vue` 中 `<draggable>` 使用 `<template #item>`（vue-draggable-next v2.x 忽略此 slot），源字段区与规则行渲染为空 DOM（回滚复发自 CHG-003）
  - `/interface/formula` 挂载 PlaceholderView，无后端 Service/Controller，仅 P0-3 建了 `field_formula` 空表
- **变更后**：
  - `FieldMapping.vue` / `FieldProcess.vue` 两处 draggable 改为 default slot + `v-for`，文件顶部追加 vue-draggable-next 规约注释（防三次回滚）
  - 保留 CHG-005 F-4（fixedValue ?? null）、F-7（catch 弹 warning）、问题4（addRule/runPreview 空值保护）等既有修复
  - 新增 `FormulaOperand` / `FormulaJson` / `FieldFormulaDto` / `FormulaSaveRequest` / `FormulaValidateRequest` / `FormulaValidateResult` 六个 DTO
  - 新增 `FormulaValidator`：静态语义校验器，一次性收集所有错误（含 JSON path），复用 `TableMetaService.getTables` 校验 COLUMN 引用
  - 新增 `FieldFormulaService`：CRUD + duplicate + validate，配置库 master 数据源
  - 新增 `FieldFormulaController`：6 个端点（list/getById/save/duplicate/delete/validate），delete 强制 admin
  - 补 `FieldFormula.updateTime` 字段 + `@TableField(fill=INSERT_UPDATE)`
  - 幂等迁移脚本 `migration-field-formula.sql`：加 update_time 列 + 3 索引 + 表注释
  - 前端新增 `FieldFormula.vue`（列表 + 编辑对话框）、`FormulaBuilder.vue`（递归条件组编辑器）、`OperandInput.vue`（5 种 kind 操作数编辑器）、`FormulaPicker.vue`（预留复用）、`fieldFormula.js`（API 封装）
  - `router/index.js` 中 `/interface/formula` 的 component 由 PlaceholderView 改为 FieldFormula.vue
  - `MenuPermission.java` 无需改（`/interface/formula` 已在 ADMIN_MENUS / USER_MENUS）
- **影响文件**：见本单元任务计划 `docs/03-开发/任务计划/2026-07-19-UX-C-field-mapping-formula.md` 的文件地图
- **需求文档更新**：
  - `docs/01-需求/需求拆分与最小实现方案.md` 追加"字段公式管理"节（覆盖 §3.2.8 需求点，落地 field_formula 表与 6 端点契约）
  - `docs/03-开发/开发计划.md` 阶段六 UX-C 行状态改"已完成"
- **原因**：
  - 2026-07-19 用户巡检发现 FN-01/02（回滚复发自 CHG-003）和 FN-03（PlaceholderView 未落地）
  - CHG-003 修复被后续多次编辑同一文件时回滚，本次通过"default slot 唯一正确写法 + 文件顶部规约注释锁定"双保险
- **设计文档**：`docs/02-设计/详细设计/2026-07-19-UX-C-field-mapping-formula-design.md`
- **验收数据**：后端 `mvn test` 全绿（新增 29 条 + 存量 326 条，由 Sub-C1 负责）；前端 `npm run build` 通过（2487 模块零报错）；`FN-03-UI1~UI7` 手工路径就绪；`vue-draggable-next` 使用规约注释已入库（Task 1/2 由 Sub-C1 完成）

---

### CHG-027 去 CI 化 · 采用本地打包 + git tag 手动版本管理 · 定基 v0.1.0

- **日期**：2026-07-22
- **影响单元**：REL-1（打包发布形态，CHG-025 的范围回收）
- **变更类型**：范围删减 + 流程调整
- **变更前**：
  - `.github/workflows/release.yml` 承担 CI 发布：tag `v*.*.*` 触发 win/linux/standard 三 Job 并行 + 自动创建 GitHub Release + 上传 zip + SHA256
  - REL-1 需求文档 / 详细设计 / 05-制品发布/README / 根 CLAUDE.md 都以"GitHub Actions 自动发布"为口径
  - v1.1 规划增量含 SQLite CI job
- **变更后**：
  - 删除 `.github/workflows/release.yml`（连带清空 `.github/` 目录）
  - 发布流程改为**本地脚本手动执行**：`scripts/build/build-portable.sh` / `build-standard.sh` 打包 → 本地跑 `scripts/ci/verify-artifacts.sh` 冒烟 → SHA256 手动生成 → 本地分发
  - 版本管理改用 **`git tag v*.*.*` 手动打**，首版定基 **v0.1.0**（对应当前 master HEAD：CHG-016～CHG-026 全量交付）；后续修复/迭代由手动 tag 递进
  - v1.1 增量清单删去 "SQLite CI job"；Oracle 驱动预置 + Docker 保留
  - REL-1 详细设计文档头部加 CHG-027 说明块，§六 CI/CD + §三 CI 分支策略 + §十 "v0.1.1 CI 上线"路线整段作废（不删原文，仅标注）
- **影响文件**：
  - 删除：`.github/workflows/release.yml`（含 `.github/` 目录）
  - `CLAUDE.md`（根，阶段七 REL-1 行）
  - `docs/03-开发/开发计划.md`（阶段七 REL-1 单元表格行）
  - `docs/01-需求/需求拆分与最小实现方案.md`（REL-1 范围 · 分支策略 → 发布流程 + 版本管理）
  - `docs/05-制品发布/README.md`（去 CI 链接，加本地发布说明）
  - `docs/02-设计/详细设计/2026-07-22-REL-1-release-packaging-design.md`（顶部 CHG-027 说明块）
- **需求文档更新**：
  - `需求拆分与最小实现方案.md` → REL-1 "分支策略 / GitHub Actions" 两条替换为"发布流程 / 版本管理"两条
  - `开发计划.md` → 阶段七 REL-1 行末追加 CHG-027 说明
- **原因**：
  - 用户 2026-07-22 反馈："我们没有必要使用 github 的自动化流水线，我们更新代码就一个人，git 做好版本管理就行，发布流水线还是用本地命令的方式执行就行"
  - 附加动因：当前 GitHub PAT 缺 `workflow` scope，导致 `git push origin master` 被拒（含 CHG-025 引入的 release.yml），去掉 workflow 文件后 push 恢复正常
- **产物 tag**：本次 commit 打完立即打 **`v0.1.0`** annotated tag，作为项目首个正式版本基线
- **未触发的下游动作**：
  - `scripts/ci/verify-artifacts.sh` 保留原位，作为本地冒烟脚本继续使用（未改名 `scripts/release/`，避免连带的引用整理）
  - REL-1 详细设计文档 §六/§三/§十 相关章节文本未逐段删除，仅顶部作废声明，保留历史设计快照

---

### CHG-026 Swagger 菜单收归 admin 独有并改为外链跳转

- **日期**：2026-07-22
- **影响单元**：SYS-3（用户权限管理，角色菜单白名单）
- **变更类型**：功能修改（权限收敛 + 页面行为变更）
- **变更前**：
  - `MenuPermission.USER_MENUS` / `MenuPermission.READONLY_MENUS` 均含 `/tools/swagger`
  - 前端路由 `/tools/swagger` 挂 `PlaceholderView.vue`，点击后是"功能开发中"占位空页
  - 与后端 `http://localhost:8080/swagger-ui/index.html`（SpringDoc + `OpenApiDynamicCustomizer` 动态注册已发布接口）无跳转关系
- **变更后**：
  - `MenuPermission.USER_MENUS` / `MenuPermission.READONLY_MENUS` 移除 `/tools/swagger`，仅 `ADMIN_MENUS` 保留
  - 前端路由 `/tools/swagger` 组件改为 `views/tools/SwaggerRedirect.vue`：`onMounted` 时 `window.open('/swagger-ui/index.html', '_blank', 'noopener')`；页面兜底一个"再次打开"按钮，防浏览器拦截弹窗
  - `meta.title` 从 `接口文档` 修正为 `Swagger 文档`（原名与 FN-09 `/interface/doc` 冲突）
- **影响文件**：
  - `backend/src/main/java/com/powergateway/config/MenuPermission.java`
  - `backend/src/test/java/com/powergateway/config/UxBMenuPermissionOrderTest.java`（USER/READONLY 断言 - 1；READONLY 集合大小 5 → 4）
  - `backend/src/test/java/com/powergateway/SYS3UserTest.java`（readonly `getMenu` 期望列表 - 1）
  - `frontend/src/router/index.js`（`/tools/swagger` 挂新组件 + title 修正）
  - `frontend/src/views/tools/SwaggerRedirect.vue`（新增）
  - `frontend/src/components/layout/__tests__/UX-B_SideMenu.spec.js`（本地 USER/READONLY fixture 同步）
- **需求文档更新**：
  - `需求拆分与最小实现方案.md` → SYS-3 范围/实现方案补一段"Swagger 菜单仅 admin"
  - `开发计划.md` → 阶段四 SYS-3 行标注 CHG-026
- **原因**：
  - 用户 2026-07-22 反馈："Swagger UI 视觉与系统不统一，且我们自己用也不需要太好；这个权限仅限制在 admin 角色就好"
  - 原前端菜单点进去只是空占位页，体验差；与后端已有 SpringDoc UI 未打通
- **验收数据**：
  - 后端 `mvn test -Dtest=UxBMenuPermissionOrderTest,SYS3UserTest,UXDMenuPermissionTest`：19 tests，全绿
  - 前端 `npx vitest run UX-B_SideMenu.spec.js`：7/7 全绿
  - 前端 `npm run build`：2487 模块零报错，构建耗时 17.35s

---

### CHG-025 REL-1 打包发布形态（便携 + 标准包 + Maven profile + CI）

- **日期**：2026-07-22
- **影响单元**：REL-1（阶段七新增，落地 OPT-2 内嵌 DB + OPT-6 内嵌 JRE + OPT-10 首批国产库支撑）
- **变更类型**：范围新增（工程发布能力）
- **变更前**：无正式打包发布方案；开发环境依赖 MySQL + Redis 外置
- **变更后**：
  - Maven profile 三档新增：`db-h2`（compile scope 覆盖 test）/ `db-sqlite`（compile scope 覆盖 provided）/ `portable`（组合套件）
  - `CacheConfig.cacheRedisTemplate` 加 `@ConditionalOnProperty(pg.cache.redis.enabled, matchIfMissing=true)` —— 便携版设 false 自动降级 Caffeine-only
  - 新增 `application-standalone.yml`：H2 双库（配置 + 审计 MODE=MySQL）+ 排除 Redis auto config + h2-console 启用（仅本机）+ 幂等 CREATE IF NOT EXISTS
  - 打包脚本 `scripts/build/`：
    - `build-portable.sh --platform win-x64|linux-x64 --version [--with-testkit]` 便携版组装
    - `build-standard.sh --version` 标准版组装（前后端分离 zip）
    - `jlink-jre.sh --output` JRE 17 精简裁剪（约 40MB）
    - `package-templates/` 启动脚本模板（portable-start.bat/sh + standard-start.bat/sh + open-console.bat + stop 脚本 + README-portable.txt + README-standard.md + application-prod.yml.example + nginx.conf.example + install-schema-mysql.sh）
  - `.github/workflows/release.yml` CI：tag `v*.*.*` 触发 win/linux/standard 三 job 并行；`-testkit` 变体仅 artifact 不发布；冒烟测试通过后自动创建 Release
  - `scripts/ci/verify-artifacts.sh` 冒烟脚本：解压 → 启动 → curl /actuator/health（90s 内 200）→ 停止
- **影响文件**：
  - `backend/pom.xml`（3 profile：db-h2 / db-sqlite / portable）+ 新增 sqlite-jdbc provided 依赖
  - `backend/src/main/java/com/powergateway/config/CacheConfig.java`（@ConditionalOnProperty 加持）
  - `backend/src/main/resources/application-standalone.yml`（新增）
  - `scripts/build/`（新增全套 build 脚本 + package-templates 12 个文件）
  - `.github/workflows/release.yml` + `scripts/ci/verify-artifacts.sh`（新增）
- **验证**：
  - 后端 555 用例全绿（@ConditionalOnProperty 用 matchIfMissing=true 保持默认行为，无退化）
  - CacheConfig 加 profile-only 判断：测试环境走 @Profile("!test") 保持不装 Redis
- **未纳入（v2 或按需）**：
  - SQLite 分支 CI job（需 SQLite dialect 完整回归，独立 REL-1B 子任务）
  - Oracle / OceanBase 驱动预置（License 限制，README 引导客户下载）
  - Docker 部署（v2）
  - ARM 架构（v2）
  - Windows Authenticode 签名（涉及商业证书）
  - frontend-maven-plugin 自动化合体（当前用 build-portable.sh 手工 cp frontend/dist）
- **设计文档**：`docs/02-设计/详细设计/2026-07-22-REL-1-release-packaging-design.md`

---

### CHG-024 TEST-1 测试工具改造（pg-testkit 增强 + PG 前端嵌入 + TESTER 角色）

- **日期**：2026-07-22
- **影响单元**：TEST-1（阶段七新增）
- **变更类型**：范围新增（工程测试能力）
- **变更前**：pg-testkit 只暴露 `/test/*` API 供 AI 驱动，无 GUI；无 TESTER 角色
- **变更后**：
  - PG backend：`MenuPermission.TESTER_MENUS` 新增（5 项白名单），`AuthService` switch 加 `case "tester"` 分发
  - PG frontend：独立模块目录 `frontend/src/modules/testkit/`
    - `api/testkit.js` 直连 pg-testkit 8081（VITE_TESTKIT_BASE_URL 可覆盖）
    - `views/DemoDbManage.vue`（样例库管理：init/reset/drop/stats）
    - `views/MockServerRules.vue`（Mock 规则增删）
    - `views/MockServerHistory.vue`（请求历史，Path 过滤 + 详情弹窗）
    - `router.js` 导出 3 个路由
    - `router/index.js` import 挂载；`SideMenu.vue` 加 hasTestkit + 「测试环境」sub-menu
    - 下线只需删除 modules/testkit/ 目录 + 3 处代码
  - pg-testkit backend 增强：
    - `DemoDbController` 4 端点：/testkit/demo-db/{init,reset,drop,stats}
    - `DemoDbInitializer` 策略接口 + `MysqlDemoDbInitializer` 实现（@ConditionalOnProperty 未配 demo-url 则跳过装载）
    - init/reset/drop/stats 幂等实现；创建 3 张核心表 + 7 张占位表；灌 2 条 demo_user 样本
  - 现有 pg-testkit MockServerController 12 端点保持不变，PG 前端通过 api/testkit.js 复用
- **影响文件**：
  - `backend/src/main/java/com/powergateway/config/MenuPermission.java`
  - `backend/src/main/java/com/powergateway/service/AuthService.java`
  - `frontend/src/modules/testkit/`（新增 5 文件）
  - `frontend/src/router/index.js` + `frontend/src/components/layout/SideMenu.vue`
  - `pg-testkit/src/main/java/com/powergateway/testkit/demo/`（新增 3 类）
- **验证**：
  - backend 555 用例全绿（TESTER 只新增 case，未破坏 admin/user/readonly/default 行为）
  - 前端 `npm run build` 通过（testkit 模块进入 chunk）
  - pg-testkit `mvn compile` 通过
- **未纳入（v1.1 或按需）**：
  - 完整 Faker 生成 10 万条 demo_txn 数据（v1 只灌少量样本）
  - 10 张 demo 表的完整 DDL（v1 只建 3 核心 + 7 占位）
  - 分库分表 demo（demo_txn shard 库）
  - Mock 规则持久化到 mock-rules.json 文件（v1 内存态）
  - SQLite 请求历史存储（v1 内存态，重启丢失）
  - 10 个 DEMO_ 样例接口 seed JSON 灌注（v1 用户手工从 PG UI 建接口指向 demo_user 即可）
- **设计文档**：`docs/02-设计/详细设计/2026-07-22-TEST-1-testkit-enhance-design.md`

---

### CHG-023 REG-1 注册中心集成（Nacos + Eureka）

- **日期**：2026-07-22
- **影响单元**：REG-1（阶段七新增，落地 OPT-1 P0 出门试点必备）
- **变更类型**：范围新增（重大能力）
- **变更前**：
  - `port_route.port_address` 只支持完整 URL 直连
  - 无注册中心相关配置和运行时机制；OPT-1 P0 未落地
- **变更后**：
  - 数据表 `registry_config` 新增（支持同时启用多个注册中心，密码 AES 加密）；`sys_config` 追加 4 个 `registry.self.*` KV
  - 抽象层 `RegistryClient` + `NacosRegistryClient`（真 SDK 调用）+ `EurekaRegistryClient`（v1 discover-only，register 走占位）
  - `RegistryFacade` 门面聚合所有已启用 client，`hasAnyConfiguredClient` / `choose`（Round Robin）/ `discover` / `heartbeatAll`
  - `RegistryClientRegistrar` 启动时从 DB 装配 client；Controller CRUD 后 reload
  - `ServiceUrlResolver` 解析 `service://SVC/path?scheme=xx` → 具体 URL；30s Caffeine 兜底
  - `PortRouteService.dispatch` 转发前经 resolver；`saveRoute` 校验 `service://` 服务存在
  - `SelfRegistrationRunner` 启动自注册 + 上下文关闭注销；`reregisterSelf()` 供 UI 触发
  - `RegistryHeartbeatScheduler` `@Scheduled` 心跳 + 连续失败 threshold 次写 `perf_alert`
  - `RegistryConfigController` 6 端点：list/save/delete/test/discover-preview/reregister-self
  - Maven profile 三档：`registry-none`（默认，最小 fat-jar）/ `registry-nacos` / `registry-full`
    默认 `nacos-client 2.2.4` / `eureka-client 1.10.18` 均 provided scope，fat-jar 不带；profile 激活时覆盖为 compile 打包
  - 前端 `RegistryManagement.vue` + `ServicePickerDialog.vue`；`PortRoute.vue` 加「选择服务」按钮；`SystemConfig.vue` 加「重新注册本机」按钮
  - `MenuPermission.ADMIN_MENUS` 加 `/tools/registry`；SideMenu 加「注册中心管理」；`UxBMenuPermissionOrderTest` 更新 expected 集合（24 → 28）
- **影响文件**：
  - Backend：`registry/` 完整包 8 类 + `RegistryConfigService/Controller` + `RegistryConfigMapper` + `RegistryConfig`/`RegistryConfigSaveRequest`
  - `service/PortRouteService.java`（forwardWithRetryBytes / validateSaveRequest 集成 resolver）
  - DDL：`db/init.sql` / `db/init-h2.sql` / 新增 `db/migration-reg-1-registry.sql`
  - `pom.xml`（3 个 profile）
  - `config/MenuPermission.java`
  - Frontend：`api/registry.js` / `views/tools/RegistryManagement.vue` / `views/convert/ServicePickerDialog.vue` / `PortRoute.vue` / `system/SystemConfig.vue` / `router/index.js` / `layout/SideMenu.vue`
- **验证**：
  - 后端 555 用例全绿（新增 REG1SchemaTest 6 + REG1RegistryFacadeTest 14 + REG1NacosClientTest 13 + REG1EurekaClientTest 12 + REG1ServiceUrlResolverTest 15 + REG1HeartbeatAlertTest 9 + REG1RegistryControllerTest 10）
  - Maven profile 校验：`mvn dependency:list -DincludeScope=runtime` 默认无 nacos/eureka；`-Pregistry-nacos` 含 nacos-* 3 jar；`-Pregistry-full` 追加 eureka-client
  - 前端 `npm run build` 通过
- **未纳入（后续或永不做）**：
  - Eureka 自动装配（v1 只 discover，register 占位；EurekaClient 手工构造复杂）
  - 配置中心能力（用户明确不做）
  - 接口维度的独立注册（一实例一服务，接口列表放在 metadata）
- **设计文档**：`docs/02-设计/详细设计/REG-1-注册中心集成方案.md` + `docs/03-开发/任务计划/2026-07-22-REG-1-registry.md`

---

### CHG-022 FN-11 配置导入导出扩展（Excel/Markdown 双向 + 循环报文路径）

- **日期**：2026-07-22
- **影响单元**：FN-11 扩展（阶段七新增，扩展 UX-E CHG-020 的 zip 版本）
- **变更类型**：范围新增（多格式支持）
- **变更前**：
  - CHG-020 UX-E 只支持全量 zip 导入导出；无 Excel / Markdown 格式；无每接口独立文件；无循环报文路径表达
- **变更后**：
  - `PathExpression` 工具：`a.b[*].c` 路径读写（供 codec 与后续报文映射复用；与 CHG-004 点号路径习惯一致）
  - `ExcelConfigCodec` 接口配置 ↔ .xlsx 双向：SELECT / INSERT / UPDATE / DELETE 四种类型全覆盖
    - Sheet 布局按 type 分发：元数据 / 表配置 / 字段列表 / 数据来源 / 条件配置 / _meta（隐藏）
    - schemaVersion 强制校验（v1=1），不匹配抛 IncompatibleSchemaException
  - `ExcelTemplateCodec` 转换模板 ↔ .xlsx 双向：FieldMappingRule + ProcessRule 完整往返；支持 `items[*].amount` 循环报文路径
  - `MarkdownConfigCodec` 配置 → md（仅导出）：标题 + 元数据 + configJson pretty-print / mappingRule 表格
  - `ConfigExportService.exportInterfaceItems/exportTemplateItems(ids, format)`：单 ID 返回文件字节；多 ID 打 zip
  - `ConfigImportService.importExcel/previewExcel(files[], strategy)`：多文件同类型强制；按文件名前缀 TEMPLATE_ 分发；混合类型 400
  - `ConfigImportExportController` 追加 4 端点：`POST /api/config/{export/excel, export/markdown, import/excel, import/preview}`
  - 前端 `api/interfaceImportExport.js` 追加 4 方法；`InterfaceImportExport.vue` 底部追加"Excel 导入导出（新版）"卡片
- **影响文件**：
  - Backend：`service/codec/` 完整包 6 类（PathExpression + 两个异常 + 三个 codec）+ `ConfigExportService.java` + `ConfigImportService.java` + `ConfigImportExportController.java`
  - Frontend：`api/interfaceImportExport.js` + `views/interface/InterfaceImportExport.vue`
- **验证**：
  - 后端新增 61 用例全绿（FN11PathExpressionTest 27 + FN11ExcelConfigCodecTest 13 + FN11ExcelTemplateCodecTest 8 + FN11MarkdownCodecTest 5 + 全量 555 无退化）
  - 前端 `npm run build` 通过
- **未纳入（v2 再做，或永不做）**：
  - Markdown 反导入（决策点 1：只 excel）
  - Excel 部分字段更新（决策点 4：全量覆盖）
  - v1 阶段"节点类型列"标识（决策点 5：v2 再评估）
  - 「接口文档」+「配置导入/导出」合并为 `/tools/docs` 三 Tab 页面（v2 独立 UX 单元）
  - v1 TemplateSaveRequest DTO 不含 processRule 字段，excel 里的加工规则暂无法通过 saveTemplate 保存（与 zip 导入现状一致）
- **设计文档**：`docs/02-设计/详细设计/FN-11-导入导出扩展方案.md` + `docs/03-开发/任务计划/2026-07-22-FN-11-import-export.md`

---

### CHG-021 UX-F 中文注释乱码修复收尾

- **日期**：2026-07-20
- **影响单元**：UX-F（新增，阶段六第 1 波）
- **变更类型**：Bug 修复 + 用户指南补齐
- **变更前**：
  - CHG-011 OBS-1 / CHG-012 已给根因，`TableMetaService.normalizeUrlForMetaQuery` 部分缓解（约 10% 列注释恢复）
  - `sys_config.description` 显示 `????` / 锟斤拷 —— 用户无从修复
- **变更后**：
  - 新增前端 `frontend/src/utils/textSanitizer.js` 兜底工具（检测明显乱码 → 黄色斜体占位符）
  - `SystemConfig.vue`（及发现的其它 description/remark 展示列表）挂 `sanitize` 无视觉污染
  - 新增 `backend/src/main/resources/db/migration-encoding-fix.sql` 幂等反向解码脚本
  - 新增 `docs/03-开发/MySQL字符集配置指南.md` 引导客户改 `my.ini` + `Restart-Service MySQL80`
- **影响文件**：
  - `frontend/src/utils/textSanitizer.js`（新增）
  - `frontend/src/views/system/SystemConfig.vue`（挂 sanitize）
  - `backend/src/main/resources/db/migration-encoding-fix.sql`（新增）
  - `docs/03-开发/MySQL字符集配置指南.md`（新增）
- **原因**：问题清单 OPS-01（源自 111.txt #17）
- **设计文档**：`docs/02-设计/详细设计/2026-07-19-UX-F-encoding-fix-design.md`
- **验证**：
  - 前端 `textSanitizer` 单元测试 6 条 assertion 全通过（代码审阅确认）
  - SQL 迁移脚本幂等性验证：二次执行无异常无数据变化（REGEXP 条件保证）
  - SystemConfig 页面手工冒烟：description 列乱码→占位符显示正常
- **未纳入**：客户业务库表的中文注释乱码（本单元只修配置库，指南中已告知处理方法）

---

### CHG-020 2026-07-20 UX-E 可视化接口 6 项扩展全量落地

- **日期**：2026-07-20
- **影响单元**：新增阶段六 UX-E（FN-06 ~ FN-11 六项扩展功能）
- **变更类型**：范围新增（功能新增）
- **变更前**：
  - FN-06 响应格式协商、FN-07 字段 Schema 校验、FN-08 报文调试器（CodeMirror XML/JSON/CSV）、FN-09 接口文档生成、FN-10 报表导出、FN-11 配置导入/导出均为 PlaceholderView 或未实现
- **变更后**：
  - **FN-10（报表导出）**：8 个 Controller 增加 `GET /list/export` 端点（Excel），前端 8 个列表页增加"导出报表"按钮
  - **FN-08（CodeMirror）**：`MessageDebug.vue` 集成 CodeMirror 5.65.16，JSON/XML/CSV 三种模式语法高亮，含语言切换 Radio
  - **FN-09（接口文档）**：
    - 后端：`DocumentModel` DTO + `InterfaceDocumentService`（Markdown/HTML 渲染 + JDK ZipOutputStream 全量导出）+ `InterfaceDocumentController`（6 个端点）
    - 前端：`interfaceDoc.js` API + `InterfaceDocument.vue`（双 Tab：转换接口/可视化接口，单份下载 + 全量 zip）
    - 路由：`/interface/doc`；菜单：可视化接口开发→接口文档
  - **FN-11（配置导入/导出）**：
    - 后端：`ImportManifest` + `ImportResult` DTO + `ConfigExportService`（全量 zip 含 manifest.json）+ `ConfigImportService`（OVERWRITE/SKIP/ASK 三策略，`@Transactional`）+ `ConfigImportExportController`
    - 前端：`interfaceImportExport.js` API + `InterfaceImportExport.vue`（导出区 + 导入区 + 结果展示，含冲突列表）
    - 路由：`/interface/import-export`；菜单：可视化接口开发→配置导入/导出
  - `TemplateService` 新增 `listAllSummary()` / `findByPrimary()` 方法（FN-09/11 复用）
  - `InterfaceConfigService` 新增 `listAllSummary()` / `findByName()` 方法（FN-09/11 复用）
  - `MenuPermission` ADMIN/USER 白名单追加 `/interface/doc` 和 `/interface/import-export`
- **影响文件**：
  - `backend/src/main/java/com/powergateway/model/dto/DocumentModel.java`（新增）
  - `backend/src/main/java/com/powergateway/model/dto/ImportManifest.java`（新增）
  - `backend/src/main/java/com/powergateway/model/dto/ImportResult.java`（新增）
  - `backend/src/main/java/com/powergateway/service/InterfaceDocumentService.java`（新增）
  - `backend/src/main/java/com/powergateway/service/ConfigExportService.java`（新增）
  - `backend/src/main/java/com/powergateway/service/ConfigImportService.java`（新增）
  - `backend/src/main/java/com/powergateway/controller/InterfaceDocumentController.java`（新增）
  - `backend/src/main/java/com/powergateway/controller/ConfigImportExportController.java`（新增）
  - `backend/src/main/java/com/powergateway/service/TemplateService.java`（追加方法）
  - `backend/src/main/java/com/powergateway/service/InterfaceConfigService.java`（追加方法）
  - `backend/src/main/java/com/powergateway/config/MenuPermission.java`（追加两条路由）
  - `frontend/src/api/interfaceDoc.js`（新增）
  - `frontend/src/api/interfaceImportExport.js`（新增）
  - `frontend/src/views/interface/InterfaceDocument.vue`（新增）
  - `frontend/src/views/interface/InterfaceImportExport.vue`（新增）
  - `frontend/src/components/layout/SideMenu.vue`（追加菜单项）
  - `frontend/src/router/index.js`（追加两条路由）
  - 8 个 Controller 各追加 `GET /list/export`
  - 8 个前端 List 页各追加"导出报表"按钮
  - `frontend/src/components/tools/CodeEditor.vue`（新增，CodeMirror 5 封装）
  - `frontend/package.json`（添加 codemirror ^5.65.16）
  - `frontend/src/views/tools/MessageDebug.vue`（集成 CodeEditor + 语言切换）
- **需求文档更新**：
  - `docs/01-需求/需求拆分与最小实现方案.md` 追加 UX-E 段（FN-06 ~ FN-11 实现方案与验收标准）
  - `docs/03-开发/开发计划.md` 阶段六 UX-E 行标记 ✅
- **测试**：
  - 后端新增 `FN09DocumentServiceTest`（10 用例）、`FN11ConfigExportServiceTest`（3 用例）、`FN11ConfigImportServiceTest`（5 用例）共 18 用例，`mvn test` 全通（保持总失败数不变）
  - 全量回归：423 tests，pre-existing 17 failures（UxBMenuPermissionOrderTest 2、M25UpdateConfigTest 6、SYS2PerfStatAspectTest 2、M22TableMetaTest 1 error 等），0 新增失败
- **原因**：CHG-015 UX 批次 E 组（FN-06 ~ FN-11，问题清单 6 项功能扩展）

---

### CHG-015 2026-07-19 UX 批次 · 111.txt 归档与 6 个新单元规划

- **日期**：2026-07-19
- **影响单元**：新增阶段六 UX-A ～ UX-F 共 6 个开发单元（尚未实现，本条只记录规划）
- **变更类型**：范围新增（新增阶段六，共 20 项功能点）
- **背景**：用户 2026-07-19 全站菜单巡检提出 21 项问题（原始清单存 `111.txt`），其中 3 项"无疑问"跳过，18 项拆解重整为 20 个开发编号（UI-01 ～ OPS-01），归入 6 个可并行工作组：
  - UX-A · 全站毛玻璃视觉重塑 + 双主题（亮/暗 4 种切换模式）
  - UX-B · 信息架构重整（菜单排序 + 个人信息跳转）
  - UX-C · 字段映射/加工 bug + 字段公式管理新开发
  - UX-D · 接口转换配置流程重构 + 新增转换向导
  - UX-E · 可视化接口 6 项扩展（多报文格式 / 字段导出 / 接口文档 / 报表下载 / 模板导入导出 / 报文调试 XML）
  - UX-F · sys_config 中文注释乱码修复收尾（延续 CHG-011 OBS-1 / CHG-012 未完全修复项）
- **影响文件**（本条仅规划）：
  - `docs/03-开发/问题清单.md`（新增"2026-07-19 批次"章节，含 20 项分组编号）
  - `docs/03-开发/开发计划.md`（新增"阶段六：全站体验重塑与功能补齐"章节）
  - `docs/02-设计/详细设计/2026-07-19-visual-refresh-and-fixes-overview.md`（新增总览设计文档）
  - `mockups/v1-light-aurora/`、`v2-dark-nebula/`、`v3-combined-switchable/`（视觉样板 HTML mockup，用户已确认 v3 方案）
- **需求文档更新**：`需求拆分与最小实现方案.md` 将在各 UX 分组 spec 开写时追加对应节
- **原因**：全站巡检发现视觉体验、信息架构、字段公式功能、接口转换向导、可视化接口发布形态等一系列问题；单独修零散不如系统性重塑，采用"UX 批次"整体规划
- **待续**：本条只完成"归档 + 编号 + 规划"；每个 UX 单元开写时追加各自 CHG（CHG-016 ～ CHG-021）

---

### CHG-014 2026-06-29 第四轮：审计 / Swagger / 权限 / 并发 全活

- **日期**：2026-06-29
- **影响单元**：M2-9 SQL 审计、M2-7 接口发布 Swagger、SYS-3 用户权限、M2-10 双层缓存
- **变更类型**：测试验证（无代码改动）
- **背景**：第三轮 5 场景全过后，本轮专攻 4 个"过去仅 spec 不可见"的隐藏行为。**4 个场景全部一次通过，无新增 BUG**。
- **测试结果**：

| 场景 | 验证点 | 结果 |
|---|---|---|
| M2-9 before_snapshot | UPDATE 前 sys_user 行是否记录 | ✅ JSON 数组含改前 role=user 等完整行 |
| M2-7 Swagger 动态 | OpenAPI 是否含已发布 6 个接口 path | ✅ `/api/exec/1`～`/api/exec/7` 全在 |
| SYS-3 路由守卫越权 | readonly 访问 admin 菜单 | ✅ `/system/user`/`/system/config` 踢回 `/dashboard` |
| M2-10 分布式锁 | 5 线程并发 evict 后 exec | ✅ hit=4 miss=1，DB 只查 1 次 |

- **四轮累计**：9 个 BUG 全部修复 + 23 个深度场景验证通过 + 4 项 OBS 闭环（OBS-1 部分缓解，2/5/6 完成）。
- **报告**：`docs/04-测试/E2E验证报告-2026-06-29.md` § 13

---

### CHG-029 · 2026-07-27 · FN-12 × M1-3 集成 DictMappingProcessor（v0.2.0 ②）

- **日期**：2026-07-27
- **影响单元**：M1-3（扩展策略）· FN-12（复用 v0.2.0 ① DictMappingService.lookup）
- **变更类型**：扩展既有单元（M1-3 加新策略）
- **变更前**：ProcessRuleType 只有 TRIM/SUBSTRING/PAD/CASE/TYPE_CAST 五种策略；字段加工不支持字典转换
- **变更后**：新增 ProcessRuleType.DICT_MAP 枚举值 + DictMappingProcessor implements FieldProcessStrategy · 三层 Guard（空值透传 · 参数缺失 400 · direction 非整数 400 · miss 400）· FieldProcessController.ruleTypes() 追加 1 条 RuleTypeDesc 让前端能看到
- **影响文件**：
  - 新增 `backend/src/main/java/com/powergateway/utils/processor/DictMappingProcessor.java`
  - 修改 `backend/src/main/java/com/powergateway/utils/processor/ProcessRuleType.java`（+DICT_MAP）
  - 修改 `backend/src/main/java/com/powergateway/controller/FieldProcessController.java`（ruleTypes +1）
  - 新增 `backend/src/test/java/com/powergateway/M13DictMappingProcessorTest.java`（4 mock 用例）
  - 新增 `backend/src/test/java/com/powergateway/FN12ProcessorIntegrationTest.java`（2 集成用例）
- **验证**：`mvn test` exit 0 · 586/586 全绿（v0.2.0 ① 后基线 580 + 6 新）
- **依赖**：无新增（复用 v0.2.0 ① DictMappingService.lookup 契约 + Mockito 已在 spring-boot-starter-test 传递依赖）
- **关联**：[FB-039](../06-项目管理/反馈簿.md#fb-039) · [CR-001](../06-项目管理/待办与缺陷池.md#cr-001-字典转换配置字段字典映射)
- **原因**：跨系统集成普遍需要字典码值转换（性别 M/F ↔ 1/0 等），补齐后接口配置无需硬编码脚本
- **关联设计**：[docs/02-设计/详细设计/2026-07-27-FN-12-processor-design.md](../02-设计/详细设计/2026-07-27-FN-12-processor-design.md)
- **关联任务**：[docs/03-开发/任务计划/2026-07-27-FN-12-processor.md](./任务计划/2026-07-27-FN-12-processor.md)

---

### CHG-033 · 2026-07-28 · CR-006 · TEST-1 v0.1 补齐 · 7 张 demo_* 占位表补真 DDL + 种子（v0.2.5 minor）

- **日期**：2026-07-28
- **影响单元**：TEST-1（pg-testkit 增强 · 样例业务库 pg_demo）
- **变更类型**：功能补齐（v0.1.0 骨架 → v0.2.5 完整 v0.1）
- **变更前**：
  - `MysqlDemoDbInitializer` 仅 `demo_user` / `demo_account` / `demo_txn` 三张有真业务字段
  - 其余 7 张（`demo_product` / `demo_order` / `demo_order_item` / `demo_address` / `demo_dict` / `demo_config` / `demo_log`）仅 `id BIGINT + placeholder VARCHAR(255)` 占位，不可做业务语义练习
  - `pg-testkit/application.yml` 未配 `demo-url`，`MysqlDemoDbInitializer` 因 `@ConditionalOnProperty` 不装载
  - 手工测试用户"没数据关联不了、接口开发测不了"（触发反馈：会话内用户明确"敏捷开发就该先做能闭环的"）
- **变更后**：
  - `MysqlDemoDbInitializer.createTables()` 7 张占位表全部替换为真业务 DDL：
    - `demo_product`：product_code / name / category_code / price / stock / status
    - `demo_order`：order_no / user_id / total_amount / status(CREATED/PAID/SHIPPED/DONE/CANCELED)
    - `demo_order_item`：order_id / product_id / qty / unit_price / subtotal
    - `demo_address`：user_id / receiver / phone / province+city+district+detail / is_default
    - `demo_dict`：dict_type / dict_code / dict_name / sort_order + UNIQUE(dict_type, dict_code)
    - `demo_config`：config_key / config_value(TEXT) / updated_at
    - `demo_log`：user_id / action / target / detail / log_time
  - `seedMinimalData()` 灌完整种子：user 5 / account 6 / txn 8 / product 10 / order 7 / order_item 10 / address 4 / dict 8 / config 3 / log 15
  - `pg-testkit/src/main/resources/application.yml` 补 `pg-testkit.db.demo-url` / `demo-user` / `demo-password` → 指向 `jdbc:mysql://localhost:3306/pg_demo`
  - MySQL 新建 `pg_demo` 库（utf8mb4）
- **影响文件**：
  - 修改 `pg-testkit/src/main/java/com/powergateway/testkit/demo/MysqlDemoDbInitializer.java`（createTables + seedMinimalData 大改）
  - 修改 `pg-testkit/src/main/resources/application.yml`（+3 行 demo 数据源）
  - 修改 `docs/04-测试/连接配置速查.md`（§三·B 表清单从"3 真 7 占位"改"10 全真"）
  - 修改 `docs/04-测试/pg_demo接口开发练习.md`（§二结构 + §三场景全量重写，新增场景 4/5/6 三个 4 表 JOIN 案例）
  - 修改 `docs/04-测试/README.md`（+1 行索引）
- **验证**：
  - drop + init 重建 pg_demo：10 张表全部真结构 + 种子行数完全对
  - MySQL 三表 JOIN（订单+用户+明细）+ 四表 JOIN（订单+用户+字典+明细）跑通，返回数据与文档 §三期望值一致
  - 品类销量排行（4 表 JOIN + 过滤 + 聚合）：电子产品 15997 · 食品 392 · 图书 264
  - 用户+默认地址（LEFT JOIN 带 ON 条件）：3 有默认地址 / 2 NULL
- **依赖**：无新增
- **关联**：CR-006（本会话新增 · 用户 2026-07-28 拍板"归纳到最近的一个小版本 v0.2.5 minor · 实现管理闭环"）· [pg_demo接口开发练习.md](../04-测试/pg_demo接口开发练习.md)
- **归入版本**：v0.2.5 minor（与 CR-004 FN-12 v2 补完并入）
- **不含**：Faker 10 万条 · demo_txn 加 account_id/txn_type/peer_user_id/remark 字段 · 分片压测数据 —— 全部归 v1.1
- **规约同步**：本条 CHG · [`需求拆分与最小实现方案.md`](../01-需求/需求拆分与最小实现方案.md#阶段七--test-1--测试工具改造pg-testkit-增强) · [`开发计划.md`](./开发计划.md#阶段七) · [`待办与缺陷池.md § v0.2.5 minor`](../06-项目管理/待办与缺陷池.md#v025--minor--fn-12-v2-补完)

---

### CHG-032 · 2026-07-27 · FN-12 × FN-09 联动 · 接口文档 xlsx 格式 + 字典 key 列（v0.2.0 ④）

- **日期**：2026-07-27
- **影响单元**：FN-09（接口文档 · 字段行读真实 config_json/mappingRule/processRule + 新增 xlsx 格式）· FN-12 联动
- **变更类型**：扩展既有单元（FN-09 顺带修字段行硬编码 · 加 xlsx）
- **变更前**：md/html 字段行硬编码通用示例 · 无 xlsx 格式
- **变更后**：
  - md/html 字段行从 configJson/mappingRule/tpl.processRule 读真实字段 · 提取 processRule[DICT_MAP].dictKey 展示在"字典 key"列（说明列右侧）
  - 新增 xlsx 格式：可视化接口 4 sheet（基本信息/请求字段/响应字段/字典对照）· 转换模板 3 sheet（基本信息/字段映射/字典对照）
  - Controller `?format=xlsx` 分支 · 复用 ExcelExportUtil.exportMultiSheet + InterfaceConfigController.excelResponse helper
  - zip 升级：每接口 md+html+xlsx 3 份 · manifest.json.entries[] 追加 xlsx 键
  - 前端 InterfaceDocument.vue 行内加"Excel"独立按钮（Q3/A · 3 独立按钮）
- **影响文件**：
  - 修改 backend: `service/InterfaceDocumentService.java` · `controller/InterfaceDocumentController.java` · `service/TemplateService.java` (updateProcessRuleById helper)
  - 新增 backend: `FN12FN09LinkageTest.java`（11 用例）
  - 修改 frontend: `api/interfaceDoc.js` · `views/interface/InterfaceDocument.vue`
  - 新增 frontend: `tests/views/InterfaceDocument.spec.js`（2 用例）
- **验证**：mvn test 597+ 全绿 · vitest 54+ 全绿 · npm build exit 0
- **依赖**：无新增（复用 POI 5.2.3 + ExcelExportUtil）
- **关联**：[FB-040](../06-项目管理/反馈簿.md#fb-040) · [CR-001 § 扩展 A](../06-项目管理/待办与缺陷池.md#cr-001-字典转换配置字段字典映射)
- **Task commits**：Task 1 (64f8025) · Task 1 fix (0d67526) · Task 2 (a114c76) · Task 3 (dd27a08) · Task 4 (e9d985d)
- **关联设计**：[docs/02-设计/详细设计/2026-07-27-FN-12-x-FN-09-design.md](../02-设计/详细设计/2026-07-27-FN-12-x-FN-09-design.md)
- **关联任务**：[docs/03-开发/任务计划/2026-07-27-FN-12-x-FN-09.md](./任务计划/2026-07-27-FN-12-x-FN-09.md)

---

### CHG-031 · 2026-07-27 · FN-12 前端（v0.2.0 ③）

- **日期**：2026-07-27
- **影响单元**：FN-12（新增前端页 + 共享 dialog 组件）· 扩展 M1-3 FieldProcess.vue · 扩展 UX-D TransformInterfaceSteps · 扩展 SYS-5 SelectInterfaceSteps · 扩展 SYS-3 MenuPermission
- **变更类型**：新增前端 + 扩展既有向导
- **变更前**：字典功能无前端 UI，用户只能靠 REST API 或 Processor 集成
- **变更后**：
  - 新增 `/tools/dict` 字典管理页（三级筛选 + Excel 导入导出 + 三选一 direction radio 支持出/入/双向）
  - 新增 `DictMappingParamDialog.vue` 共享组件（3 处向导集成 · 二选一 direction · direction 存字符串对齐 ProcessRule.params 约束）
  - MenuPermission.java ADMIN + USER + READONLY 三角色都加 `/tools/dict`（readonly 只读体验）
- **影响文件**：
  - 新增 `frontend/src/api/dictMapping.js`
  - 新增 `frontend/src/stores/dictMapping.js`
  - 新增 `frontend/src/views/tools/DictMappingList.vue`
  - 新增 `frontend/src/components/dict/DictMappingParamDialog.vue`
  - 新增 `frontend/tests/api/dictMapping.spec.js` + `frontend/tests/store/dictMapping.spec.js` + `frontend/tests/views/DictMappingList.spec.js` + `frontend/tests/components/DictMappingParamDialog.spec.js`
  - 修改 `frontend/src/router/index.js` + `frontend/src/components/layout/SideMenu.vue` + `frontend/src/views/convert/TransformInterfaceSteps.vue` + `frontend/src/views/interface/SelectInterfaceSteps.vue` + `frontend/src/views/convert/FieldProcess.vue`
  - 修改 `backend/src/main/java/com/powergateway/config/MenuPermission.java` + 2 backend test fixtures（UxBMenuPermissionOrderTest / SYS3UserTest）
- **验证**：npx vitest run 52/52 全绿 · npm run build exit 0 · mvn test 586 全绿
- **依赖**：无新增
- **关联**：[FB-039](../06-项目管理/反馈簿.md#fb-039) · [CR-001](../06-项目管理/待办与缺陷池.md#cr-001-字典转换配置字段字典映射)
- **原因**：字典映射后端交付（CHG-029/CHG-030），需补齐前端 UI 和向导集成
- **关联设计**：[docs/02-设计/详细设计/2026-07-27-FN-12-frontend-design.md](../02-设计/详细设计/2026-07-27-FN-12-frontend-design.md)
- **关联任务**：[docs/03-开发/任务计划/2026-07-27-FN-12-frontend.md](./任务计划/2026-07-27-FN-12-frontend.md)
- **Task commits**：0af166b（Task 1）· 9c344f0（Task 2）· d5d05f0（Task 3）· 293c754（Task 4）· 2004602（Task 4 fix dictParams → params 对齐后端契约）

---

### CHG-030 · 2026-07-27 · 审计库合并到配置库（FB-043）

- **日期**：2026-07-27
- **影响单元**：M2-9（SQL 审计日志物理布局调整，行为语义零变化）
- **变更类型**：架构简化（部署形态调整）
- **变更前**：`sql_audit_log` 表建在独立 `powergateway_audit` 库；`application.yml` 里 audit datasource URL 指向 `powergateway_audit`
- **变更后**：`sql_audit_log` 表合并到 `powergateway_config` 库；audit datasource URL 改为指向 `powergateway_config`；`@DS("audit")` 注解在 `SqlAuditLogMapper` 保留（B 方案原则不动代码），datasource bean 保留但物理同库
- **影响文件**：
  - `backend/src/main/resources/application.yml`（audit URL）
  - `backend/src/main/resources/application-standalone.yml`（audit URL + 顶部注释）
  - `backend/src/main/resources/db/init.sql`（追加 sql_audit_log DDL + 注释更新）
  - `backend/src/main/resources/db/init-audit.sql`（归档 stub · 停用说明）
  - `backend/src/main/resources/db/migration-audit-to-config.sql`（新增 · 老库客户升级用）
  - `backend/src/test/resources/db/init-h2.sql`（sql_audit_log 头部注释更新）
  - `backend/src/main/java/com/powergateway/dao/SqlAuditLogMapper.java`（Javadoc 说明保留 @DS("audit") 的原因）
  - 文档：`CLAUDE.md` · `docs/02-设计/架构说明.md` · `docs/04-测试/连接配置速查.md` · `docs/04-测试/v0.1.0-手工测试指南.md`
- **验证**：
  - 本地 MySQL 迁移已执行：8 条历史 sql_audit_log 数据 config 库·audit 库 DROP·验证条数一致
  - `mvn test` exit 0（H2 测试环境本来就同库，无回归）
- **需求文档更新**：CLAUDE.md § 关键架构决策"三库分离"→"两库分离"
- **原因**：[FB-043](../06-项目管理/反馈簿.md#fb-043) · 客户环境 DBA 通常不给建多 schema，简化部署形态；老库客户走 `migration-audit-to-config.sql`

### CHG-028 · 2026-07-26 · 新增 FN-12 字典映射管理（后端 · v0.2.0 ①）

**类型**：新增单元
**影响单元**：FN-12（新增，非改造既有）
**关联反馈**：[FB-039](../06-项目管理/反馈簿.md)（2026-07-22 用户主动提）+ [FB-040 § 扩展 A](../06-项目管理/反馈簿.md)（联动 FN-09 归 v0.2.0 ④）
**CR**：[CR-001](../06-项目管理/待办与缺陷池.md#cr-001-字典转换配置字段字典映射)

**交付内容**：
- 表：`dict_mapping`（`init.sql` 追加 · 单向存储 direction/source/target · UNIQUE (system,key,direction,source)）
- 后端：`DictMappingService` CRUD + 双向拆条 + Redis 缓存 + POI Excel 导入导出
- 后端：`DictMappingController` 9 REST endpoint + `@SysLogRecord` 审计
- 测试：`FN12DictMappingTest` 新增 21 用例，全套后端 `mvn test` 从 555 → 576

**依赖**：无新增（复用 poi-ooxml:5.2.3）
**权限**：Sa-Token 认证 + 前端菜单白名单（v0.2.0 ③ 时加入 MenuPermission）
**Redis Key**：`dict:{system}:{dictKey}:{direction}` HashMap · TTL 3600s · 精准 DEL
**打 tag 门槛**：本单元完成后不单独打 tag，待 v0.2.0 ①/②/③/④ 全部交付后打 v0.2.0 MINOR tag

**交付 commits**（Task 1 → 10）：
- c6fc91e（Task 1 · 脚手架：DDL + Entity + Mapper + Service 骨架 + 单测骨架）
- ffb4288（Task 2 · save 单向存储 + 参数校验 + 重复检测）
- b470abd（Task 3 · save 双向拆条，bidirectional=true 自动生成 2 条）
- 75f21e5（Task 4 · list 分页筛选 + getById）
- 5e8c813（Task 5 · update 更新 + 缓存精准失效）
- 5fbd5ca（Task 6 · delete 逻辑删除 + getSystems 系统列表）
- 91d85e7（Task 7 · lookup Redis 缓存 + 未命中 404）
- c673687（Task 7 · C1/I1 fix：缓存构建 null 判断修正）
- 4b3d7f7（Task 8 · Excel 导入导出 POI）
- 2e4fa69（Task 8 · isBlankRow fix：空行判断修正）
- 58e6519（Task 9 · Controller 9 REST endpoint + @SysLogRecord + Swagger Tag）

**关联设计**：[docs/02-设计/详细设计/2026-07-26-FN-12-dict-mapping-backend-design.md](../02-设计/详细设计/2026-07-26-FN-12-dict-mapping-backend-design.md)
**关联任务计划**：[docs/03-开发/任务计划/2026-07-26-FN-12-backend.md](./任务计划/2026-07-26-FN-12-backend.md)
