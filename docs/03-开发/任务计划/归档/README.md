# 任务计划 · 归档区

已完成交付的单元任务计划归档区。**只读**。

## 归档触发条件

任务计划文件对应的单元交付完成（反映在 CHG-XXX + 版本 tag）后 → 从 `../` 移动到本目录对应版本子目录。**保留 CHG-XXX 引用即可追溯**,原任务计划正文不再修改。

## 归档目录索引

按交付版本归档：

- `v0.1.0/` · v0.1.0 首个基线覆盖（22 份计划 · M1/M2/SYS/AUX/UX/FN-11/REG-1/TEST-1/REL-1 系列）
- `v0.1.1/` · v0.1.1 patch 计划（FB-037 / FB-038 · 2 份）
- `v0.2.0/` · v0.2.0 FN-12 全链路（4 份计划）

## 未归档条件

若单元还处于以下状态,保留在 `../` 活跃区：

- 设计阶段（仅 `docs/02-设计/详细设计/YYYY-MM-DD-单元.md` 存在,任务计划未开工）
- 实施中（编码进行）
- 交付但 CHG 未归档

## 归档动作

由 Claude 在**交付发版时统一执行**:每次 `git tag -a vX.Y.Z` 之前 → 把该版本对应的任务计划移入 `vX.Y.Z/` 子目录 → 更新 `../README.md` 索引指向。

## 当前状态

- **目录已建**：2026-07-28（v0.2.x 复盘 + 归档整理）
- **首次归档**：规划在 v0.3.0 tag 前统一执行（把 v0.1.0/v0.1.1/v0.2.0 三个批次全部归档）
