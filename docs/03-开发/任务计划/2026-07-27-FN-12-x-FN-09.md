# FN-12 × FN-09 联动实施计划（v0.2.0 ④）

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development`

**Goal:** 修 FN-09 现有字段行硬编码（读真实 config_json / mappingRule）· 提取 DICT_MAP 参数 dictKey 展示 · md/html 加"字典 key"列 · 新增 xlsx 4-sheet 格式 · zip 每接口 3 份 · 前端加"下载 Excel"按钮。

**Architecture:** 后端复用既有 DocumentModel + StringBuilder 渲染骨架 · 追加 xlsx 4-sheet 生成用 `ExcelExportUtil.exportMultiSheet()`（已就绪 · 无新依赖）· Controller 现有 `?format=md|html` 扩展 xlsx · 前端行内加第 3 按钮。

**Tech Stack:** Spring Boot 2.7.18 · Apache POI 5.2.3（复用）· Jackson · Vue 3 · vitest

**Spec 源**：[`docs/02-设计/详细设计/2026-07-27-FN-12-x-FN-09-design.md`](../../02-设计/详细设计/2026-07-27-FN-12-x-FN-09-design.md)

## Global Constraints

- JDK 1.8+ · Spring Boot 2.7.18 · 中文注释 · 一 task 一 commit · 端口 8080/5173 不变 · 无新依赖
- 字段行加"字典 key"列**位置：说明列右侧**（Q2/A · 请求字段: 字段名/类型/必填/说明/字典key · 响应字段: 字段名/类型/说明/字典key · 字段映射: 序号/源字段/目标字段/转换方式/字典key）
- 4-sheet 命名严格按 spec 原话（Q4/A）：**基本信息 / 请求字段 / 响应字段 / 字典对照**（可视化接口）· **基本信息 / 字段映射 / 字典对照**（转换模板 · 3 sheet · 无响应）
- xlsx 格式集成到 Controller `?format=xlsx` · 复用 `InterfaceConfigController.excelResponse` helper
- zip 每接口 md + html + xlsx 3 份 · manifest.json `entries[]` 追加 `xlsx` 键
- 前端 3 独立按钮（Q3/A · md/html/Excel 并排 · 与既有风格一致）

## 文件结构

**Create:**
- `backend/src/test/java/com/powergateway/FN12FN09LinkageTest.java`（全套测试 · 10+ 用例）

**Modify:**
- `backend/src/main/java/com/powergateway/service/InterfaceDocumentService.java`（字段行改造 + xlsx 生成 + zip 升级）
- `backend/src/main/java/com/powergateway/controller/InterfaceDocumentController.java`（format=xlsx 分支）
- `frontend/src/api/interfaceDoc.js`（xlsx blob 支持）
- `frontend/src/views/interface/InterfaceDocument.vue`（第 3 按钮）
- `frontend/tests/views/InterfaceDocument.spec.js` OR 新建（若无既有）

---

## Task 1: 后端字段行改造 + extractDictKey + md/html 加"字典 key"列

**Files:**
- Modify: `backend/src/main/java/com/powergateway/service/InterfaceDocumentService.java`
- Create: `backend/src/test/java/com/powergateway/FN12FN09LinkageTest.java`

**Interfaces:**
- Consumes: `InterfaceConfig.configJson`（JSON String · 结构由 SubagentImpl 探索确认 · 常见 `{fields:[{name,type,required,desc,processRules:[{type,params}]}]}` 或类似）· `ConvertTemplate.mappingRule`（JSON · 常见 `[{src,target,process,params}]`）
- Produces:
  - 私有 `extractDictKey(List<Map<String,Object>> processRules)` 返 `String`（第一条 `type=='DICT_MAP'` 的 `params.dictKey`，无则空串 `""`）
  - `buildVisualModel` / `buildTransformModel` 字段表 header 追加"字典 key"列 · row 追加从字段的 processRules 提取的 dictKey · 无字段时保持"（无）"占位

- [ ] **Step 1.1: 写 4 个测试**（RED）

`backend/src/test/java/com/powergateway/FN12FN09LinkageTest.java`：

```java
package com.powergateway;

import com.fasterxml.jackson.databind.ObjectMapper;
import com.powergateway.dao.SysUserMapper;
import com.powergateway.model.ConvertTemplate;
import com.powergateway.model.InterfaceConfig;
import com.powergateway.model.dto.DictMappingSaveRequest;
import com.powergateway.service.*;
import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;
import org.springframework.test.context.ActiveProfiles;
import org.springframework.transaction.annotation.Transactional;

import java.util.*;

import static org.assertj.core.api.Assertions.assertThat;

/** FN-12 × FN-09 联动测试（v0.2.0 ④） */
@SpringBootTest
@ActiveProfiles("test")
@Transactional
class FN12FN09LinkageTest {

    @Autowired private InterfaceDocumentService docService;
    @Autowired private InterfaceConfigService interfaceService;
    @Autowired private TemplateService templateService;
    @Autowired private DictMappingService dictMappingService;

    // ---- Task 1: 字段行改造 + dictKey 提取 + md/html 加列 ----

    @Test
    void visualMd_字段表header含字典key列() {
        // 前置：创建含 configJson.fields 的 InterfaceConfig
        Long id = insertVisualWithFields();
        String md = docService.buildMarkdownForVisual(id);
        assertThat(md).contains("字典 key");
    }

    @Test
    void visualMd_字段行DICT_MAP的dictKey被展示() {
        Long id = insertVisualWithDictMapField("GENDER");
        String md = docService.buildMarkdownForVisual(id);
        assertThat(md).contains("GENDER");   // dictKey 在字段行出现
    }

    @Test
    void transformMd_字段映射表header含字典key列() {
        Long id = insertTransformWithMapping();
        String md = docService.buildMarkdownForTemplate(id);
        assertThat(md).contains("字典 key");
    }

    @Test
    void extractDictKey_无DICT_MAP规则返空串() {
        // 反射调私有方法 or 通过公开方法间接测试
        List<Map<String, Object>> rules = Arrays.asList(
            Map.of("type", "TRIM", "params", Map.of("mode", "BOTH"))
        );
        String result = com.powergateway.service.InterfaceDocumentServiceTestHelper
                        .extractDictKey(rules);   // 若无 helper 则暴露 package-private
        assertThat(result).isEmpty();
    }

    // ---- 测试辅助 ----

    private Long insertVisualWithFields() {
        InterfaceConfig cfg = new InterfaceConfig();
        cfg.setName("测试接口FN12FN09");
        cfg.setType("SELECT");
        cfg.setStatus("draft");
        cfg.setConfigJson("{\"fields\":[" +
                "{\"name\":\"userId\",\"type\":\"String\",\"required\":true,\"desc\":\"用户ID\"}," +
                "{\"name\":\"gender\",\"type\":\"String\",\"required\":false,\"desc\":\"性别\"}" +
            "]}");
        // 直接调用 mapper 插入（interfaceService.save 可能有 config 校验，用 mapper 更快）
        // 若 save 可接受 draft 状态就用 save
        return interfaceService.save(cfg);  // 假设签名接受 InterfaceConfig 返 Long
    }

    private Long insertVisualWithDictMapField(String dictKey) {
        InterfaceConfig cfg = new InterfaceConfig();
        cfg.setName("测试字典字段FN12FN09");
        cfg.setType("SELECT");
        cfg.setStatus("draft");
        cfg.setConfigJson("{\"fields\":[{\"name\":\"gender\",\"type\":\"String\",\"required\":false,\"desc\":\"性别\"," +
            "\"processRules\":[{\"type\":\"DICT_MAP\",\"params\":{\"system\":\"CIF\",\"dictKey\":\""+dictKey+"\",\"direction\":\"1\"}}]}]}");
        return interfaceService.save(cfg);
    }

    private Long insertTransformWithMapping() {
        ConvertTemplate tpl = new ConvertTemplate();
        tpl.setName("测试模板FN12FN09");
        tpl.setSrcFormat("JSON");
        tpl.setTargetFormat("XML");
        tpl.setMappingRule("[{\"src\":\"a\",\"target\":\"b\",\"process\":\"TRIM\"}]");
        return templateService.save(tpl);
    }
}
```

**注意实现者**：`InterfaceConfigService.save` / `TemplateService.save` 的实际签名可能不同（可能是接受 DTO · 返 Long id）· 请先 `grep -n "public Long save" service/InterfaceConfigService.java` 确认后适配 helper 方法。

- [ ] **Step 1.2: RED 验证**

```bash
cd backend && mvn test -Dtest=FN12FN09LinkageTest -q
```
Expected: FAIL（字段表还没有"字典 key"列 · dictKey 未提取）

- [ ] **Step 1.3: 实现字段行改造**

修改 `InterfaceDocumentService.java`：

**新增 imports**：
```java
import com.fasterxml.jackson.core.type.TypeReference;
import com.fasterxml.jackson.databind.ObjectMapper;
```

**新增字段**：
```java
private static final ObjectMapper OM = new ObjectMapper();
```

**新增私有方法 `extractDictKey`**（package-private for test）：
```java
/** 从字段级 processRules 提取第一条 DICT_MAP 的 dictKey · 无返 "" · package-private for test */
static String extractDictKey(List<Map<String, Object>> processRules) {
    if (processRules == null) return "";
    for (Map<String, Object> r : processRules) {
        if ("DICT_MAP".equals(r.get("type"))) {
            Object params = r.get("params");
            if (params instanceof Map) {
                Object dk = ((Map<?, ?>) params).get("dictKey");
                return dk == null ? "" : dk.toString();
            }
        }
    }
    return "";
}

/** 解析 configJson.fields 数组 · 失败返空 list */
@SuppressWarnings("unchecked")
private List<Map<String, Object>> parseFields(String configJson) {
    if (configJson == null || configJson.isEmpty()) return Collections.emptyList();
    try {
        Map<String, Object> root = OM.readValue(configJson, new TypeReference<Map<String, Object>>() {});
        Object fields = root.get("fields");
        if (fields instanceof List) return (List<Map<String, Object>>) fields;
    } catch (Exception e) { /* 静默降级 */ }
    return Collections.emptyList();
}

/** 解析 mappingRule 数组 · 失败返空 list */
@SuppressWarnings("unchecked")
private List<Map<String, Object>> parseMappingRules(String mappingRule) {
    if (mappingRule == null || mappingRule.isEmpty() || "[]".equals(mappingRule.trim())) return Collections.emptyList();
    try {
        return OM.readValue(mappingRule, new TypeReference<List<Map<String, Object>>>() {});
    } catch (Exception e) { /* 静默降级 */ }
    return Collections.emptyList();
}
```

**改造 `buildVisualModel`**：把 `reqTable` 硬编码换成从 `parseFields(cfg.getConfigJson())` 读真实字段 · header 加"字典 key" · row 加 `extractDictKey(...)`；`respTable` 同理（响应字段可能来自 configJson.responseFields 或复用 fields · **按 config_json 实际结构决定**，若无 responseFields 保留现有 SELECT/其他类型的硬编码但 header + row 都加"字典 key" 列· row 值填空串）。

**改造 `buildTransformModel`**：`table.add(Arrays.asList("序号", "源字段", "目标字段", "转换方式", "字典 key"))` · 从 `parseMappingRules(tpl.getMappingRule())` 遍历添加 row：`{seq, r.src, r.target, r.process, extractDictKey(fieldProcessRules)}`（注意 mappingRule 里 processRule 结构可能是 `params` 或 `dictParams`，v0.2.0 ③ Task 4 fix 已统一为 `params`）。

- [ ] **Step 1.4: GREEN 验证**

```bash
mvn test -Dtest=FN12FN09LinkageTest -q
```
Expected: 4/4 pass

- [ ] **Step 1.5: 全量 mvn test**

```bash
mvn test -q
```
Expected: 588+ 全绿（v0.2.0 ③ 后 586 + FN12FN09 4 新）

- [ ] **Step 1.6: Commit**

```bash
git add backend/src/main/java/com/powergateway/service/InterfaceDocumentService.java \
        backend/src/test/java/com/powergateway/FN12FN09LinkageTest.java
git commit -m "feat(FN-09): 字段行读真实 configJson/mappingRule + 提取 dictKey + md/html 加字典 key 列 (Task 1)"
```

---

## Task 2: xlsx 4-sheet 生成 + 字典对照数据组装

**Files:**
- Modify: `backend/src/main/java/com/powergateway/service/InterfaceDocumentService.java`
- Modify: `backend/src/test/java/com/powergateway/FN12FN09LinkageTest.java`（追加 3 用例）

**Interfaces:**
- Consumes: `ExcelExportUtil.exportMultiSheet(LinkedHashMap<String, SheetSpec<?>>)` （现有工具，返 `byte[]`）· `DictMappingMapper.selectByLookup(system, dictKey, direction)` 返 `List<DictMapping>`
- Produces:
  - `buildVisualXlsx(Long id) → byte[]` 4 sheet
  - `buildTransformXlsx(Long id) → byte[]` 3 sheet（转换模板无"响应字段"）
  - 私有 `collectDictReferences(fields)` 返 `List<Triple<system,dictKey,direction>>`（去重）

- [ ] **Step 2.1: 追加 3 测试**

```java
@Autowired private com.powergateway.dao.DictMappingMapper dictMappingMapper;

@Test
void visualXlsx_返4sheet() throws Exception {
    Long id = insertVisualWithDictMapField("GENDER");
    byte[] bytes = docService.buildVisualXlsx(id);
    assertThat(bytes).isNotNull();
    // 用 POI 读取验证 sheet 数量
    try (org.apache.poi.ss.usermodel.Workbook wb =
         new org.apache.poi.xssf.usermodel.XSSFWorkbook(new java.io.ByteArrayInputStream(bytes))) {
        assertThat(wb.getNumberOfSheets()).isEqualTo(4);
        assertThat(wb.getSheetAt(0).getSheetName()).isEqualTo("基本信息");
        assertThat(wb.getSheetAt(1).getSheetName()).isEqualTo("请求字段");
        assertThat(wb.getSheetAt(2).getSheetName()).isEqualTo("响应字段");
        assertThat(wb.getSheetAt(3).getSheetName()).isEqualTo("字典对照");
    }
}

@Test
void visualXlsx_字典对照sheet含预置字典条目() throws Exception {
    // 前置：先预置 dict_mapping 数据
    DictMappingSaveRequest req = new DictMappingSaveRequest();
    req.setSystemCode("CIF"); req.setDictKey("GENDER"); req.setDirection(1);
    req.setSourceValue("M"); req.setTargetValue("1"); req.setBidirectional(false);
    dictMappingService.save(req);

    Long id = insertVisualWithDictMapField("GENDER");
    byte[] bytes = docService.buildVisualXlsx(id);
    try (org.apache.poi.ss.usermodel.Workbook wb =
         new org.apache.poi.xssf.usermodel.XSSFWorkbook(new java.io.ByteArrayInputStream(bytes))) {
        org.apache.poi.ss.usermodel.Sheet dict = wb.getSheetAt(3);
        // 至少 header + 1 数据行
        assertThat(dict.getPhysicalNumberOfRows()).isGreaterThanOrEqualTo(2);
    }
}

@Test
void transformXlsx_返3sheet() throws Exception {
    Long id = insertTransformWithMapping();
    byte[] bytes = docService.buildTransformXlsx(id);
    try (org.apache.poi.ss.usermodel.Workbook wb =
         new org.apache.poi.xssf.usermodel.XSSFWorkbook(new java.io.ByteArrayInputStream(bytes))) {
        assertThat(wb.getNumberOfSheets()).isEqualTo(3);
        assertThat(wb.getSheetAt(0).getSheetName()).isEqualTo("基本信息");
        assertThat(wb.getSheetAt(1).getSheetName()).isEqualTo("字段映射");
        assertThat(wb.getSheetAt(2).getSheetName()).isEqualTo("字典对照");
    }
}
```

- [ ] **Step 2.2: RED**

```bash
mvn test -Dtest=FN12FN09LinkageTest -q
```
Expected: 3 新 FAIL（`buildVisualXlsx` / `buildTransformXlsx` 未定义）

- [ ] **Step 2.3: 实现**

在 `InterfaceDocumentService` 追加：

```java
@Autowired
private com.powergateway.dao.DictMappingMapper dictMappingMapper;

public byte[] buildVisualXlsx(Long id) {
    InterfaceConfig cfg = interfaceService.getById(id);
    List<Map<String, Object>> fields = parseFields(cfg.getConfigJson());
    java.util.LinkedHashMap<String, com.powergateway.utils.ExcelExportUtil.SheetSpec<?>> sheets = new java.util.LinkedHashMap<>();

    // Sheet 1 基本信息
    sheets.put("基本信息", buildBasicSheet(cfg));

    // Sheet 2 请求字段（含 字典 key）
    sheets.put("请求字段", buildFieldsSheet(fields, false));

    // Sheet 3 响应字段（含 字典 key · 若无 responseFields 则空表）
    sheets.put("响应字段", buildFieldsSheet(fields, true));  // 或从 configJson.responseFields 读

    // Sheet 4 字典对照
    sheets.put("字典对照", buildDictRefSheet(fields));

    try {
        return com.powergateway.utils.ExcelExportUtil.exportMultiSheet(sheets);
    } catch (Exception e) {
        throw new BusinessException(500, "接口文档 xlsx 生成失败：" + e.getMessage());
    }
}

public byte[] buildTransformXlsx(Long id) {
    ConvertTemplate tpl = templateService.getById(id);
    List<Map<String, Object>> mappings = parseMappingRules(tpl.getMappingRule());
    java.util.LinkedHashMap<String, com.powergateway.utils.ExcelExportUtil.SheetSpec<?>> sheets = new java.util.LinkedHashMap<>();
    sheets.put("基本信息", buildBasicSheetForTpl(tpl));
    sheets.put("字段映射", buildMappingSheet(mappings));
    sheets.put("字典对照", buildDictRefSheetFromMappings(mappings));
    try {
        return com.powergateway.utils.ExcelExportUtil.exportMultiSheet(sheets);
    } catch (Exception e) {
        throw new BusinessException(500, "转换模板文档 xlsx 生成失败：" + e.getMessage());
    }
}

// 私有 helper 若干（buildBasicSheet / buildFieldsSheet / buildDictRefSheet 等）
// 具体实现按 ExcelExportUtil.SheetSpec 结构调 · SubagentImpl 探索并适配
```

**关键实现要点**：
- `collectDictReferences(fields)`：遍历字段 processRules · 找 DICT_MAP · 收集 `{system, dictKey, direction}` 去重（用 `LinkedHashSet<List<String>>` 或类似）
- 对每个三元组调 `dictMappingMapper.selectByLookup(system, dictKey, Integer.parseInt(direction))` 拿 List<DictMapping> · 加到"字典对照" sheet 行
- 若字段无 DICT_MAP · 字典对照 sheet 只 header 无数据行

- [ ] **Step 2.4: GREEN**

```bash
mvn test -Dtest=FN12FN09LinkageTest -q
```
Expected: 7/7 pass

- [ ] **Step 2.5: Commit**

```bash
git add backend/src/main/java/com/powergateway/service/InterfaceDocumentService.java \
        backend/src/test/java/com/powergateway/FN12FN09LinkageTest.java
git commit -m "feat(FN-09): xlsx 4-sheet 生成 + 字典对照 sheet 数据组装 (Task 2)"
```

---

## Task 3: Controller endpoint 加 xlsx + zip 升级含 xlsx + manifest 加 xlsx 键

**Files:**
- Modify: `backend/src/main/java/com/powergateway/service/InterfaceDocumentService.java`（`exportAllTransformZip` / `exportAllVisualZip` 加 xlsx entry + manifest xlsx 键）
- Modify: `backend/src/main/java/com/powergateway/controller/InterfaceDocumentController.java`（format=xlsx 分支返 ResponseEntity byte[]）
- Modify: `backend/src/test/java/com/powergateway/FN12FN09LinkageTest.java`（3 测试）

- [ ] **Step 3.1: 追加 3 测试**

```java
@Test
void visualZip_manifest含xlsx键() throws Exception {
    insertVisualWithFields();
    byte[] zipBytes = docService.exportAllVisualZip();
    // 解压找 manifest.json
    try (java.util.zip.ZipInputStream zis = new java.util.zip.ZipInputStream(new java.io.ByteArrayInputStream(zipBytes))) {
        java.util.zip.ZipEntry e;
        String manifestJson = null;
        Set<String> names = new HashSet<>();
        while ((e = zis.getNextEntry()) != null) {
            names.add(e.getName());
            if ("manifest.json".equals(e.getName())) {
                java.io.ByteArrayOutputStream buf = new java.io.ByteArrayOutputStream();
                zis.transferTo(buf);
                manifestJson = buf.toString(java.nio.charset.StandardCharsets.UTF_8);
            }
        }
        assertThat(manifestJson).contains("\"xlsx\"");
        assertThat(names).anyMatch(n -> n.endsWith(".xlsx"));
    }
}

@Test
void transformZip_每接口含md_html_xlsx三份() throws Exception {
    insertTransformWithMapping();
    byte[] zipBytes = docService.exportAllTransformZip();
    try (java.util.zip.ZipInputStream zis = new java.util.zip.ZipInputStream(new java.io.ByteArrayInputStream(zipBytes))) {
        java.util.zip.ZipEntry e;
        int mdCount = 0, htmlCount = 0, xlsxCount = 0;
        while ((e = zis.getNextEntry()) != null) {
            if (e.getName().endsWith(".md")) mdCount++;
            if (e.getName().endsWith(".html")) htmlCount++;
            if (e.getName().endsWith(".xlsx")) xlsxCount++;
        }
        assertThat(mdCount).isEqualTo(htmlCount).isEqualTo(xlsxCount);
        assertThat(xlsxCount).isGreaterThan(0);
    }
}

@Test
void controllerFormatXlsx_returnsExcelResponse() throws Exception {
    // 通过 MockMvc 或直接调 controller 方法 · 验证 ResponseEntity 200 + content-type
    // 简化：直接调 docService.buildVisualXlsx(id) 并验证 bytes 非空
    Long id = insertVisualWithFields();
    byte[] bytes = docService.buildVisualXlsx(id);
    assertThat(bytes.length).isGreaterThan(100);
}
```

- [ ] **Step 3.2: RED**

```bash
mvn test -Dtest=FN12FN09LinkageTest -q
```
Expected: 3 新 FAIL（zip 无 xlsx · manifest 无 xlsx 键）

- [ ] **Step 3.3: 实现**

修改 `exportAllTransformZip` / `exportAllVisualZip` 循环内追加：

```java
byte[] xlsx = buildTransformXlsx(id);   // 或 buildVisualXlsx(id)
String xlsxKey = "transform/" + safe + ".xlsx";
entries.put(xlsxKey, xlsx);
item.put("xlsx", xlsxKey);   // manifest entry 加 xlsx 键
```

修改 `InterfaceDocumentController` 现有 GET endpoint · 追加 `xlsx` format 分支：

```java
if ("xlsx".equalsIgnoreCase(format)) {
    byte[] bytes = docService.buildVisualXlsx(id);   // 或 buildTransformXlsx
    return InterfaceConfigController.excelResponse(bytes,
        "接口文档_" + id + "_" + InterfaceConfigController.tsSuffix() + ".xlsx");
}
```

- [ ] **Step 3.4: GREEN + 全量**

```bash
mvn test -Dtest=FN12FN09LinkageTest -q   # 10/10 pass
mvn test -q                              # 全量 596+ 全绿
```

- [ ] **Step 3.5: Commit**

```bash
git add backend/src/main/java/com/powergateway/service/InterfaceDocumentService.java \
        backend/src/main/java/com/powergateway/controller/InterfaceDocumentController.java \
        backend/src/test/java/com/powergateway/FN12FN09LinkageTest.java
git commit -m "feat(FN-09): Controller format=xlsx + zip 升级每接口 md/html/xlsx 3 份 + manifest 加 xlsx 键 (Task 3)"
```

---

## Task 4: 前端 xlsx 下载按钮 + API 支持

**Files:**
- Modify: `frontend/src/api/interfaceDoc.js`（现有 downloadTransformDoc/downloadVisualDoc 若无 blob 响应支持则加）
- Modify: `frontend/src/views/interface/InterfaceDocument.vue`（行内加"Excel"独立按钮）
- Modify: `frontend/tests/views/InterfaceDocument.spec.js`（若无则新建 · 2 用例）

**Interfaces:**
- 前端调 `GET /api/doc/visual/{id}?format=xlsx` responseType blob · 触发下载

- [ ] **Step 4.1: 检查现状 · 决定 API 层是否已支持 blob**

```bash
grep -n "responseType\|blob" frontend/src/api/interfaceDoc.js
```
若已有：直接用 `downloadVisualDoc(id, 'xlsx')` · 若无：追加 xlsx 分支。

- [ ] **Step 4.2: 写 2 测试**

```js
// frontend/tests/views/InterfaceDocument.spec.js（新建 or append）
import { mount, flushPromises } from '@vue/test-utils'
import { setActivePinia, createPinia } from 'pinia'
import { beforeEach, describe, test, expect, vi } from 'vitest'
import ElementPlus from 'element-plus'

vi.mock('@/api/interfaceDoc', () => ({
  listTransformDocs: vi.fn().mockResolvedValue({ data: [] }),
  listVisualDocs: vi.fn().mockResolvedValue({ data: [
    { id: 1, name: '测试接口', type: 'SELECT', status: 'draft' }
  ]}),
  downloadVisualDoc: vi.fn(),
  downloadTransformDoc: vi.fn(),
  exportVisualZip: vi.fn(),
  exportTransformZip: vi.fn()
}))
import * as api from '@/api/interfaceDoc'
import InterfaceDocument from '@/views/interface/InterfaceDocument.vue'

beforeEach(() => {
  setActivePinia(createPinia())
  Object.values(api).forEach(fn => fn.mockReset && fn.mockReset())
})

describe('InterfaceDocument.vue', () => {
  test('行内含 3 按钮 MD/HTML/Excel', async () => {
    api.listVisualDocs.mockResolvedValue({ data: [
      { id: 1, name: '测试接口', type: 'SELECT', status: 'draft' }
    ]})
    const w = mount(InterfaceDocument, { global: { plugins: [ElementPlus] } })
    await flushPromises()
    expect(w.text()).toMatch(/MD|Markdown/)
    expect(w.text()).toContain('HTML')
    expect(w.text()).toContain('Excel')
  })

  test('点 Excel 按钮调 downloadVisualDoc(id, "xlsx")', async () => {
    api.listVisualDocs.mockResolvedValue({ data: [{ id: 42, name: 'x', type: 'SELECT', status: 'draft' }]})
    api.downloadVisualDoc.mockResolvedValue(new Blob(['x']))
    const w = mount(InterfaceDocument, { global: { plugins: [ElementPlus] } })
    await flushPromises()
    // 找 Excel button 并点击（可能需要根据实际 DOM 结构调整选择器）
    const btn = w.findAll('button').find(b => b.text().includes('Excel'))
    if (btn) {
      await btn.trigger('click')
      expect(api.downloadVisualDoc).toHaveBeenCalledWith(42, 'xlsx')
    }
  })
})
```

- [ ] **Step 4.3: 实现**

在 `InterfaceDocument.vue` 每个 tab 的表格行操作列 · 现有 md/html 按钮后追加：

```vue
<el-button size="small" @click="download(row.id, 'xlsx')">Excel</el-button>
```

`download` 函数复用现有的（若 md/html 已能触发 blob 下载 · 传 xlsx 即可）。

在 `api/interfaceDoc.js` 若现有实现不支持 blob · 确保 `responseType: 'blob'` 已加。

- [ ] **Step 4.4: 全量 vitest + build**

```bash
cd frontend && npx vitest run    # 全绿
cd frontend && npm run build     # exit 0
```

- [ ] **Step 4.5: Commit**

```bash
git add frontend/src/api/interfaceDoc.js \
        frontend/src/views/interface/InterfaceDocument.vue \
        frontend/tests/views/InterfaceDocument.spec.js
git commit -m "feat(FN-09): 前端 InterfaceDocument 加 Excel 独立下载按钮 + API blob 支持 (Task 4)"
```

---

## Task 5: 归档 CHG-032 + 待办勾掉 + push

**Files:**
- Modify: `docs/03-开发/变更记录.md`（追加 CHG-032 到 CHG-031 前）
- Modify: `docs/06-项目管理/待办与缺陷池.md`（v0.2.0 ④ 勾掉）

- [ ] **Step 5.1: 全量回归**

```bash
cd backend && mvn test -q       # 596+ 全绿
cd frontend && npx vitest run   # 全绿
cd frontend && npm run build    # exit 0
```

- [ ] **Step 5.2: 追加 CHG-032**

在 `docs/03-开发/变更记录.md` 找 `### CHG-031` 那一行 · 前面追加：

```markdown
### CHG-032 · 2026-07-27 · FN-12 × FN-09 联动 · 接口文档 xlsx 格式 + 字典 key 列（v0.2.0 ④）

- **日期**：2026-07-27
- **影响单元**：FN-09（接口文档 · 字段行读真实 config_json + 新增 xlsx 格式）· FN-12 联动
- **变更类型**：扩展既有单元（FN-09 顺带修字段行硬编码 · 加 xlsx）
- **变更前**：md/html 字段行硬编码通用示例 · 无 xlsx 格式
- **变更后**：
  - md/html 字段行从 configJson/mappingRule 读真实字段 · 提取字段级 processRule[DICT_MAP].dictKey 展示在"字典 key"列
  - 新增 xlsx 格式：可视化接口 4 sheet（基本信息/请求字段/响应字段/字典对照）· 转换模板 3 sheet（基本信息/字段映射/字典对照）
  - Controller `?format=xlsx` 分支 · 复用 ExcelExportUtil.exportMultiSheet
  - zip 升级：每接口 md+html+xlsx 3 份 · manifest.json.entries[] 追加 xlsx 键
  - 前端 InterfaceDocument.vue 行内加"Excel" 独立按钮
- **影响文件**：
  - 修改 backend: InterfaceDocumentService.java · InterfaceDocumentController.java
  - 新增 backend: FN12FN09LinkageTest.java（10 用例）
  - 修改 frontend: api/interfaceDoc.js · views/interface/InterfaceDocument.vue
  - 新增 frontend: tests/views/InterfaceDocument.spec.js（2 用例）
- **验证**：mvn test 596+ 全绿 · vitest 全绿 · npm build exit 0
- **依赖**：无新增（复用 POI 5.2.3 + ExcelExportUtil）
- **关联**：[FB-040](../06-项目管理/反馈簿.md#fb-040) · [CR-001 § 扩展 A](../06-项目管理/待办与缺陷池.md#cr-001-字典转换配置字段字典映射)
- **Task commits**：Task 1-5 SHA 见 git log
- **关联设计**：[docs/02-设计/详细设计/2026-07-27-FN-12-x-FN-09-design.md](../02-设计/详细设计/2026-07-27-FN-12-x-FN-09-design.md)

```

- [ ] **Step 5.3: 勾掉待办 v0.2.0 ④**

```markdown
- [x] **④ FN-12 × FN-09 联动** ✅ 2026-07-27 → CHG-032 · commits <Task 1~5 SHA>
```

- [ ] **Step 5.4: Commit + Push**

```bash
git add docs/03-开发/变更记录.md docs/06-项目管理/待办与缺陷池.md
git commit -m "docs(项目管理): v0.2.0 ④ FN-12 × FN-09 联动交付归档 · CHG-032 · 待办勾掉"
git push origin master
```

---

## 相关文档

- [spec](../../02-设计/详细设计/2026-07-27-FN-12-x-FN-09-design.md)
- [v0.2.0 ① 后端 spec](../../02-设计/详细设计/2026-07-26-FN-12-dict-mapping-backend-design.md)
- [v0.2.0 ③ 前端 spec](../../02-设计/详细设计/2026-07-27-FN-12-frontend-design.md)
- 执行：`superpowers:subagent-driven-development` · 5 Task · 2 人日
