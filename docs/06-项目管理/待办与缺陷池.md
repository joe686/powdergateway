# 待办与缺陷池

> Backlog + 缺陷池 + 变更申请模板。**持续维护**，条目落地后勾掉并链接到 CHG。

---

## 一、Backlog（未做的功能）

按目标版本分组。每条格式：`- [ ] 条目描述 → 影响单元 · 出处`。

### v0.2.0

> **执行顺序（用户 2026-07-26 拍板 · A 方案）**：先跑 FN-12 四步（业务价值最高，CR-001 用户主动提），再补 pg-testkit 三项 + SQLite 两项。详见 [路线图 § v0.2.0](./路线图.md#v020--fn-12-字典映射--test-1-完善--sqlite-分支预估-2--3-周)。

- [ ] **① FN-12 字典映射管理 · 后端**：`dict_mapping` 表 + Service + Controller + Excel 导入导出 + Redis 缓存 → CR-001
- [ ] **② FN-12 × M1-3 集成**：新增 `DictMappingProcessor` 策略 + `ProcessRuleType.DICT_MAP` → CR-001 · 复用 M1-3
- [ ] **③ FN-12 前端**：`DictMappingList.vue`（`/tools/dict`）+ 三级联动下拉嵌入字段加工规则选择器 + M1-6 / M2-3/4/5/6 五处向导嵌入 → CR-001
- [ ] **④ FN-12 × FN-09 联动**：接口文档新增 Excel(.xlsx) 多 sheet 下载（基本信息 / 请求字段+字典key / 响应字段+字典key / 字典对照），md/html 同步加字典 key 列 → CR-001 § 扩展 A · [FB-040](./反馈簿.md#fb-040)
- [ ] **⑤ pg-testkit Faker 数据**：`DemoDbInitializer` 加 Faker 依赖，为 10 表各生成合理量级测试数据 → TEST-1 v1.1
- [ ] **⑥ pg-testkit 完整 10 表 DDL**：`demo-db-init.sql` 覆盖典型业务场景（客户 / 订单 / 账户 / 交易 / 卡片…） → TEST-1 v1.1
- [ ] **⑦ pg-testkit Mock 规则持久化**：Mock 配置存 `mock_rule` 表，重启不丢；管理页由内存 → DB 驱动 → TEST-1 v1.1
- [ ] **⑧ SQLite Maven profile**：`db-sqlite` 打通端到端；便携版 zip 用 SQLite 替代 H2 → REL-1 v1.1
- [ ] **⑨ SQLite 便携版完整回归**：后端全套 CRUD + 首页 + 系统概览端到端 → REL-1 v1.1

### v0.3.0

- [ ] 标准包默认预置 ojdbc11.jar（先确认 License 二次分发合规性） → REL-1 v1.1
- [ ] 标准包默认预置 OceanBase 驱动 → REL-1 v1.1
- [ ] `Dockerfile`（后端）+ `docker-compose.yml`（后端 + MySQL + Redis + Nginx） → REL-1 v1.1
- [ ] 更新产品说明书 SKU 矩阵，加"容器化"一栏 → 05-制品发布/产品说明书.md

### v0.4.0（REL-1B 子任务）

- [ ] SQL 语法差异排查清单（LIMIT ↔ ROWNUM / DUAL / NVL vs IFNULL / 日期函数 / 字符串拼接 …）→ REL-1B
- [ ] MyBatis Dialect Bean 三份（MySQL/Oracle/OceanBase），按 `db_connection.db_type` 自动装载 → REL-1B
- [ ] Oracle 容器化回归环境搭建 → REL-1B
- [ ] OceanBase 容器化回归环境搭建 → REL-1B
- [ ] 独立测试套件跑全 CRUD 用例（含 M2-3/4/5/6 + M2-10 缓存 + M2-8 分片） → REL-1B

### v1.0

- [ ] 7 个 SKU 全套端到端联调（便携 Win/Linux H2 + 便携 Win/Linux SQLite + 标准 MySQL/Oracle/OceanBase） → REL-1
- [ ] 首页 + 系统监控端到端压测（查询 ≤ 300ms / 写入 ≤ 500ms） → SYS-2 / M2-10 关联
- [ ] 便携版 5 分钟快速上手手册 → 05-制品发布/
- [ ] 标准版生产部署手册（拆出独立文档）→ 05-制品发布/
- [ ] 升级迁移指南（拆出独立文档） → 05-制品发布/
- [ ] License / 版权页 / 免责声明 → 前端 About 页 · 05-制品发布/

### v2+（长期，未立项）

- [ ] Windows exe 安装包（NSIS / Inno Setup）
- [ ] ARM 架构支持（jlink ARM JRE）
- [ ] Windows Authenticode 数字签名（需商业证书）
- [ ] GitHub Actions CI 回归（若团队扩员）
- [ ] SaaS 化 / 多租户
- [ ] 移动端管理页

---

## 二、缺陷池

按严重度分档。每条格式：`- [ ] [P0/P1/P2] 一句话现象 · 复现步骤 / 触发条件 · 影响范围`。

### P0 · 阻塞发布

（当前空）

### P1 · 影响主流程但有 workaround

（当前空）

### P2 · 视觉 / 交互 / 文档

（当前空）

---

## 三、变更申请模板

用户提出的新需求 / 范围变更，先落到此处，评估后决定进入哪个版本。

```markdown
### CR-XXX <标题>

- **提出日期**：YYYY-MM-DD
- **提出人**：用户 / 内部
- **需求描述**：<用户原话或复述>
- **动机**：<为什么要做>
- **影响单元**：<可能触及的已交付单元>
- **预估工时**：<S / M / L>
- **建议归入版本**：<v0.1.x / v0.2.0 / …>
- **状态**：待评审 / 已接受 → 待办 / 已拒绝（附原因）
- **落地 CHG**：<CHG-XXX，接受后填>
```

### 待评审 CR

（当前空）

### 已接受待落地 CR（前置版本占位）

#### CR-001 字典转换配置（字段字典映射）

- **提出日期**：2026-07-22
- **提出人**：用户
- **反馈来源**：[FB-039](./反馈簿.md#fb-039)
- **需求描述**：
  转换接口（模块一：接口转换 M1-x）与可视化接口（模块二：数据库 CRUD M2-x）都需要**跨系统字典值映射**能力。表设计要素：
  - 字典对接的系统号（对端系统标识）
  - 字典标识（如 `GENDER` / `ACCT_STATUS`）
  - A 系统字典值 / B 系统字典值（多对一：多个源值可映射到同一目标值）
  - 中文含义
  - **转换方向**：出向（PG → 对端）/ 入向（对端 → PG）/ 双向（"配置一次生成两条"）
- **动机**：现有 `FieldProcessor`（M1-3）只有 Trim/Pad/Substring/Case/TypeCast 5 种通用加工策略，跨系统集成时**普遍**需要字典码值转换（性别 M/F ↔ 1/0，账户状态 A/F ↔ ACTIVE/FROZEN 等）。当前用户只能通过硬编码脚本或多条字段加工规则拼凑，成本高、易错。补齐后可显著减少集成配置工时（对齐产品"减少接口开发工时 50%"目标）。
- **影响单元**：
  - **新增** FN-12 字典映射管理（表 + Controller + Service + 前端管理页）
  - **扩展** M1-3 `FieldProcessor` 新增 `DictMappingProcessor` 策略
  - **集成点** M1-6 报文转换（字段映射时可选加工规则 = 字典转换）、M2-3/4/5/6 可视化接口 4 步向导的字段加工步骤
  - **前端** 字段加工规则选择器新增"字典转换"类型 + 系统/字典/方向下拉

- **设计方案（推荐 B，供评审）**：

  **表结构 `dict_mapping`**（B 方案 · 规范化单向存储 + 双向由前端拆条）：
  ```sql
  CREATE TABLE dict_mapping (
    id            BIGINT       PRIMARY KEY AUTO_INCREMENT,
    system_code   VARCHAR(64)  NOT NULL COMMENT '对端系统标识（关联 registry_config.service_name 或 db_connection.name）',
    dict_key      VARCHAR(128) NOT NULL COMMENT '字典标识，如 GENDER',
    direction     TINYINT      NOT NULL COMMENT '1=出向(PG→对端) 2=入向(对端→PG)',
    source_value  VARCHAR(255) NOT NULL COMMENT '源值',
    target_value  VARCHAR(255) NOT NULL COMMENT '目标值（多对一允许重复）',
    cn_label      VARCHAR(255)          COMMENT '中文含义',
    status        TINYINT      DEFAULT 1,
    deleted       TINYINT      DEFAULT 0,
    create_time   DATETIME     DEFAULT CURRENT_TIMESTAMP,
    update_time   DATETIME     DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    UNIQUE KEY uk_src (system_code, dict_key, direction, source_value),
    KEY idx_lookup (system_code, dict_key, direction, source_value)
  );
  ```
  - **前端"双向"复选框**：勾选后一次保存生成 2 条（direction=1 A→B, direction=2 B→A），后端 Service 处理
  - **多对一**：`uk_src` 只对 `source_value` 唯一，允许多个源指向同一 `target_value`
  - **反查缓存**：Redis KEY `dict:{system}:{dict_key}:{direction}` → HashMap<source,target>，TTL 1h（对齐 M2-2 元数据缓存策略）

  **对比 A 方案**（用户原提议：一条记录带 a_value/b_value + direction 字段）：
  - 优点：直观，一条即"一对映射"
  - 缺点：多对一时需存冗余（同一 target 有 N 行 a_value/b_value 组合），方向字段与列名耦合（direction=2 时读列的意图翻转，易错）

  **B 方案**规范化更彻底、查询更直接（select target_value where direction=? and source_value=?），推荐用 B。

  **FieldProcessor 集成**：
  ```java
  new ProcessRule(ProcessRuleType.DICT_MAP, Map.of(
      "system", "CIF",
      "dictKey", "GENDER",
      "direction", "1"   // 1=出向 2=入向
  ))
  ```
  策略实现读 `Redis dict:CIF:GENDER:1` 查 target；未命中报 `BusinessException(400, "字典 GENDER 值 M 在系统 CIF 未定义映射")`。

- **前端 UI**：
  - 新增菜单 `辅助工具 → 字典管理`（`/tools/dict`，`DictMappingList.vue`）
    - 顶部筛选：系统 + 字典标识 + 方向
    - 表格：source_value / target_value / cn_label / 方向 / 状态 / 操作
    - 新增/编辑对话框：系统 + 字典标识 + source_value + target_value + cn_label + 方向单选（出向 / 入向 / **双向 → 后端拆两条**）
    - 支持 Excel 导入导出（对齐 FN-11）
  - 字段加工规则选择器（`FieldProcess.vue` / `InterfaceWizard.vue` 步骤 6）追加"字典转换"选项，选中后展开三级联动下拉（系统 → 字典标识 → 方向）

- **预估工时**：**M**（3~5 人日）
  - 后端：新增表 + Service + Controller + Excel 导入 + Processor 集成 + 单测（≈1.5 天）
  - 前端：管理页 + 字段加工选择器集成 + 3 处向导嵌入（≈2 天）
  - 联调 + 文档 + CHG（≈0.5 天）

- **归入版本**：**v0.2.0**（用户 2026-07-22 确认）
- **确认方案**：**B 方案**（单向存储 `direction/source_value/target_value`，前端"双向"复选框由后端拆两条）
- **状态**：✅ 已接受 · 待 v0.2.0 排期编码
- **落地 CHG**：（实施后填）

##### 扩展 A · 字典 × 接口文档联动（来自 [FB-040](./反馈簿.md#fb-040)，2026-07-26 追加）

字典功能落地后，`FN-09 接口文档` 输出需同步升级：

1. **字段规格表新增"字典 key"列**
   - 影响：`InterfaceDocumentService.buildVisualModel()` 请求/响应字段表、`buildTransformModel()` 字段映射表
   - 列名建议：`字典标识`（对应 `dict_mapping.dict_key`），空则渲染 `—`
   - 数据来源：接口配置中字段加工规则若为 `DICT_MAP` 类型，从 `params.dictKey` 提取；否则视为无字典
2. **新增 Excel(.xlsx) 多 sheet 下载**（用户 2026-07-26 确认方案）
   - 新增接口 `GET /api/interface-doc/{id}/xlsx`，FN-09 从 md/html 双格式扩为**三格式**（md + html + xlsx）
   - Excel 结构：
     - `Sheet 1 · 基本信息`：接口名称 / 类型 / 状态 / 访问路径 / 默认响应格式
     - `Sheet 2 · 请求字段`：字段名 / 类型 / 必填 / 说明 / **字典 key**
     - `Sheet 3 · 响应字段`：字段名 / 类型 / 说明 / **字典 key**
     - `Sheet 4 · 字典对照`：按接口涉及的字典去重汇总（system_code + dict_key + direction 分组），列出全部 source_value / target_value / cn_label 对照
   - 转换模板文档（`buildTransformModel`）同结构：Sheet 2 改"字段映射"（源字段 / 目标字段 / 转换方式 / **字典 key**），Sheet 4 保持字典对照
   - `exportAllVisualZip / exportAllTransformZip` 打包结构升级：每个接口出 md + html + xlsx 三份文件，manifest.json 新增 `xlsx` 键
   - 前端：接口文档预览页 / 批量导出按钮新增"Excel"下载选项
   - 依赖：引入 EasyExcel（阿里，MyBatis-Plus 生态友好）或 Apache POI；用户偏好未指定，实施时选轻量的 EasyExcel
3. **影响单元与工时增量**（在 CR-001 原 M 工时基础上）
   - **+2 人日**：后端 EasyExcel 集成 + `InterfaceDocumentService` Excel 渲染器 + Controller + zip 结构升级 + 前端下载按钮 + 8~12 个新增用例（含字典关联提取、多 sheet 生成、空字典兜底、批量导出 manifest 兼容）
4. **落地时机**：与 CR-001 主体同批交付（v0.2.0），避免字典功能上线后文档信息滞后


### 已接受待落地 CR

#### CR-002 FN-BIZ 业务菜单生成能力 + SYS-3 升级

- **提出日期**：2026-07-26
- **提出人**：用户
- **反馈来源**：[FB-041](./反馈簿.md#fb-041)
- **需求描述**：**远期战略功能**。将已发布 CRUD 接口一键生成为可运行业务菜单，PowerGateway 从"接口开发平台"跃升为"业务系统生成平台"。伴生 SYS-3 权限体系大改造（三固定角色 → 自定义角色 + 菜单分级 + 授权继承 + 分级授权）。详见 6 轮渐进披露原文与设计文档。
- **动机**：
  - 已发布接口价值最大化：不仅是外部 API，同时是业务用户可直接操作的功能
  - 零编码交付 MIS 系统，进一步兑现产品"减少接口开发工时 50%"承诺
  - 现有 SYS-3 三固定角色白名单不支持业务应用自建角色 / 菜单分级 / 分级授权，倒逼升级
- **影响单元**：
  - **新增** FN-BIZ 独立子模块 `pg-biz-menu`（Maven aggregator）
  - **改造** SYS-3（伴生大改造，见 [SYS-3 升级设计](../02-设计/详细设计/2026-07-26-SYS-3-upgrade-design.md)）
  - **改造** AUX-2 首页 DashboardView（新增 4 卡片）
  - **改造** M2-7 接口发布（新增 `default_app_menu_id` 字段）
  - **扩展** SYS-1 / M2-9 / SYS-2 三张日志表加 `trace_id` 列
  - **复用** FN-12 字典功能（v0.2.0 前置）、`POST /api/exec/{id}`（M2-7）、`QueryCacheManager`（M2-10）等
- **设计文档**（已交付）：
  - [FN-BIZ 主设计文档](../02-设计/详细设计/2026-07-26-FN-BIZ-business-menu-generation-design.md)
  - [SYS-3 升级设计文档](../02-设计/详细设计/2026-07-26-SYS-3-upgrade-design.md)
- **关键决策摘要**（brainstorming 沉淀）：
  1. 一接口一菜单，仅覆盖 4 类 CRUD（M2-3/4/5/6），不含 M1-x 转换接口
  2. 独立"业务菜单生成中心"作为唯一入口，接口配置页不加零散按钮
  3. 归属机制混合：接口发布时可选默认应用 + 生成中心可覆盖，支持一接口挂多应用
  4. 通用固定页面模板（信息+动态表单+响应），非拖拽编辑器
  5. 部署形态：**PG 后台内嵌**（不搞独立门户/域名/用户体系）
  6. 菜单四态：正常 / 已过期 / 已停用 / 已孤立（事件驱动同步）
  7. 字典 FN-12 自动联动（表单下拉 + 表格反向映射）
  8. 权限：**全系统 SYS-3 大升级**（自定义角色 + 菜单分级 + 授权继承 + 分级授权）
  9. 业务监控：新建 business_op_log 表（放**配置库**）+ 全局 trace_id 串四表
  10. 可裁剪：Maven profile 编译期 + `sys_config.biz.menu.enabled` 运行时开关双控（含前端所有报表/按钮/菜单/饼图完整关闭面）
- **预估工时**：**XL**（保守估计 4~6 周全职）
  - SYS-3 升级：~1.5 周（含迁移对账）
  - FN-BIZ 后端：~2 周（生成 + 通用执行 + trace_id 全链路 + business_op_log）
  - FN-BIZ 前端：~1.5 周（6 页面 + DynamicFormRenderer + 首页卡片）
  - 联调 + 手工验证 + 文档 + CHG：~1 周
- **归入版本**：**v0.5.0**（用户 2026-07-26 明确拍板：**单一大版本，只做本功能**，与 v0.3/v0.4 常规演进错开）
- **状态**：✅ 已接受 · 待 v0.5.0 排期编码（待用户复审两份设计后进入 writing-plans）
- **落地 CHG**：（实施后填）

### 已拒绝 CR（备查）

（当前空）

---

## 四、维护约定

- **常态巡检**：每次发新版本前扫一遍本文件，勾掉已落地条目、把新识别的缺陷/待办追加进对应池
- **不重复登记**：若一条待办已同步到 `03-开发/任务计划/` 下的详细任务分解，正文里加 `→ 见任务计划/YYYY-MM-DD-XXX.md`，避免两处内容漂移
- **及时删陈旧**：明确决定不做的条目，从"待评审"直接进"已拒绝"并写清原因，不长期挂着占位

---

## 五、快速命令速查

```bash
# 打 patch tag
git tag -a v0.1.1 -m "PowerGateway v0.1.1 · <一句话总结>"
git push origin master v0.1.1

# 本地打便携版（Win）
bash scripts/build/build-portable.sh --platform win-x64 --version 0.1.1

# 本地打标准包
bash scripts/build/build-standard.sh --version 0.1.1

# 本地冒烟
bash scripts/ci/verify-artifacts.sh dist/powergateway-standard-0.1.1.zip

# 后端全量测试
cd backend && mvn test

# 前端 build + 单测
cd frontend && npm run build && npx vitest run
```
