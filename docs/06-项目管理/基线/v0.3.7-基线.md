# v0.3.7 基线(2026-08-03 编码完成 · 待手工回归 + tag)

**版本编号**:v0.3.7 · **类型**:minor · **主线**:承接 v0.3.0/v0.3.2/v0.3.5 顺延项集合收尾
**发布日期**:2026-08-03(编码完成 · v0.3.5 tag 已 push · v0.3.7 待手工回归 + push)
**归档 CHG**:CHG-045 · CHG-046 · CHG-047 · CHG-048(4 条)

## 主要交付(5 Task)

### Task 1 · CR-007 InterfaceWizard PG 功能号输入(CHG-045)

v0.3.1 Task 1.5 顺延项 · InterfaceWizard step:db 加 functionId 输入 · store + composable + buildPayload 三处对齐。

### Task 2 · SOCKET 出站独立配置页(CHG-046)

`/interface/socket-outbound` · 新建 `SocketOutboundConfig.vue` · 基本 + 目标连接(ip/port/framing/charset/超时) + 请求 XML 模板 + 测试连接按钮。

### Task 3 · INBOUND_SOCKET 入站独立配置页(CHG-046)

`/interface/socket-inbound` · 新建 `SocketInboundConfig.vue` · 基本 + 监听(port/framing/charset/连接数) + XPath 功能号提取 + info alert 解释链路。

### Task 4 · JSON 骨架折叠树预览组件(CHG-048)

零依赖 100 行手写 `JsonTree.vue` + `JsonNode.vue` 递归组件 · Query/Insert/UpdateConfig 3 页字段加工 step 加 el-tabs "编辑 / 树预览" 双 tab。

### Task 5 · Docker 部署形态补齐(CHG-047)

`backend/Dockerfile` + `docker-compose.yml`(mysql/redis/backend 三服务) + `docs/03-开发/Docker部署方案.md`。REL-1 第三形态就位 · Oracle 已内置(pom scope=runtime · 无需 profile)。

## 变更文件清单

**前端**(9 处):
- `store/wizard.js`(+1 字段 functionId)
- `composables/useInterfaceEdit.js`(+1 行回填)
- `views/interface/SelectInterfaceSteps.vue`(step:db 加输入 + buildPayload)
- `views/interface/SocketOutboundConfig.vue`(新建 218 行)
- `views/interface/SocketInboundConfig.vue`(新建 168 行)
- `views/interface/QueryConfig.vue`(+ JsonTree tab)
- `views/interface/InsertConfig.vue`(+ JsonTree tab)
- `views/interface/UpdateConfig.vue`(+ JsonTree tab)
- `components/JsonTree.vue` + `components/JsonNode.vue`(新建)
- `router/index.js`(+2 路由)
- `components/layout/SideMenu.vue`(+2 menu-item + 常量)

**后端**(2 处):
- `config/MenuPermission.java`(ADMIN/USER 各 +2 项)
- `test/config/UxBMenuPermissionOrderTest.java`(期望值 30 → 32)

**部署**(3 处):
- `backend/Dockerfile`(新建)
- `docker-compose.yml`(项目根新建)
- `docs/03-开发/Docker部署方案.md`(新建)

## 自动化验证

- 后端 `mvn test -Dtest=UxBMenuPermissionOrderTest` 通过(菜单权限期望值 32 校准)
- 前端 `npm run build` 通过

## 手工验收 checklist

### CR-007 wizard functionId
- [ ] 新建 SELECT 接口 · 填 PG 功能号 PG-TEST-001 · 保存后 InterfaceList 显示
- [ ] 编辑同一接口 · 回填 functionId 应显示
- [ ] 重复 PG 功能号保存应报唯一约束错

### SOCKET 出站
- [ ] 侧栏"接口定义 · SOCKET 出站配置"进入
- [ ] 测试连接按钮:填 127.0.0.1:9999(pg-testkit) · 应返回 reachable
- [ ] 保存后 InterfaceList 出现该接口 · type=SOCKET
- [ ] 编辑模式回填 socket 段字段

### INBOUND_SOCKET 入站
- [ ] 侧栏"接口定义 · SOCKET 入站配置"进入
- [ ] 填端口 9099 + 分帧 XML_BOUNDARY + 编码 UTF-8 · 保存
- [ ] 发布该接口 · Manager 应拉起 Netty ServerSocket 监听 9099
- [ ] `docker compose exec backend netstat -lntp | grep 9099` 应有监听
- [ ] 编辑模式回填 inbound 段字段

### JSON 骨架折叠区
- [ ] QueryConfig Step 3 · processRules 输入嵌套 JSON · 切"树预览" tab 显示折叠树
- [ ] 输入无效 JSON · 应显示"JSON 解析失败"红字
- [ ] object/array 头部 ▶ 点击折叠 · ▼ 点击展开
- [ ] 折叠时显示"N 项/N 键"预览

### Docker
- [ ] `mvn clean package -DskipTests`
- [ ] `docker compose up -d` · 三容器 healthy
- [ ] `curl http://localhost:8080/actuator/health` 或访问 swagger-ui.html
- [ ] `admin` / `Admin@123` 登录

## 顺延后续版本

- **v0.3.8+**:frontend nginx 镜像 · Eureka selfRegister 实装(独立 minor)· SocketInboundServerManager 自动 lifecycle hook
- **v0.4.0+**:k8s helm chart(需求确定后)· business_op_log 第四表 → v0.5.0 CR-002 FN-BIZ
- **可选优化**:FieldProcess 编辑器嵌入组件化(替代 JSON textarea + JsonTree)· JSON 树 diff / edit 能力

## Tag checklist

- [ ] `mvn clean package -DskipTests`(可选 · 验证 Docker 场景 · 本次跳过)
- [ ] 4 页 + wizard + SOCKET 双页 + JsonTree 手工验收 pass
- [x] backend `mvn test -Dtest=UxBMenuPermissionOrderTest` 通过
- [x] frontend `npm run build` 通过
- [ ] frontend dist commit
- [ ] `git tag -a v0.3.7`(本地 · push 待用户确认)
