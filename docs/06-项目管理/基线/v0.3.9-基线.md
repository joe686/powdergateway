# v0.3.9 基线(2026-08-03 编码完成 · 待手工回归 + tag)

**版本编号**:v0.3.9 · **类型**:minor · **主线**:v0.3.7 3 项顺延收尾
**发布日期**:2026-08-03(编码完成 · v0.3.7 tag 已 push · v0.3.9 待手工回归 + push)
**归档 CHG**:CHG-049 · CHG-050 · CHG-051(3 条)

## 主要交付(3 Task)

### Task 1 · SocketInboundServerManager 自动 lifecycle hook(CHG-049)

InterfaceConfigService.publish/disable/delete 三处 hook · INBOUND_SOCKET 接口自动拉起/停止 Netty ServerSocket · 无需重启后端。

### Task 2 · frontend nginx 镜像 + docker-compose 集成(CHG-050)

- 新建 `frontend/Dockerfile`(多阶段 node build → nginx runtime)
- 新建 `frontend/nginx.conf`(SPA history + /api + /swagger-ui 代理)
- `docker-compose.yml` 加 frontend service · 4 服务一键起(mysql/redis/backend/frontend)

REL-1 三形态(便携 + 标准 + Docker)完备。

### Task 3 · Eureka selfRegister REST API 实装(CHG-051)

`EurekaRegistryClient.register/deregister` 走 Eureka REST API(POST/DELETE /eureka/apps/{APP}) · 零新增依赖 · discover/heartbeat 复用原 EurekaClient SDK。

## 变更文件清单

**后端**(3 处):
- `service/InterfaceConfigService.java`(+3 hook + 1 required=false 注入)
- `socket/inbound/SocketInboundServerManager.java`(javadoc 注释更新)
- `service/registry/eureka/EurekaRegistryClient.java`(+90 行 register/deregister REST 实装)

**后端测试**(1 处):
- `test/service/registry/eureka/REG1EurekaClientTest.java`(mock RestTemplate + 2 处 verify)

**前端 / 部署**(5 处):
- 新建 `frontend/Dockerfile`
- 新建 `frontend/nginx.conf`
- 新建 `frontend/.dockerignore`
- `docker-compose.yml`(+ frontend service · tag 0.3.7 → 0.3.9)
- `docs/03-开发/Docker部署方案.md`(REL-1 完备说明)

## 自动化验证

- 后端 `mvn test -Dtest=REG1EurekaClientTest` 通过
- 后端 `mvn test` 全量通过(待背景任务确认)
- 前端 `npm run build` 通过

## 手工验收 checklist

### Task 1 · lifecycle hook
- [ ] 建 INBOUND_SOCKET 接口 port=9099 · 保存(status=draft)· 用 netstat 应无 9099
- [ ] 前端 InterfaceList 点"发布" · Manager 应 start · netstat 应有 9099
- [ ] 点"禁用" · Manager 应 stop · netstat 应无 9099
- [ ] 换端口 9199 保存新接口 · 发布 · netstat 有 9199
- [ ] 两个接口都发布时 port 冲突场景(同 9099)· 应 log warn 且 publish 不回滚

### Task 2 · nginx 镜像
- [ ] `cd frontend && docker build -t powergateway-frontend:0.3.9 .` 通过(~2min · npm ci + vite build)
- [ ] `docker compose up -d` · 4 容器起 · frontend healthy
- [ ] 浏览器 http://localhost:5173 显示 login 页 · admin/Admin@123 登录
- [ ] Swagger:http://localhost:5173/swagger-ui/index.html 正常展示(nginx 代理转发 backend:8080)
- [ ] SPA history 路由:http://localhost:5173/interface/wizard 直连不 404

### Task 3 · Eureka selfRegister
- [ ] 起 Eureka Server(如 pg-testkit 或本机 docker run steeltoeoss/eureka-server)
- [ ] 后端 `/tools/registry` 建 Eureka 类型注册中心 · serverAddr http://127.0.0.1:8761/eureka/
- [ ] 触发一次 register(如通过测试 API 或 RegistryHeartbeatScheduler)
- [ ] Eureka Server 面板应看到 POWERGATEWAY 实例 up · ip:port 正确
- [ ] deregister 后 Eureka 面板应移除

## 顺延后续版本

- **pg-testkit MultiAppSelfRegister**:v0.3.11+ 独立 minor(测试环境同时注册多个假应用给 backend discover)
- **v0.4.0 REL-1B**:k8s helm chart 骨架 + Docker CI 冒烟 + Oracle 业务库集成文档
- **v0.5.0 FN-BIZ**:business_op_log 第四表 + BizLogAspect
- **可选优化**:Eureka 走 spring-cloud-starter(需评估依赖冲突)· 支持 heartbeat PUT 每 30s(现在只 discover)

## Tag checklist

- [ ] 3 Task 手工验收 pass(v0.3.9-基线 checklist)
- [x] backend `mvn test -Dtest=REG1EurekaClientTest` 通过
- [x] backend `mvn test` 全量通过(待确认)
- [x] frontend `npm run build` 通过
- [ ] frontend dist commit
- [ ] `git tag -a v0.3.9`(本地 · push 待用户确认)
