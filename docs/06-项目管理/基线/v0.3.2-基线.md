# PowerGateway v0.3.2 基线

- **版本**:v0.3.2 (minor)
- **交付日期**:2026-08-02(编码完成 9/12 Task · 待打 tag)
- **主题**:SOCK-5 lcpt-bank 入站场景闭环 · 后端能力完整
- **前置版本**:[v0.3.1](./v0.3.1-基线.md)(2026-08-02 中间收官 · CR-007 双层路由 + CR-003 版本 + trace_id 三表)
- **未含**:Task 8 前端 INBOUND_SOCKET 3 step · Task 10 端到端手工验收 · Eureka selfRegister 实装

---

## 一、交付内容

### 1. SOCK-5 后端入站完整能力(CHG-042)

**触发**:2026-07-28 用户 FB-052 lcpt-bank 场景细化 · Q1=B(CR-007 v0.3.1 前置) · Q17=A(JSON 骨架最小 UI) · Q20=C(短连接 + 长连接预留) · Q23=A(v0.3.1 trace_id 三表就绪 · orchestrator 复用)。

| 子单元 | 交付内容 |
|---|---|
| **SOCK-5-A** 入站 Socket | Netty ServerBootstrap 复用 v0.3.0 codec(三分帧 + 双编码全支持) · Server/Manager/Handler/Context/Config · @PostConstruct 扫描已发布接口拉起 · connectionMode 仅 short 实装(Q20=C) |
| **SOCK-5-B** HTTP 出站 | HttpOutboundExecutor 复用 REG-1 RegistryFacade.choose(Eureka discover) · X-Trace-Id + X-Original-Function-Id header 透传(用户 Q1 明确) · **Eureka selfRegister 独立 minor 补齐**(discover 已 verify) |
| **SOCK-5-C** 编排 | InboundSocketOrchestrator 核心:dom4j 手工递归提 //FunctionId(免 jaxen) → v0.3.1 双层路由 → 加载 config → XML→Map + 提 bizBody/bizHeader → wrapRequest + 追加 _originalFunctionId → HTTP 出站 → unwrapResponse → buildResponseXml → Channel 回写 · AuditContext opType=INBOUND_SOCKET_EXEC · traceId 全链路 · 异常 writeError XML |
| **SOCK-5-D** JSON 骨架 | JsonSkeletonRenderer 手工简化 JSONPath($/[n]/.name 交替 · 免 com.jayway.jsonpath) · wrapRequest flat(host)+ bank 数组包 · unwrapResponse list 优先 |
| **Task 7 整合** | ExecDispatchService 加 case INBOUND_SOCKET(400 · TCP 端口触发)· InterfaceConfigService.save 校验(SOCKET/INBOUND_SOCKET 不强制 dbConnectionId · INBOUND_SOCKET 必填 inbound+outbound) |

### 2. pg-testkit HTTP Mock(CHG-043 · Task 5 简化)

- HttpMockController @Controller · 8081/mock/** · UTF-8 · 递归提 body.head.FunctionId · 加载 mocks/http/{fnId}.json → fallback default.json
- mocks/http/180345.json:bank 报文 180345 应答样本
- **Eureka MultiApp SelfRegister 独立 minor 补齐**(v0.3.2 用户手工 Eureka 注册 pg-testkit 应用名)

---

## 二、测试指标

| 项 | 数值 |
|---|---|
| **后端 mvn test** | 740 用例全绿(v0.3.1 基线 699 + v0.3.2 SOCK-5 新增 41) |
| 后端 v0.3.2 新增测试类 | SOCK5AInboundServerTest(9)+ SOCK5BHttpOutboundTest(5)+ SOCK5COrchestratorTest(13)+ SOCK5DJsonSkeletonRendererTest(14)= 41 用例 |
| **pg-testkit mvn test** | 7 用例全绿(SocketMockServerTest 4 + HttpMockControllerTest 3) |
| **前端 npm run build** | 通过(v0.3.2 未动前端 · 沿用 v0.3.1 dist) |
| **手工端到端 MT-21-\* / Task 10 bank 180345** | **⏸ 待用户手工验收** |

---

## 三、未完成 · 顺延后续版本

| 项 | 顺延到 | 原因 |
|---|---|---|
| **Task 8 前端 INBOUND_SOCKET 3 step + JSON 骨架折叠区**(3 人日 · InterfaceWizard SelectInterfaceSteps 714 行) | **v0.3.5 或 v0.3.7 独立 minor** | 用户 2026-08-02 拍板拆下会话 · 与 v0.3.1 Task 5 分类型 4 页 + SOCK-2 wizard 深度集成同批做 |
| **Task 10 端到端 bank 报文 180345 手工验收** | 待用户配合(pg-testkit + 用户 Eureka 实例 + telnet/nc) | 需真实运行环境 |
| **Eureka selfRegister 实装** | **独立 minor**(v0.3.5/7) | 需 spring-cloud-starter-netflix-eureka-client 或手动装配 · 与 SocketInboundServerManager 自动 lifecycle hook 一起做 · discover 已 verify · selfRegister 只影响联机反向发现 PG |
| **SocketInboundServerManager 自动 hook**(接口 publish/disable/delete 事件监听自动 start/stop Server) | 独立 minor | v0.3.2 用户可通过重启 backend 或手动调 manager API |
| pg-testkit Eureka MultiAppSelfRegister | 独立 minor | 用户手工 Eureka 注册 pg-testkit 应用名(如 lcpt-hstc-online-query · IP=127.0.0.1 · port=8081) |
| business_op_log 第四表 | **v0.5.0** | CR-002 FN-BIZ 一起做 |

---

## 四、Git 提交序列(v0.3.2 起 · 5 commit)

```
[Task 5]  d4d2ccf feat(testkit): SOCK-5-B pg-testkit HTTP Mock 联机 · bank 180345 应答样本
[Task 7]  038ea75 feat(sock): SOCK-5 后端整合 · Manager + Handler orchestrator 集成 + Service 校验
[Task 3]  05305f3 feat(sock): SOCK-5-C 入站编排 orchestrator + 渠道 fn 透传 + traceId 全链路
[Task 4]  03358d0 feat(sock): SOCK-5-B HttpOutboundExecutor · Eureka discover + traceId 透传
[Task 6]  cde5507 feat(sock): SOCK-5-D JsonSkeletonRenderer + JsonSkeletonConfig
[Task 2]  1720bb4 feat(sock): SOCK-5-A 入站 Socket Server 骨架 + traceId
```

---

## 五、Tag 前 checklist

- [x] 后端 mvn test 全绿(740)· pg-testkit mvn test 全绿(7)
- [x] CHG-042/043 归档
- [x] 本基线文档创建
- [ ] 路线图 v0.3.2 迁"已发布"(附 Task 8 顺延说明)
- [ ] 待办 v0.3.2 段勾掉 + Task 8 保留活跃
- [ ] 反馈簿 FB-052 状态"部分交付 · v0.3.0 出站 + v0.3.2 入站已交付"→ ✅ 主链路交付
- [ ] backend/CLAUDE.md 加 SOCK-5-A/B/C/D 单元
- [ ] 3 顶层索引同步
- [ ] 手工 P0+P1 + MT-21-* + Task 10 端到端(待用户)
- [ ] `git tag -a v0.3.2`(本地 · **push 待用户确认**)
- [ ] `git tag -a v0.3.2-preview -m "银行 Socket 场景试点可用"`(Q4=A · 附 preview tag)

---

## 六、下一版本预告

**Task 8 前端 INBOUND_SOCKET wizard 集成**(v0.3.5 或 v0.3.7):3 step + JSON 骨架折叠区 · 与 v0.3.1 Task 5 分类型 4 页 + SOCK-2 wizard 深度集成同批。

**Eureka selfRegister 实装 + SocketInboundServerManager 自动 hook**(独立 minor):补齐联机反向发现 PG 能力 + 接口 publish/disable 事件监听自动启停 Server。
