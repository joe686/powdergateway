# v0.3.13 基线(2026-08-03 · minor · pg-testkit Eureka heartbeat 续约)

**版本编号**:v0.3.13 · **类型**:minor · **主线**:v0.3.11 CHG-054 短板补齐
**发布日期**:2026-08-03 · **归档 CHG**:CHG-056(1 条)

## 主要交付(4 Task)

### Task 1 · EurekaClient.renew(PUT /apps/{APP}/{instId})

Netflix Eureka lease renewal REST · PUT 返 200 = RENEWED · 404 = NOT_FOUND(需 re-register)· 其他 = FAILED。

### Task 2 · MultiAppSelfRegister.renewAll + 404 自愈

遍历 registered 逐个 renew · 404 自动调 register(自愈无需人工介入)· 返成功数。

### Task 3 · HeartbeatScheduler + PgTestKitApplication @EnableScheduling

`@Scheduled(fixedDelay=30000, initialDelay=30000)` · 只在 enabled=true + registered 非空时执行 · 空跑无害。

### Task 4 · E2E 验证 + 归档 + tag

- pg-testkit `mvn compile` ✅
- lcpt-register 联机 · heartbeat 15000ms 缩短 · 60s 后 3 应用仍 UP(v0.3.11 早 evict)

## 变更文件清单

**pg-testkit**(5 处):
- `eureka/EurekaClient.java`(+40 行 renew + RenewResult 枚举)
- `eureka/MultiAppSelfRegister.java`(+15 行 renewAll)
- 新建 `eureka/HeartbeatScheduler.java`(35 行)
- `PgTestKitApplication.java`(+ @EnableScheduling)
- `application.yml`(+ heartbeat-interval-ms: 30000)

**docs**:
- `docs/03-开发/变更记录.md`(CHG-056)
- 本文件 · 路线图 · docs/README · 06-项目管理/README

## 手工验收 checklist

- [ ] pg-testkit `application.yml` enabled=true · 起 pg-testkit
- [ ] 等 90s+(超过 v0.3.11 evict 时间)
- [ ] `curl http://localhost:8091/eureka/apps` 3 应用仍全 UP
- [ ] pg-testkit log 应有 `heartbeat renewAll 3/3` debug(或 warn 若部分失败)
- [ ] 手工 curl DELETE Eureka Server 侧某应用 · 下次 renew 应触发 404 自愈 re-register

## 顺延后续

- v0.4.0 REL-1B(helm chart · CI · 内嵌 Eureka Server 可选模块)
- v0.5.0 FN-BIZ(business_op_log 第四表 · CR-002)

## Tag checklist

- [x] pg-testkit mvn compile 通过
- [x] E2E 60s 后 3 应用仍 UP · heartbeat 生效证明
- [x] application.yml enabled 恢复默认 false
- [ ] `git tag -a v0.3.13`(本地 · push 待用户确认)
