# 反馈簿

> **单一入口**：所有需求反馈 / 问题反馈 / 视觉调整 / 新想法，都追加到本文件的[「活跃反馈区」](#三活跃反馈区)。
> **单一出口**：处理进度与结果直接回写本文件的**状态列**；重大范围变更同步归档到 [变更记录 CHG-XXX](../03-开发/变更记录.md)；发新版本时归档到 [路线图 · 已发布](./路线图.md#已发布)。

---

## 一、使用说明

### 你（用户）怎么反馈

在[「活跃反馈区」](#三活跃反馈区)的**顶部**追加一条，最简格式：

```markdown
### FB-XXX <一句话现象>
- 时间：YYYY-MM-DD
- 反馈：<你的原话，越具体越好，可以粘图>
```

`XXX` 是编号，从当前活跃反馈区最新那条 +1 递增。**你不用管分类、优先级、目标版本，我来补**。

### 我（Claude）接手后自动做的

1. **补齐条目字段**：分类（Bug / 新需求 / 视觉调整）、优先级（P0~P2）、目标版本、状态
2. **规划**：
   - Bug 且小 → 直接修
   - Bug 且大 或 新功能 → 在 `docs/03-开发/任务计划/YYYY-MM-DD-FB-XXX.md` 写详细任务分解（TDD）
   - 新需求 → 同步落一份 CR 条目到 [待办与缺陷池 § 变更申请](./待办与缺陷池.md#三变更申请模板)
3. **实施**：按 TDD 走 Red → Green → Refactor（后端加测试；前端跑 build + 相关 vitest）
4. **归档**：范围变更写 CHG-XXX 到 [变更记录.md](../03-开发/变更记录.md)；非范围变更不写 CHG
5. **回写状态**：把本反馈条目的状态列改到最新状态，加上 CHG 引用或 commit 短 SHA
6. **发版**：累计到打新 tag 时，同步 [路线图.md](./路线图.md) 已发布表

### 你在哪看结果

| 想看什么 | 去哪 |
|---|---|
| 我反馈的这条现在什么状态？ | 本文件[「活跃反馈区」](#三活跃反馈区)对应 FB-XXX 的状态列 |
| 具体做了什么改动？ | 本文件里 FB-XXX 引用的 CHG-XXX 或 commit SHA |
| 涉及哪些设计变更？ | [变更记录.md](../03-开发/变更记录.md) 对应 CHG |
| 修完在哪个版本发布？ | [路线图.md](./路线图.md) 已发布表 |
| 详细任务分解（大改） | `docs/03-开发/任务计划/` 对应 FB-XXX 文件 |

---

## 二、状态图例

| 图标 | 状态 | 含义 |
|------|------|------|
| 📥 | 待处理 | 你刚提交，我还没受理 |
| 🔍 | 已受理规划中 | 我在写方案/任务分解，未开始编码 |
| 🔧 | 开发中 | 代码在改 |
| 🧪 | 测试中 | 代码改完，跑测试或等你验证 |
| ✅ | 已交付 | 已合并 master + CHG-XXX 归档 + 版本已发（或待打 tag） |
| 🚫 | 已拒绝 | 明确不做（拒绝原因在条目里写清楚） |
| 💤 | 已延后 | 归入某个未来版本，见目标版本字段 |

---

## 三、活跃反馈区

> **在此区顶部追加新反馈**。编号从下方历史归档最大编号 + 1 开始（当前活跃反馈的最新条目决定下一编号）。

**下一个可用编号：`FB-052`**

### FB-051 系统配置菜单：分组标题 `????` + 每行右侧「（编码异常，请联系管理员）」

- **时间**：2026-07-27
- **反馈原文**：看一下系统配置菜单,有乱码。
- **反馈截图**：`docs/06-项目管理/反馈图片/系统配置菜单，有乱码.png`（顶部分组卡片 header 显示 `????`,下方每行 `registry.*` key 右侧显示黄色斜体「（编码异常,请联系系统管理员）」）
- **分类**：Bug + 老问题复发（**两层原因**：① 前端 UX-F/CHG-021 兜底遗漏 1 处 · 分组名 `<template #header>` 未过 `sanitize` · ② 用户本地 MySQL 字符集配置有历史遗留问题,`sys_config.group_name` / `description` 中文字段被写成 `????`）
- **优先级**：P2（有 workaround = 忽略视觉污染,配置本身仍可用;根治方案 UX-F 已给出 `docs/03-开发/MySQL字符集配置指南.md`,用户可自助修数据层）
- **目标版本**：**v0.2.1 patch**（与 FB-050 同批 · 待打 tag）
- **状态**：✅ 已交付（待随 v0.2.1 patch tag 发版）
- **根因分析**（Step 3 排查已完成）：
  1. **兜底逻辑本身工作正常**：截图上"（编码异常,请联系系统管理员）"正是 UX-F/CHG-021 交付的 `sanitize()` 兜底文案（[`textSanitizer.js`](../../frontend/src/utils/textSanitizer.js) 触发规则:`U+FFFD` / 连续 3+ 问号 / 锟斤拷）· 每行 description 已被正确保护
  2. **前端兜底遗漏点** [`SystemConfig.vue:31`](../../frontend/src/views/system/SystemConfig.vue#L31)：`<template #header>{{ group }}</template>` **未过 sanitize**,导致分组名 `????` 直接暴露(截图顶部的 `????` = 某个乱码的 `groupName` 值)· UX-F 当年只修了 `description` 一处,漏了 `groupName`
  3. **数据层根因(部署环境问题,非代码 bug)**：用户本地 MySQL 服务端字符集大概率是 `latin1`,或 JDBC url 缺 `useUnicode=true&characterEncoding=UTF-8` → 中文 INSERT 时被丢弃为 `?`· 老数据一旦存成 `????` 后前端无法逆转 · UX-F 已给完整修复指南 [`docs/03-开发/MySQL字符集配置指南.md`](../../docs/03-开发/MySQL字符集配置指南.md)(改 my.ini + 迁移 SQL + JDBC url 参数)
- **修复方案**（分两个动作）：
  - **代码补丁(必做,前端 1 行)**：`SystemConfig.vue:31` 改为 `<template #header>{{ sanitize(group, '（编码异常）') }}</template>` · 补齐 UX-F 遗漏,让分组名也走兜底,不再露 `????`
  - **数据修复(用户自助 or patch 附带)**：按 [`MySQL字符集配置指南.md`](../../docs/03-开发/MySQL字符集配置指南.md) 走 7 步 —— 改 `my.ini` 加 `character-set-server=utf8mb4` + 检查 JDBC url + 用户可选择重跑 `init.sql`(或人肉 UPDATE sys_config 恢复中文) · v0.2.1 patch 交付时可附一份 `migration-encoding-fix.sql` 兜底(如 UX-F 未产出的话)
- **验收要点**：
  - 截图场景:即便数据库仍有乱码,分组名不再显示 `????`,而显示"（编码异常）"占位(与 description 一致)
  - 若用户按指南修完数据层,该菜单所有中文完整显示,无兜底文案
  - `npm run build && npx vitest run` 前端全绿,不引入回归
- **处理链路**：Step 1 Ingest ✅ → Step 2 Triage ✅（Bug/P2）→ Step 3 Plan ✅（根因链 + 补丁方案就位）→ **Step 4 Execute ✅**：`SystemConfig.vue:31` 分组名过 `sanitize()` 补齐 UX-F 遗漏 + `migration-encoding-fix.sql` 在 UX-F 4 条基础上追加 FB-051 REG-1 4 条（`registry.self.service_name` / `registry.self.ip.override` / `registry.heartbeat.interval.seconds` / `registry.heartbeat.fail.threshold`，按 [`init.sql:178-181`](../../backend/src/main/resources/db/init.sql#L178) 原文覆写 · HEX 白名单幂等）· 本地 MySQL 已跑 migration 8 条全部恢复中文（用户已确认）· `npm run build` 通过 · commit `ff2f62a` → **Step 5 Archive ✅ 无 CHG**（前端遗漏补齐 + 数据修复 · 不改交付单元行为语义）→ Step 6 随 v0.2.1 patch tag 发版

### FB-050 辅助工具 → Swagger 文档打开显示本系统菜单（Vite dev proxy 未覆盖 /swagger-ui）

- **时间**：2026-07-27
- **反馈原文**：辅助工具里面的 Swagger 文档,打开了还是我们这个系统的菜单,要排查定位一下问题,如果有问题就要修复。可以放到后面的版本修复,做好记录。
- **分类**：Bug（开发环境入口不可用 · 生产同源部署时不受影响 · 无需 CHG,不改交付单元行为语义）
- **优先级**：P2（辅助功能 · 有 workaround = admin 直接开 `http://localhost:8080/swagger-ui/index.html`,不阻塞主流程）
- **目标版本**：**v0.2.1 patch**（与 FB-051 同批 · 待打 tag）
- **状态**：✅ 已交付（待随 v0.2.1 patch tag 发版）
- **根因链**（Step 3 排查已完成 · 便于后续实施者直接下手）：
  1. **前端触发点** [`frontend/src/views/tools/SwaggerRedirect.vue:20-27`](../../frontend/src/views/tools/SwaggerRedirect.vue#L20)：`SWAGGER_URL = '/swagger-ui/index.html'`,`onMounted` 里 `window.open(SWAGGER_URL, '_blank', 'noopener')` 打开新窗口
  2. **实际请求路径**：因是相对路径,新窗口打开的是 `http://localhost:5173/swagger-ui/index.html`(前端 dev server 端口 5173,不是后端 8080)
  3. **Vite proxy 配置缺口** [`frontend/vite.config.js:16-20`](../../frontend/vite.config.js#L16)：`proxy` 只声明了 `/api → 8080`,**未覆盖 `/swagger-ui` / `/v3/api-docs` / `/webjars`**
  4. **SPA history fallback 兜底**：`/swagger-ui/index.html` 未匹配任何前端路由 → Vite 默认 fallback 到 `index.html` → 渲染 PowerGateway 前端应用 → 展示 MainLayout(用户看到的"我们这个系统的菜单")
  5. **生产不受影响的原因**:生产 build 后前端静态文件由 Spring Boot 从 `backend/src/main/resources/static/` 提供(同 8080 端口),`/swagger-ui/*` 与前端 SPA 在同一 servlet 容器下由 SpringDoc 自己处理,不会被前端路由拦截 → 所以本 bug **仅 dev 环境触发**
- **修复方案 · 用户 2026-07-27 拍板走 B**：
  - ~~方案 A（最小改动 1 行）：`SwaggerRedirect.vue` 的 `SWAGGER_URL` 改为绝对地址~~ · 已弃用（host/port 硬编码不健壮）
  - **✅ 方案 B（已选）**：`vite.config.js` proxy 追加 3 条：`/swagger-ui`、`/v3/api-docs`、`/webjars` 全部 `target: http://localhost:8080, changeOrigin: true`。前端代码不动 · dev/prod 行为对齐 · 3 行改动。**实施时无需再问决策**
- **验收要点**：
  - dev 环境启 `npm run dev` + 后端 8080,登录 admin 后点辅助工具 → Swagger 文档 → 新窗口打开真正的 Swagger UI(SpringDoc 界面,有各 Controller 分组和 Try it out 按钮)
  - 非 admin 用户菜单里不显示 Swagger 文档条目(现有权限 CHG-026 已收归,回归时确认未破坏)
  - `frontend && npm run build && npx vitest run` 前端单测全绿(现有 [UX-B_SideMenu.spec.js](../../frontend/src/components/layout/__tests__/UX-B_SideMenu.spec.js) 已覆盖菜单显隐,无回归风险)
- **处理链路**：Step 1 Ingest ✅ → Step 2 Triage ✅（Bug/P2）→ Step 3 Plan ✅（用户拍板方案 B）→ **Step 4 Execute ✅**：`vite.config.js` proxy 追加 `/swagger-ui` / `/v3/api-docs` / `/webjars` 3 条 → `http://localhost:8080` · 前端代码不动 · `npm run build` 通过无回归 · commit `03de314` → **Step 5 Archive ✅ 无 CHG**（仅 dev 环境配置修复 · 不改交付单元行为语义）→ Step 6 随 v0.2.1 patch tag 发版

### FB-049 字段配置字典下拉无值时可跳转字典菜单新建 · 回填选中

- **时间**：2026-07-27
- **反馈原文**：设置字段配置的时候,如果没有想要的字典值就可以跳转到对应的菜单,设置完成后回去可以接着选。
- **分类**：新需求（前端联动跳转 + 状态保持 · 增强 FN-12 与 M1-6/M2-3/4/5/6 向导集成体验）
- **优先级**：P1（不阻塞主流程,但当前无跳转 → 用户被打断需切菜单/开多标签,与"减少工时"产品目标相悖）
- **目标版本**：**v0.2.5 minor**（用户 2026-07-27 拍板 · 见 [CR-004](./待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转)）
- **状态**：💤 已延后 → v0.2.5 · 关联 [CR-004](./待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转) 已接受
- **影响面**（Step 3 影响分析）：
  - **触点**：`FieldProcess.vue` / `InterfaceWizard.vue` step 6 字典选择器（三级联动"系统 → 字典键 → 方向"下拉，见 [CR-001 § 前端 UI](./待办与缺陷池.md#cr-001-字典转换配置字段字典映射)）+ `DictMappingList.vue` + 各 wizard 入口路由
  - **交互设计**：字典键下拉底部加"+ 新建字典键"链接 → 跳 `DictMappingList.vue` 并 URL query 预填 `?system=CIF&dictKey=GENDER&direction=1&returnTo=<原 wizard 路径 + step>`；字典页保存后跳回原路径 + 通过 `sessionStorage['pg-dict-return-value']` 传递新建的 dict_key，原页面 mount 时读取 + 重拉下拉 + auto-select
  - **状态保留**：wizard 已填字段（前 5 步的报文/字段映射）必须在跳走前存 `sessionStorage['pg-wizard-draft-{interfaceId}']`,回来 restore；否则用户失去半小时工作量 → 关键护栏
  - **依赖**：FB-047 拆菜单后跳转目标要走对应侧（转换接口场景 → `/transform/dict` · 可视化接口场景 → `/interface/dict`）
- **处理链路**：Step 1 Ingest ✅ → Step 2 Triage ✅（新需求/P1）→ Step 3 Plan ✅（落 [CR-004](./待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转) · 用户 2026-07-27 拍板归 v0.2.5 minor）→ 等 v0.2.5 排期 Step 4/5/6

### FB-048 字典管理新增弹窗改批量键值对形态 · 一次可添几十上百条

- **时间**：2026-07-27
- **反馈原文**：这设置起来太不方便了,我觉得新增弹窗可以做成加一下就多一条键值对的这种。然后新增的这个弹窗可以输入了系统和字典键之后就可以在原来的基础上再加,这样方便操作一点,有的键值对有几十上百个,不太好。
- **分类**：新需求（前端交互形态重构 + 后端批量创建接口 · 现有单条弹窗不适合中量数据）
- **优先级**：P1（严重影响字典配置效率 · 用户实际数据规模明确说"几十上百"）
- **目标版本**：**v0.2.5 minor**（用户 2026-07-27 拍板 · 见 [CR-004](./待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转)）
- **状态**：💤 已延后 → v0.2.5 · 关联 [CR-004](./待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转) 已接受
- **影响面**（Step 3 影响分析）：
  - **触点**：`DictMappingList.vue` 内嵌的新增/编辑对话框（当前一条一保存,见 [CR-001 § 前端 UI](./待办与缺陷池.md#cr-001-字典转换配置字段字典映射)）+ `DictMappingController` 后端
  - **新交互形态**：弹窗顶部区固定"系统 + 字典键 + 方向"三个字段(输入后锁定不可改) → 下方"键值对列表区"可循环 `+ 添加一行` 按 source_value / target_value / cn_label 三列表单编辑 · 支持删除行 · 支持粘贴 TSV(Excel 复制) · 提交时全部条目共享顶部三字段
  - **后端**：现有 `POST /api/dict-mapping` 单条 → 新增 `POST /api/dict-mapping/batch` 收 `{system, dictKey, direction, items: [{source, target, label}, ...]}` · 整批事务;或让单条 API 直接支持数组(推荐前者,语义清晰)
  - **与 FN-11 关系**：FN-11 Excel 导入仍保留(覆盖 100+ 大量数据) · 本次批量弹窗覆盖"3-30 条中量数据"填空 · 两者互补不冲突
  - **验收要点**：一次提交 50 条要在 500ms 内落表(含 Redis 缓存刷新) · 中间某行校验失败要精准报错到行号 · 支持粘贴 3 列 TSV 一次预填
- **处理链路**：Step 1 Ingest ✅ → Step 2 Triage ✅（新需求/P1）→ Step 3 Plan ✅（落 [CR-004](./待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转) · 用户 2026-07-27 拍板归 v0.2.5 minor）→ 等 v0.2.5 排期 Step 4/5/6

### FB-047 字典映射管理按业务场景拆两菜单 · 单表加字段区分

- **时间**：2026-07-27
- **反馈原文**：这个字典映射管理,我想可以拆成两个菜单,分别放到接口开发还有转接口里面去。数据库用一个表就够了。但是在数据库里面加一个字段是表示转接口,还是接口开发的。给前端展示的话,两个菜单各自过滤一下就行了。
- **分类**：新需求（IA 调整 + 后端表结构变更 · 改造已交付 FN-12 · 需 CHG）
- **优先级**：P1（影响使用心智 · 用户明确诉求业务隔离 · 但当前"辅助工具"入口 workaround 可用）
- **目标版本**：**v0.2.5 minor**（用户 2026-07-27 拍板 · 见 [CR-004](./待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转)）
- **状态**：💤 已延后 → v0.2.5 · 关联 [CR-004](./待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转) 已接受
- **影响面**（Step 3 影响分析）：
  - **表变更**：`dict_mapping` 加字段 `scope TINYINT NOT NULL COMMENT '1=接口转换M1侧 2=可视化接口M2侧 3=通用共享'` · 唯一键从 `uk_src(system_code, dict_key, direction, source_value)` 扩为 `uk_src(scope, system_code, dict_key, direction, source_value)`
  - **增量迁移 SQL**：老数据默认 scope=? 待用户拍板(推荐 3 = 通用共享,老配置对两侧继续可见,不破坏行为语义)
  - **后端**：`DictMappingController` 所有查询/保存加 `scope` 过滤参数;`DictMappingProcessor` 运行时按当前接口类型自动传 scope（M1-3 场景传 1 · M2-x exec 传 2 · 都 fallback 到 3 共享条目）
  - **前端**：
    - 现有 `/tools/dict` 保留为"辅助工具 → 字典总览"(scope=3 或全量,待定) 或直接下线(待用户拍)
    - 新增 `/transform/dict`（挂"接口转换"菜单下 · scope=1 锁定）+ `/interface/dict`（挂"接口开发"菜单下 · scope=2 锁定）
    - `DictMappingList.vue` 抽通用组件,3 个入口分别传 `scope` prop（1/2/3）· 查询 / 新增 / 编辑都锁定 scope 字段
  - **测试文档同步**：MT-19-* 字典管理 12 条手工用例要按 scope 维度拆开重排(每侧独立 6 条 or 分层)
  - **CHG**：范围变更 · 影响 [CHG-028](../03-开发/变更记录.md#chg-028)（表结构）+ [CHG-031](../03-开发/变更记录.md#chg-031)（前端菜单）+ 增量迁移 SQL 类比 FB-045 附加交付资产
- **处理链路**：Step 1 Ingest ✅ → Step 2 Triage ✅（新需求/P1）→ Step 3 Plan ✅（落 [CR-004](./待办与缺陷池.md#cr-004-fn-12-v2--分场景拆菜单--批量键值对弹窗--字段联动跳转) · 老数据 `scope=3` 兜底 + 开发环境实施时 TRUNCATE + `/tools/dict` 下线 · 用户 2026-07-27 拍板）→ 等 v0.2.5 排期 Step 4/5/6

### FB-046 亮色主题弹窗类颜色偏灰对比弱 · 影响全站 el-dialog / el-drawer / el-message-box

- **时间**：2026-07-27
- **反馈原文**：亮色环境下弹窗类的菜单，颜色偏灰，而且内容对比不明显，看起来就难受。要优化一下，把这个排版做好看。可以下一个版本修复。记得做好相关记录。
- **反馈截图**：`docs/06-项目管理/反馈图片/亮色菜单，编辑弹窗类要优化.png`（新增渠道配置对话框亮色态 · 弹窗大片灰底 + 表单区必填红字融于灰底 + 底部按钮区无分隔感）
- **分类**：视觉调整（全站 UX 打磨 · 不改行为语义 · 无需 CHG）
- **优先级**：P2（视觉打磨 · 不阻塞主流程 · workaround = 切暗色主题）
- **目标版本**：**v0.2.x 补丁通道**（依 [路线图 · 打 tag checklist](./路线图.md#打-tag-命令) 视觉修正走 PATCH · 用户明确"下一个版本修复"= 下次 patch 批次）
- **状态**：💤 已延后 → v0.2.x（等下次 patch 批次统一做）
- **影响面**（Step 3 影响分析,便于后续实施者一次改齐）：
  - **触点定位**：`el-dialog` / `el-drawer` / `el-message-box` 三者已在 [element-plus-override.css:31](../../frontend/src/styles/element-plus-override.css#L31) 全局用 `var(--pg-glass-bg)`（毛玻璃基座 + `backdrop-filter: blur`）；此基座暗色下效果良好，亮色下问题集中
  - **根因假设**：① 亮色 `--pg-glass-bg` token 偏灰（而非近白半透明）+ ② 表单区 `el-form-item` 无独立底色兜底 + ③ 弹窗与页面 body 无 `border` / `box-shadow` 强化边界 → 三者叠加导致糊
  - **修复触点集中在**：[tokens.css](../../frontend/src/styles/tokens.css) 亮色主题变量组（`--pg-glass-bg` / `--pg-dialog-header-bg` / `--pg-border-strong` / `--pg-shadow-dialog`）+ [element-plus-override.css:31](../../frontend/src/styles/element-plus-override.css#L31) 补 border + 弹窗内部 `.el-form` 区局部提亮 + footer 分隔线
  - **回归护栏**：实施时同步在暗色主题下用同一截图场景对比，防止修亮色崩暗色
- **处理链路**：Step 1 Ingest ✅ → Step 2 Triage ✅（视觉调整/P2/v0.2.x）→ Step 3 Plan ✅（挂 [待办 § v0.2.x](./待办与缺陷池.md#v02x--补丁通道持续) · 纯 CSS 改 tokens + override · 无需 TDD 任务计划文档）→ 待下次 patch 批次统一走 Step 4/5/6

### FB-045 v0.2.0 无增量迁移脚本 · 老库升级后 dict_mapping 表缺失

- **时间**：2026-07-27
- **反馈原文**：手工测试 MT-19-02 时发现 `powergateway_config.dict_mapping` 表不存在（`ERROR 1146 Table doesn't exist`），但 init.sql 里第 258 行明明定义了。init.sql 只在**空库首次**执行，老用户从 v0.1.x 拉 v0.2.0 代码后不重建库 → FN-12 相关表全缺 → 前端管理页能进（前端路由硬编码）但保存必败。
- **分类**：Bug（部署/交付缺陷 · v0.2.0 交付时未附增量迁移 SQL · 影响所有从 v0.1.x 升级的用户）
- **优先级**：P0（阻塞 FN-12 全链路验证 · 已阻塞本次 MT-19-* 手工测试；老库客户拉最新代码即中招）
- **目标版本**：v0.2.1（补丁通道 · 补交 `migration-v011-to-v020.sql` + 在 `docs/06-项目管理/v0.2.0-基线.md` 增补"升级步骤"章节 + 检查同期 CHG-030 的 audit 迁移是否有同类漏洞）
- **状态**：✅ 已交付（待随 v0.2.1 patch tag 发版）
- **关联发现**：MT-19-02 手工测试 · [dict_mapping DDL 定义 init.sql:258](../../backend/src/main/resources/db/init.sql#L258) · `git diff v0.1.1..v0.2.0 backend/src/main/resources/db/init.sql` 显示 v0.2.0 仅新增 `dict_mapping` 1 张表（`sql_audit_log` 只是从 init-audit.sql 挪进 init.sql，物理表老库已在 audit 库，CHG-030 的 `migration-audit-to-config.sql` 已覆盖）
- **处理链路**：Step 1 Ingest ✅ → Step 2 Triage ✅（Bug/P0/v0.2.1） → Step 3 Plan ✅（无 TDD 任务计划文档 · SQL+docs 补交无代码变更） → Step 4 Execute ✅：编 [`backend/src/main/resources/db/migration-fn-12-dict-mapping.sql`](../../backend/src/main/resources/db/migration-fn-12-dict-mapping.sql)（沿用 CHG-030 既有 slug 命名风格 · IF NOT EXISTS 幂等 · 本地重跑 3 行数据保留） + [v0.2.0 基线新增「七、升级步骤」章节](./v0.2.0-基线.md#七升级步骤老库客户-v01x--v020)（含备份/合并 audit/补建 FN-12 表/重启/手工验证 5 步） → Step 5 Archive ✅ 无 CHG（不改交付单元行为，只补部署资产） · commit `59c6bb0` → Step 6 随 v0.2.1 patch tag 发版

### FB-044 路线图未收录 v0.5.0 业务菜单 · 文档三处缺失

- **时间**：2026-07-27
- **反馈原文**：现在整个项目开发的进度怎么样，我之前说的做业务菜单的功能也没有规划进路线图里面去，把文档完善一下吧。
- **分类**：Bug（元-文档缺失 · FB-041 已延后到 v0.5.0，CR-002 已接受，两份设计文档已交付，但 [路线图.md](./路线图.md) 从 v0.4.0 直接跳到 v1.0；[待办与缺陷池.md](./待办与缺陷池.md) 无 v0.5.0 backlog 分组；[开发计划.md](../03-开发/开发计划.md) 阶段七止步于 FN-12，无 FN-BIZ 阶段条目）
- **优先级**：P1（不阻塞代码，但阻塞版本可视化 · 用户看不到 v0.5.0 已排期，会以为决策被遗忘）
- **目标版本**：v0.1.x（纯文档补齐，随下次 patch tag 落）
- **状态**：✅ 已交付
- **处理链路**：Step 4 直接改 4 处 → [路线图 v0.5.0](./路线图.md#v050--fn-biz-业务菜单生成--sys-3-升级预估-4--6-周) 段落 + [待办与缺陷池 v0.5.0 backlog](./待办与缺陷池.md#v050) 分组 + [开发计划 阶段八](../03-开发/开发计划.md#阶段八fn-biz-业务菜单生成--sys-3-升级v050-单版本待实现) · 无 CHG（不改任何交付单元行为，仅同步已定案决策到展示层）

### FB-043 审计库表迁到配置库 · 客户环境不建多 schema

- **时间**：2026-07-27
- **反馈原文**：审计库表清单（powergateway_audit）里 sql_audit_log 之前说过要把审计库都放到一个 schema 下面，如果是客户环境，他们不会给我们建多个的。你可以直接把审计库的表里面的内容迁移到系统库里面。然后两个地方的配置配成一样的。然后更新一下文档就行。
- **分类**：新需求（架构 IA 调整 · 部署形态简化 · 影响 M2-9 已交付单元的物理布局，不改行为语义）
- **优先级**：**P1**（阻塞客户交付：客户环境 DBA 不给建 2 个 database；开发环境不受影响有 workaround）
- **目标版本**：**v0.2.0**（与 v0.2.0 ②/③/④ 一批发 MINOR tag）
- **状态**：✅ 已交付
- **处理链路**：用户拍板 B 方案 + 插队 v0.2.0 ② 之前做 → 落地 [CHG-030](../03-开发/变更记录.md#chg-030--2026-07-27--审计库合并到配置库fb-043) · 本地 MySQL 8 条 sql_audit_log 数据已迁 powergateway_config · audit 库已 DROP · mvn test exit 0

### FB-042 显示当前程序版本 · 首页或易见位置

- **时间**：2026-07-26
- **反馈原文**：加一个表记录一下当前的程序版本，在首页或者什么比较容易看到的地方显示，这个放到下一个版本实现。
- **分类**：新需求（P2 · 可见性增强）
- **优先级**：P2（nice-to-have，非阻塞）
- **目标版本**：**v0.3.0**（用户明确"下一个版本"→ 当前 v0.2.0 backlog 已排 4 项 ①~④ 全满，顺延到 v0.3.0）
- **状态**：💤 已延后 → v0.3.0
- **处理链路**：Step 3 完成 → 见 [待办与缺陷池 CR-003](./待办与缺陷池.md#cr-003-程序版本显示)

### FB-041 远期创新功能：接口 → 业务菜单一键生成（含 SYS-3 权限升级伴生）

- **时间**：2026-07-26
- **反馈原文**（渐进披露，六轮补齐）：
  1. 首披露："根据接口开发生成的 api，直接生成业务菜单，我将渐进式披露我的想法，你来帮我完善"
  2. 补充："业务菜单的生成应该就是四类：增删改查。要么是原有接口配置的菜单地方生成，要么就要有新的独立菜单生成：这个独立菜单要有以下功能：节点的生成和重新生成（其实就是发布情况），其次要展示这类接口的一个清单，方便选中生成。之后这个接口清单要表示接口的生成状态，发布状态。这个信息是要跟后续前端展示要同步的。所以还比较重要。最后就是这个菜单要有一个接口生成变更之类的统计表，比如饼图。又或者把这个统计图放到首页统计大屏也可以。"
  3. 补充："生成出来的菜单元素应该相对固定，展示接口信息，然后有一部分接口请求的相关情况。然后有按钮发起，还有个清空选项内容的按钮。响应的话，查询类的下面应该有个列表的空间。其他几类，只要把响应信息反馈出来就行。"
  4. 补充："权限关联：后续还是要对现在的权限功能做改造的。首先就是我们菜单权限要分级，比如一级菜单、2级菜单。给这个用户授权了一级菜单的权限，对应的2级菜单权限应该也要有。再之后就是有高权限的用户可以给低权限的用户做授权。对这种功能菜单也是按需授权。"
  5. 补充："这个根据接口做业务菜单的功能要做成可裁剪的，方便后续升级或者版本管理。"
  6. 补充："所有做业务菜单的操作要有业务监控，记录各种操作信息，比如用户，操作发起的流水号，这个流水号是我们系统本身的，而且要跟之前我们做自动生成接口的时候记录的操作记录表要能关联起来。要不然就乱套了。这个记录要把报文什么的都保留好，要不然后续查问题不好查。这个监控，在后台一个是要做统计，放到首页。其次是要做一个菜单，方便查这个操作记录跟操作的细节。"
  7. 补充："业务菜单生成时菜单名称前要加字母以表示业务类，做好区分。或者在 sys_menu 表就要区分好，测试的工程菜单对应的也要改好。"
  8. 补充："控制裁剪的参数也应该要控制对应的报表、按钮、菜单。或者统计饼图不展示。要不然会有问题。"
  9. 定版："整体功能做一个版本吧，当一个大的版本升级，这个版本只做这个功能。设计文档拆开没问题。全流程有文档跟踪就行。"
- **分类**：新需求（**远期战略功能**，含 SYS-3 已交付单元大改造伴生）
- **优先级**：P1（用户主动主推的战略性功能，先于 v0.3/v0.4 常规演进）
- **目标版本**：**v0.5.0**（单一大版本，只做本功能 + SYS-3 升级伴生；用户 2026-07-26 明确拍板）
- **状态**：💤 已延后 → v0.5.0
- **处理链路**：
  - Step 3 完成 → 见 [待办与缺陷池 CR-002](./待办与缺陷池.md#cr-002-fn-biz-业务菜单生成能力--sys-3-升级)
  - 设计文档已交付：
    - [FN-BIZ 主设计](../02-设计/详细设计/2026-07-26-FN-BIZ-business-menu-generation-design.md)
    - [SYS-3 升级设计](../02-设计/详细设计/2026-07-26-SYS-3-upgrade-design.md)
  - 待用户复审两份设计 → 通过后进入 writing-plans 编写详细实施计划

### FB-040 字典功能排期确认 + 接口文档下载与字典联动（字典对照 sheet + 字段行加字典 key 列）

- **时间**：2026-07-26
- **反馈原文**：对字典加工的功能版本要提前，我们下一个版本就应该实现，然后：字典功能添加了之后，后续的接口文档下载要加应该 sheet 页面，存对照字典关系，然后原本的字段行，要加一列字典的 key，以相关联。
- **分类**：新需求（CR-001 扩展 —— 字典功能与 FN-09 接口文档联动）
- **优先级**：P1
- **目标版本**：v0.2.0（与 FN-12 同版本交付，符合用户"下一版本就实现"的诉求）
- **状态**：💤 已延后 → v0.2.0
- **处理链路**：
  - **排期确认**：FN-12 字典功能已在 v0.2.0 规划（见 [路线图 v0.2.0](./路线图.md#v020--test-1-完善--sqlite-分支--fn-12-字典映射预估-2--3-周)），符合"下一版本"诉求，无需调整排期
  - **文档联动扩展**：见 [待办与缺陷池 CR-001 § 扩展 A](./待办与缺陷池.md#cr-001-字典转换配置字段字典映射) · 已并入 CR-001 v0.2.0 交付范围
  - **方案已确认**（2026-07-26）：**新增 Excel(.xlsx) 多 sheet 下载**。FN-09 从 md/html 双格式扩为三格式（+ xlsx），Sheet 1 基本信息 / Sheet 2 请求字段（含"字典 key"列）/ Sheet 3 响应字段（含"字典 key"列）/ Sheet 4 字典对照。工时 +2 人日

### FB-039 转换接口 / 可视化接口新增字典转换配置（跨系统字典值映射，含方向）

- **时间**：2026-07-22
- **反馈原文**：转换接口跟可视化接口的里面，我要有字典转换配置，比如表设计成，字典对接的系统号，字典标识，A 系统字典值，对应的 B 系统的值，中文含义。这个值感觉不需要全部对应，可以多对 1，就是对外输出的时候转换的目标唯一就行。那么对应的转换方向要有控制，比如表里面加一个字段是转换方向，配置了一个值就生成两条数据，然后转换的时候根据不同的方向来转换。如果你有更好的方法就用好方法。
- **分类**：新需求（大改，跨接口转换 + 可视化接口两个模块）
- **优先级**：P1
- **目标版本**：v0.2.0（用户 2026-07-22 确认，方案 B 单向存储 + 前端双向拆两条）
- **状态**：💤 已延后 → v0.2.0
- **处理链路**：Step 3 已完成 → 见 [待办与缺陷池 § 三 CR-001](./待办与缺陷池.md#cr-001-字典转换配置字段字典映射) · 已接受 · 待 v0.2.0 排期开发

> **FB-037 / FB-038 已随 v0.1.1 打 tag（2026-07-26），条目下移至[「历史归档」](#四历史归档)**。


---

## 四、历史归档

> 已闭环的反馈条目按提出时间倒序归档。此区**只增不改**，作为项目决策与演进的可追溯档案。

### 2026-07-22 批次（v0.1.1 patch，2 条 · 全部闭环）

| FB | 反馈摘要 | 分类 | 落地 |
|----|---------|------|------|
| **FB-038** | 登录页视觉美化对齐 UX-A 圆角毛玻璃 | 视觉调整 | ✅ v0.1.1 · commit `b0db29d` · 附带修 App 层未初始化主题致 tokens undefined 的 latent bug · 参见 [任务计划 2026-07-22-FB-038.md](../03-开发/任务计划/2026-07-22-FB-038.md) |
| **FB-037** | 未登录访问管理台自动跳转登录页 | Bug（3 层根因联合：request 拦截未清 store · Sa-Token 业务码 401 未走登出分支 · MainLayout 未探测 stale token） | ✅ v0.1.1 · commit `6f557e2` · 回归护栏：`tests/store/userAuth.spec.js` 3 用例 + `tests/api/requestAuth.spec.js` 2 用例 · 参见 [任务计划 2026-07-22-FB-037.md](../03-开发/任务计划/2026-07-22-FB-037.md) |

### 2026-07-21 批次（来自 `222.txt`，15 条 · 全部闭环）

| FB | 反馈摘要 | 分类 | 落地 |
|----|---------|------|------|
| **FB-036** | 测试工具改造：需要一个可以按用户要求测试的工具，可视化接口要有全面覆盖功能的数据库，接口转换要有后端响应系统 | 新需求 | ✅ TEST-1 · CHG-024 |
| **FB-035** | 系统使用手册：各菜单功能 + 调用方式 + 效果 + 截图 | 新需求 | 💤 → v1.0（拆到 `docs/05-制品发布/`） |
| **FB-034** | 注册中心：注册后其他系统只需配置对方系统名称，不用配收发地址 | 新需求 | ✅ REG-1 `service://` 协议 · CHG-023 |
| **FB-033** | 注册中心：自己注册为 `POWERGATEWAY`，其他系统能发现并直接发报文 | 新需求 | ✅ REG-1 自注册 · CHG-023 |
| **FB-032** | 注册中心：同时注册到多个注册中心，设置系统时可选连接哪几个 | 新需求 | ✅ REG-1 Nacos + Eureka 多注册中心 · CHG-023 |
| **FB-031** | 配置导入：不能仅支持 zip，要支持 md/excel，多选同类型，每接口独立文件，与接口文档打通双向 | 新需求 | ✅ FN-11 扩展 · CHG-022 |
| **FB-030** | 配置导入导出归辅助工具大类；导出成 zip 报错 | Bug + IA 调整 | ✅ UX-B 归位 · CHG-017 · 导出 zip 见 commit `9506731` |
| **FB-029** | 导出报表功能报错 | Bug | ✅ commit `9506731` Blob 拦截器 bug |
| **FB-028** | 接口文档归辅助工具大类 | IA 调整 | ✅ UX-B · CHG-017 |
| **FB-027** | 接口转换配置向导：来源/目标报文格式按钮没有选项含义说明 | 视觉调整 | ✅ UX-D · commit `b0a4853` |
| **FB-026** | 表格 fixed 列半透明看到下面文字 | Bug | ✅ UX-A · commit `50dd6f0` |
| **FB-025** | tooltip 文字颜色与背景一致看不清 | 视觉调整 | ✅ UX-A · commit `50dd6f0` |
| **FB-024** | 主题设置抽屉打开空白 | Bug | ✅ commit `fe5fba3` + `043ac37` |
| **FB-023** | 暗色模式表格不要明暗行区分（刺眼） | 视觉调整 | ✅ UX-A · commit `50dd6f0` |
| **FB-022** | 导航栏缩小后右侧展开去掉 | 视觉调整 | ✅ UX-A · CHG-016 |

### 2026-07-19 批次（来自 `111.txt`，21 条 · 全部闭环）

| FB | 反馈摘要 | 分类 | 落地 |
|----|---------|------|------|
| **FB-021** | 转接口 + 可视化接口模板导入导出（一键导入导出、按功能号识别） | 新需求 | ✅ FN-11 + FN-11 扩展 · CHG-022 |
| **FB-020** | 转接口 / 可视化接口的常用查询菜单，导出成报表 | 新需求 | ✅ 各查询列表 Excel 导出（M1-5 / M2-2 / SYS-1 / SYS-2 都已具备） |
| **FB-019** | 接口文档：转换接口 + 可视化接口两侧，各有侧重，包含关键信息，下载即用 | 新需求 | ✅ FN-09 接口文档 · UX-E · CHG-020 |
| **FB-018** | 报文调试：美化 + XML 支持 | 视觉 + 新需求 | ✅ UX-F 美化 · AUX-1 支持 XML |
| **FB-017** | 系统配置参数注释中文乱码 | Bug | ✅ UX-F · CHG-021 |
| **FB-016** | 可视化接口菜单美化统一 | 视觉调整 | ✅ UX-A/B · CHG-016 · CHG-017 |
| **FB-015** | 接口管理三点：接口发布支持多报文格式 + 查询接口字段导出 Excel + 修改插入删除返回信息完善 | 新需求 | ✅ M2-7 支持 · FN-09 字段导出 · UX-E · CHG-020 |
| **FB-014** | 字段公式管理（当前显示"功能开发中"），要做并嵌入 CRUD 接口 | 新需求 | ✅ FN-03 · UX-C · CHG-018 |
| **FB-013** | 接口转换配置向导（对齐可视化接口开发的向导） | 新需求 | ✅ UX-D `TransformWizard.vue` |
| **FB-012** | 接口转换配置流程重构：明确系统间通信、功能号定位、报文来龙去脉层层递进 | 新需求（大改） | ✅ UX-D 向导落地 + UX-B 信息架构重整 · CHG-017 |
| **FB-011** | 转换模板管理关联性：转换模板管理放到字段映射配置前面 | IA 调整 | ✅ UX-B · CHG-017 |
| **FB-010** | 端口路由转发（无疑问） | — | — 已交付于 M1-7 |
| **FB-009** | 转换模板管理（无疑问） | — | — 已交付于 M1-5 |
| **FB-008** | 字段加工规则配置：添加规则无效 | Bug | ✅ UX-C 回滚复发修复 · CHG-018 |
| **FB-007** | 渠道模板管理（无疑问） | — | — 已交付于 M1-4 |
| **FB-006** | 字段映射配置：源字段"解析报文/手工设置"没展示解析报文，后续无法配置 | Bug | ✅ UX-C · CHG-018 |
| **FB-005** | 报文格式转换菜单排序下移（不应是转换模块的第一个） | IA 调整 | ✅ UX-B · CHG-017 |
| **FB-004** | 系统概览"缓存命中率"卡片对齐问题 | 视觉调整 | ✅ UX-A · CHG-016 |
| **FB-003** | 导航栏 + 系统概览滑块美观 | 视觉调整 | ✅ UX-A · CHG-016 |
| **FB-002** | 右上角个人信息跳转到用户权限管理 | 新需求 | ✅ UX-A 个人信息下拉 |
| **FB-001** | 前端整体视觉圆角毛玻璃风格 | 视觉重塑 | ✅ UX-A 全站视觉基座 · CHG-016 |

---

## 五、维护约定

- **一 FB 一次响应**：一条反馈从接手到闭环内**不换编号**；即使拆分为多个 CHG，都关联到同一个 FB-XXX
- **拒绝要写清楚原因**：`🚫 已拒绝` 状态必须在条目下方补一段 `**拒绝原因**：xxx`
- **延后要指定版本**：`💤 已延后` 必须在状态列写清楚 `→ vX.Y.Z`
- **每次发新版本前**：把状态在 ✅ / 🚫 / 💤 的活跃反馈整理归档到「历史归档」的新批次段落
- **合并已完成任务**：若 FB-XXX 关联的任务计划文件也已完成，可选择性删除 `docs/03-开发/任务计划/YYYY-MM-DD-FB-XXX.md`（保留 CHG 记录即可追溯）

---

## 六、相关文档

- [路线图](./路线图.md) · [待办与缺陷池](./待办与缺陷池.md) · [v0.1.0 基线](./v0.1.0-基线.md)
- [变更记录 CHG-XXX](../03-开发/变更记录.md)
- [任务计划目录](../03-开发/任务计划/)
