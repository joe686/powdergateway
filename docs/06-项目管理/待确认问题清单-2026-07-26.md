# 待确认问题清单 · 2026-07-26

> **用途**：本次会话中我根据你的指令产出手工测试文档时暴露出需要**你拍板**的开放问题。等你回来挨条勾选或补答即可。
> **不涉及编码，只涉及"要不要 / 什么时候 / 走哪条路"**。
> **文档周期**：本次决策完成后归档到 `docs/06-项目管理/archived/`，别塞进反馈簿。

## 用户回复进度（2026-07-26 21:xx）

| Q | 主题 | 状态 | 用户选择 |
|---|------|:---:|----------|
| **Q1** | v0.2.0 tag 该不该打 | ✅ 已答 | **B** — 先打 v0.1.1 |
| **Q2** | v0.1.1 装箱清单 | ✅ 已答（隐含） | 4 commits + 本轮 3 份文档 一并入 v0.1.1 |
| Q3 | 旧 `测试案例.md` 与新 `v0.1.0-手工测试指南.md` 共存策略 | ⏸ 待答 | — |
| Q4 | 手工测试是否启 pg-testkit | ⏸ 待答 | — |
| Q5 | 测试用户账号谁建 | ⏸ 待答 | — |
| Q6 | 反馈簿新条目起草约定 | ⏸ 待答 | — |
| Q7 | v0.5.0 CR-002 设计复审 | ⏸ 待答 | — |
| **Q8** | v0.2.0 backlog 落地顺序 | ✅ 已答 | **A** — FN-12 → 联动 FN-09 → pg-testkit → SQLite（已排入[待办 § v0.2.0](./待办与缺陷池.md#v020) ① ~ ⑨） |
| Q9 | 打 tag 前测试门槛 | ⏸ 待答（本次 patch tag 只跑了 mvn test + npm build，未做手工 P0） | — |
| Q10 | Windows 启动脚本 | ⏸ 待答 | — |

**下一步**：v0.1.1 tag 落地后，剩余 7 条 Q 保留待答。回来时可在本文件挑选其中若干继续拍板。

---

## 已产出物件（先确认路径没错）

| 产出 | 路径 | 状态 |
|------|------|:---:|
| DB / 端口 / 账号 连接速查 | [`docs/04-测试/连接配置速查.md`](../04-测试/连接配置速查.md) | ✅ 已写 |
| v0.1.0 手工测试指南（新增 ~85 条用例，覆盖阶段五/六/七 + FB 回归） | [`docs/04-测试/v0.1.0-手工测试指南.md`](../04-测试/v0.1.0-手工测试指南.md) | ✅ 已写 |
| 本清单 | 你正在读 | ✅ 已写 |

**关键说明**：**没有** bump 版本到 v0.2.0。原因见 Q1。

---

## Q1 · v0.2.0 tag 该不该打？【最重要】

**背景**：你说"你写好了测试的案例，就把版本做到v0.2.0"。但按 [路线图](./路线图.md#v020--test-1-完善--sqlite-分支--fn-12-字典映射预估-2--3-周) 与 [待办与缺陷池 § v0.2.0](./待办与缺陷池.md#v020) 现状：

| v0.2.0 范围条目 | 状态 |
|---|:---:|
| pg-testkit Faker 10 万条数据 | ❌ 未开工 |
| pg-testkit 完整 10 表 DDL（当前只是骨架） | ❌ 未开工 |
| pg-testkit Mock 规则持久化到 `mock_rule` 表 | ❌ 未开工 |
| REL-1 v1.1：Maven profile `db-sqlite` 打通 | ❌ 未开工 |
| REL-1 v1.1：SQLite 便携版完整回归 | ❌ 未开工 |
| **FN-12 字典映射** 表 + CRUD + Redis 缓存（CR-001） | ❌ 未开工 |
| **FN-12 × M1-3** DictMappingProcessor 集成 | ❌ 未开工 |
| **FN-12 前端** `DictMappingList.vue` + 三处向导嵌入 | ❌ 未开工 |
| **FN-12 × FN-09 联动** Excel 多 sheet 下载（CR-001 § 扩展 A · FB-040） | ❌ 未开工 |

**11 项 backlog 全部 💤 未开工，工时预估 2~3 周**。此时打 `v0.2.0` 会是"空版本"，与语义化版本（MINOR = 新功能交付）冲突。

**候选方案**：

| 方案 | 说明 | 推荐 |
|---|---|:---:|
| **A** | 保持不 tag，等 v0.2.0 backlog 完整实现后再打 `v0.2.0` | ⭐ |
| **B** | 立刻打 `v0.1.1`（FB-037 + FB-038 已交付 + 本次测试文档），v0.2.0 按原节奏演进 | ⭐⭐ |
| **C** | 强打 `v0.2.0`（不推荐，破坏 semver 与路线图承诺） | 🚫 |

**你的选择**：☐ A  ☐ B  ☐ C  ☐ 其他：____________

> 我倾向 **B**（先出 v0.1.1 patch，回收阶段性成果）—— 只要你点头我可以立刻按 v0.1.1 checklist 打 tag：过 `mvn test` / `npm run build` / 归档 FB / 更新路线图 / `git tag -a v0.1.1`。

---

## Q2 · 打 v0.1.1 tag 的话，把哪些 commit 装进去？

若 Q1 选 B，v0.1.1 装箱清单（按最近提交）：

| commit | 内容 | 来源 |
|---|---|---|
| `6f557e2` | fix(SYS-3) FB-037 401 拦截完整清 store | 反馈簿 FB-037 |
| `b0db29d` | style FB-038 登录页毛玻璃 | 反馈簿 FB-038 |
| `83978e3` | docs FB-037/038 状态回写 | 归档动作 |
| `1f818b4` | docs FB-040 + FB-041 · CR-001 § 扩展 A + CR-002 立项 | 规划动作 |
| **本轮 3 份新文档** | 连接速查 + 手工测试指南 + 本清单 | docs 增补 |

**你要 confirm**：
- ☐ 4 个已有 commit + 本轮文档合起来打一个 v0.1.1
- ☐ 只把 FB-037 / FB-038（bug 修复）算 v0.1.1，docs 独立发（不推荐，增加 tag 数）
- ☐ 其他：____________

---

## Q3 · 旧 `测试案例.md` 与新 `v0.1.0-手工测试指南.md` 怎么共存？

**现状**：
- `docs/04-测试/测试案例.md` — v1.0（2026-05-06），106 条通用用例，写在阶段 5/6/7 交付**之前**。用词偏"进入 XXX 页面"，无 DB 校验 SQL，未覆盖 SYS-5 / AUX-1/2 / UX-A~F / FN-09/11 / REG-1 / FB 回归。
- `docs/04-测试/v0.1.0-手工测试指南.md` — 本次新写，~85 条，可操作性更强（明确的菜单路径、DB 校验 SQL、优先级），覆盖 v0.1.0 全阶段。

**候选**：

| 方案 | 说明 |
|---|---|
| **A** | 两份都留（现状）：旧的作为"通用测试思路参考"，新的作为"v0.1.0 可执行脚本"，README 加区分说明 |
| **B** | 合并：把旧的 106 条中未过时的用例并入新指南，旧文件标记 deprecated 迁移到 `docs/04-测试/archive/` |
| **C** | 只保留新的：把旧的直接归档，README 更新 |

**你的选择**：☐ A  ☐ B  ☐ C

> 我倾向 **B**，一份文档一个真源，减少你重复查找。但需要 1~2 小时合并工作（我可以下次会话做）。

---

## Q4 · 手工测试期间要不要启 pg-testkit（8081/9999）？

**背景**：
- pg-testkit **骨架**已在 v0.1.0 交付：`MockServerService` / `TestApiController` / `PgAssertions` 都在，端口 8081 可跑
- **完整能力**（10 表 DDL + Faker 10 万条数据 + Mock 规则持久化）归入 v0.2.0
- 你手工点 v0.1.0 时，**唯一强依赖 pg-testkit 的**是"端口分发全链路"用例（MT-05-* 需要 9999 端口的 Mock B 系统模拟外部接口）

**候选**：

| 方案 | 说明 |
|---|---|
| **A** | 手工测试默认**不启** pg-testkit，跳过 MT-05-*（端口分发全链路），标记为"待 v0.2.0 完整化后再跑" |
| **B** | 手工测试**跑一遍 MT-05-***：启 pg-testkit + Postman 手工配一个 Mock，走完 dispatch 链路 |
| **C** | 你自己拿其他 HTTP Mock 工具（如 `mockoon` / `httpbin`）替代 9999 端口，独立于 pg-testkit |

**你的选择**：☐ A  ☐ B  ☐ C

> 我倾向 **B**：MT-05-* 只 3 条用例，值得跑一次以覆盖 M1-7 端口分发；操作步骤在指南 § 五 已写清楚。

---

## Q5 · 测试用户账号谁负责建、密码用什么？

**手工测试指南 MT-02-04 要用**：`testuser1 / Test@123`（role=`user`）、`testreader / Test@123`（role=`readonly`）

**候选**：

| 方案 | 说明 |
|---|---|
| **A** | 你手动在管理台建（按指南 MT-10-04 的步骤走一次即可，也算测试用户 CRUD） |
| **B** | 我下次会话补一段 SQL / init 脚本预置这两个账号 |
| **C** | 干脆用 admin 一路测到底，忽略权限相关用例 |

**你的选择**：☐ A  ☐ B  ☐ C

---

## Q6 · 反馈簿新条目 → 你还是我起草？

**现状**：你手工测试跑完，发现问题会追加 FB-042 起。规则里已定义"你只写最简三行（标题 + 时间 + 反馈原文），Claude 补齐分类/优先级/目标版本/状态"。

**要 confirm 一下用法**：
- ☐ 保持现状（你写最简三行，我下次会话按 `pg-feedback-flow` skill 处理）
- ☐ 加一条：**你追加 FB 时，如果标题里带 `[urgent]`，我在下次会话开始第一件事就处理**
- ☐ 其他约定：____________

---

## Q7 · v0.5.0（CR-002 业务菜单生成 + SYS-3 升级）设计文档待你复审

**背景**：FB-041（2026-07-26 你提的战略功能）已完成 Step 3 设计文档：
- [`docs/02-设计/详细设计/2026-07-26-FN-BIZ-business-menu-generation-design.md`](../02-设计/详细设计/2026-07-26-FN-BIZ-business-menu-generation-design.md)
- [`docs/02-设计/详细设计/2026-07-26-SYS-3-upgrade-design.md`](../02-设计/详细设计/2026-07-26-SYS-3-upgrade-design.md)

**要 confirm**：
- ☐ 你休息后一并复审这两份设计（然后进 `writing-plans` 排 v0.5.0 实施）
- ☐ v0.5.0 是远期战略，可以先搁置，聚焦 v0.2.0 backlog（字典 + testkit + SQLite）
- ☐ 其他优先级安排：____________

---

## Q8 · v0.2.0 backlog 落地顺序你想怎么排？

**11 项 backlog 依赖关系**（我梳理的建议）：

```
[FN-12 后端表 + CRUD]  →  [DictMappingProcessor 集成 M1-3]  →  [前端管理页]  →  [三处向导嵌入]
                                                                                      ↓
[FN-09 × FN-12 Excel 联动]  ←────────────────────────────────  三处向导嵌入完成

[pg-testkit Faker 数据] || [完整 10 表 DDL]  →  [Mock 规则持久化]
[Maven profile db-sqlite]  →  [SQLite 便携版回归]
```

**候选串行顺序**：

| 顺序 | 说明 | 工时预估 |
|---|---|---|
| **A** | FN-12 → 联动 FN-09 → pg-testkit 补齐 → SQLite | 2 周 |
| **B** | pg-testkit 补齐 → SQLite → FN-12 → 联动 | 2.5 周 |
| **C** | 并行两线：主线跑 FN-12（业务价值高），空档做 pg-testkit / SQLite | 1.8 周（若不切换成本高） |

**你的选择**：☐ A  ☐ B  ☐ C  ☐ 由 Claude 定

> 我倾向 **A**：FN-12 是 CR-001 用户主动提的需求（FB-039/040），业务优先级最高；SQLite 属"部署便利性"，可延后。

---

## Q9 · 打 tag 前是否强制完整跑 P0+P1 用例（约 40 条）？

**候选**：

| 方案 | 说明 |
|---|---|
| **A** | **强制**：任何 tag（含 patch）前，必须跑完 P0 + P1 用例并留下执行记录 md（`docs/04-测试/execution/`） |
| **B** | 分档：patch tag 只跑 P0（10 条），MINOR tag 才跑 P0+P1 |
| **C** | 不强制，你按需决定 |

**你的选择**：☐ A  ☐ B  ☐ C

> 我倾向 **B**：单人开发节奏灵活，patch tag 门槛过高会拖累修 bug 速度。

---

## Q10 · Windows 环境下有没有 MySQL/Redis 一键启动脚本？

**背景**：手工测试前需 MySQL / Redis / 后端 / 前端 4 个服务都起来。我猜你日常大概率用某种一键脚本或已跑成服务。要不要我下次会话补一份 `scripts/dev-up.ps1`?

- ☐ 补一份（推荐）
- ☐ 已有，我告诉你路径：____________
- ☐ 不需要，日常都是手动起

---

## 你的整体回复模板（可复制回来直接填）

```
Q1 (v0.2.0 tag)：
Q2 (v0.1.1 装箱)：
Q3 (测试案例合并)：
Q4 (pg-testkit)：
Q5 (测试用户)：
Q6 (反馈簿约定)：
Q7 (v0.5.0 设计复审)：
Q8 (v0.2.0 backlog 顺序)：
Q9 (tag 前测试门槛)：
Q10 (启动脚本)：

# 补充其他反馈：
```

---

## 归档

回复完毕后：
- ✅ / 🚫 逐条勾选 → 我把决策同步到 [路线图](./路线图.md) / [反馈簿](./反馈簿.md) / CLAUDE.md
- 本文件迁移到 `docs/06-项目管理/archived/2026-07-26-待确认问题清单.md`
