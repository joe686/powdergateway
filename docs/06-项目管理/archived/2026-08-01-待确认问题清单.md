# 待确认问题清单 · 2026-08-01(已归档 · 2026-08-02 全部闭环)

> **状态**：✅ 全部 24 题拍板闭环 · 2026-08-02
> **触发**：用户 2026-08-01 会话："起码规划到 v0.3.2 ...你要把能做的事情都做完,我批量回复,有问题我会跟你再沟通细节"。
> **用途**：把 v0.3.0 → v0.3.2 三个 minor(含空档 v0.3.1)规划过程中需要用户拍板的开放决策集中列出。用户 2026-08-02 逐题回复(未走模板 · 内容直接批注在各题下方)。

## 拍板结果总览(2026-08-02 用户回复)

| Q | 主题 | 结果 |
|---|---|---|
| Q1 | 三 minor 序列 | **B** · v0.3.1 中间收官 · **+补充**:CR-007 加 PG- 前缀 + FN-12 字典转换双层 |
| Q2 | v0.2.5 tag 时机 | **A** · 手工过 MT-19-* 后 push · **+补充**:1.0 前跳包不对外说明 |
| Q3 | v0.2.7 归属 | **B** · 并入 v0.3.1 |
| Q4 | MVP 预览 tag | **A** · v0.3.2-preview |
| Q5 | SOCK-1 分帧默认 | **C** · 三分帧全支持(用户实证"8 位报文长度 + 编码选项") |
| Q6 | GBK 编码时机 | **B** · v0.3.0 双编码全做完 |
| Q7 | 短/长连接 | **A** · 短连接 · 不做连接池 |
| Q8 | Eureka 自注册时机 | **A** · 延到 v0.3.2 · 用户希望"尽快见到,不能太晚" |
| Q9 | Netty 依赖 | **A** · netty-all · Spring 2.7 漏扫无法升级考虑 |
| Q10 | SOCK-3 归属 | **A** · v0.3.0 |
| Q11 | SOCK-4 兜底 | **A** · 纯 Netty |
| Q12 | host demo 模板 | 用户注"不预置 demo · 手工测试模拟生产 · 后期做复制接口远期功能" · 视为 **B** |
| Q13 | CR-007 前置 | **A** · 前置到 v0.3.1 |
| Q14 | CR-003 前置 | **A** · **+补充**:显示"版本号+作者光斓+中文日期+当前仅为测试版本"注 · 存 H2 表 · 不起眼位置 |
| Q15 | v0.2.x patch | **A** · 归 v0.3.1 |
| Q16 | 规约 5 项 | **A**(全部不采纳 · 用户"无必要勿增流程") |
| Q17 | JSON 骨架 UI | **A** · 4 JSONPath 最小 UI |
| Q18 | bank 验收 | **A** · 功能完善 |
| Q19 | CR-007 存储 | **C** · 先做 A · v1.1+ 拆独立表(见 CR-009 备查) |
| Q20 | SOCK-5-A 长连接 | **C** · 短连接 + 预留字段 |
| Q21 | FB-053 归属 | **A** · 并入 SOCK-5-B |
| Q22 | 手工用例时机 | **A** · TDD 同步 |
| Q23 | trace_id 前置 | **A** · v0.3.1 三表补齐 |
| Q24 | 老库迁移策略 | 用户注"现在还没有实际使用的客户" · 默认走 A(现有迁移脚本策略,不追加 README 流程) |
| M1-M10 | 次要决策 | **全部同意默认执行** |

## 同步动作(2026-08-02 Claude 落地)

- ✅ 路线图加 v0.3.1 章节 · 调 v0.3.7/v0.5.0 · 加"1.0 前跳包不对外说明"规约
- ✅ 待办池 CR-003 归 v0.3.1 · CR-007 归 v0.3.1 加双层机制 · 加 CR-009(独立路由表升级备查 v1.1+) · v0.2.7 段并入 v0.3.1 · v0.3.7 移除 CR-003
- ✅ v0.3.0 任务计划更新:三分帧 + 双编码 + 短连接+预留 + Eureka 延 + netty-all + SOCK-3 归 + Netty Mock + host 指南 + MT 同步
- ✅ v0.3.1 任务计划改写:CR-007 双层机制 + CR-003 扩展形态 + FB-046/054 + v0.2.7 分类型 4 页 + trace_id 三表 + 删规约调整
- ✅ v0.3.2 任务计划更新:复用 v0.3.1 CR-007 + Eureka selfRegister 补 + 短连接预留 + JSON 骨架最小 UI + FB-053 并入 SOCK-5-B + preview tag
- ✅ 3 处顶层索引更新

**原始 24 题内容 + 用户回复原话保留在下方各题**,便于将来追溯用户思路。

---

# 原始问题清单(附用户 2026-08-02 逐题回复)

---

## 快速索引

| 组 | 题号 | 主题 |
|---|---|---|
| 版本骨架 | Q1 | 三 minor 序列拍板(v0.3.0 / v0.3.1 / v0.3.2) |
| 版本骨架 | Q2 | v0.2.5 tag 何时打 · 阻不阻塞 v0.3.0 |
| 版本骨架 | Q3 | v0.2.7 分类型 4 页补齐 · 独立打还是并入 v0.3.1 |
| 版本骨架 | Q4 | v0.3.0 完成后是否打"MVP 试点预览 tag" |
| SOCK-1 出站 | Q5 | 分帧策略默认改为 XML 自闭合 · v0.3.0 就固化? |
| SOCK-1 出站 | Q6 | 字符编码 UTF-8 必做 · GBK v0.3.0 就做 or 延后? |
| SOCK-1 出站 | Q7 | 短连接为默认 · 是否预留长连接接口? |
| SOCK-1 出站 | Q8 | Eureka 自注册补齐时机 · v0.3.0 内 or v0.3.2 内? |
| SOCK-1 出站 | Q9 | Netty 依赖引入方式 |
| SOCK-3/4 | Q10 | SOCK-3 MessageDebug 扁平化预览归哪个版本 |
| SOCK-3/4 | Q11 | SOCK-4 pg-testkit 兜底 · Python 脚本 or 直接 Netty Mock |
| v0.3.0 增值 | Q12 | 是否随 v0.3.0 出 "lcpt-host demo 接口模板" 便于销售演示 |
| v0.3.1 收官 | Q13 | v0.3.1 装 CR-007 HTTP 侧路由前置? |
| v0.3.1 收官 | Q14 | v0.3.1 装 CR-003 版本显示前置? |
| v0.3.1 收官 | Q15 | v0.3.1 装 v0.2.x 攒的 patch(FB-046 + FB-054 wizard edit) |
| v0.3.1 收官 | Q16 | v0.3.1 装复盘 5 项规约调整 · 全采纳 or 部分? |
| SOCK-5 入站 | Q17 | SOCK-5-D JSON 骨架配置 UI 复杂度 |
| SOCK-5 入站 | Q18 | lcpt-bank 报文验收标准(样本用户已注明"可能不同一笔") |
| SOCK-5 入站 | Q19 | CR-007 功能号路由存储 · 复用 interface_config or 独立表 |
| SOCK-5 入站 | Q20 | SOCK-5-A 入站长连接支持范围 |
| SOCK-5 入站 | Q21 | FB-053 pg-testkit 多应用名 mock · 并入 SOCK-5-B or 独立 SOCK-4-B |
| 横跨 | Q22 | SOCK 场景手工用例 MT-XX-* 写作时机 |
| 横跨 | Q23 | trace_id 全串是否提前到 v0.3.0/v0.3.2(现归 v0.5.0) |
| 横跨 | Q24 | 老库客户对接方式(v0.2.5 scope 迁移已给脚本 · 是否统一策略) |

---

## 组 A · 版本骨架(4 题)

### Q1 · v0.3.0 → v0.3.2 三个 minor 装什么(核心决策)

**背景**：路线图现有版本序列 = v0.2.5 → v0.2.7 → v0.3.0 → v0.3.2 → v0.3.7 → v0.4.0 → v0.5.0 → v1.0。**v0.3.1 是空档**,现路线图没有 · 你说"起码规划到 v0.3.2",需要拍板 v0.3.1 是否设立以及装什么。

**候选方案**（三选一 · 影响后续所有细节题）:

| 方案 | v0.3.0 | v0.3.1 | v0.3.2 | 特点 | Claude 推荐 |
|---|---|---|---|---|:---:|
| **A · 保守** · 按现路线图不动 | SOCK-1~4 出站 | **不设** | SOCK-5-A/B/C/D + CR-007 | v0.2.7 独立打 · v0.2.x 攒 bug 独立打 · 严格贴合当前拍板 | |
| **B · 中间收官版**（推荐） | SOCK-1~4 出站 | v0.2.x 攒的 patch + CR-003 版本显示 + CR-007 HTTP 侧路由 + 复盘 5 项规约落地 + v0.2.7 分类型 4 页补齐 | SOCK-5-A/B/C/D 入站(不再含 CR-007 · 已提前) | 有喘息节奏 · CR-007 前置渠道方可 HTTP 直调 functionId · v0.3.2 更聚焦入站 Socket | ⭐⭐ |
| **C · 激进** · CR-007 提前到 v0.3.0 | SOCK-1~4 出站 + CR-007 HTTP 侧路由 | v0.2.x 攒的 patch + CR-003 + v0.2.7 + 规约 | SOCK-5-A/B/C/D 入站(不含 CR-007) | v0.3.0 交付面最大 · 但周期从 2.5-3 周 → 3-4 周 · 拉长交付 | |

**你的选择**：☐ A ☐ B ☐ C ☐ 其他:______选B，对于这个功能号的字段，我建议加一个sys这样的字段以区分是不是我们系统自己定义的一个我们的功能字段，渠道必须送这个字段，我担心会有他们渠道自己的功能号，所以要做到足够的区分。或者可以把对方的功能号字段转换成我们的，正好还有一个字典的转换功能，这块你自己规划一下，如果对内容有疑问再向我确认。我主要是担心后面要改，实际的场景就是 渠道用FunctionId这个字段发到lcpt-bank，然后我们把这个FunctionId也一并转发到联机程序里面去了，让联机去做业务处理。______

> **推荐 B 理由**:① v0.3.0 SOCK-1~4 保持"一 minor 一原子场景"节奏,不膨胀;② v0.3.1 承接 v0.2.x 攒的 bug 修复通道,不让 patch 无限攒;③ CR-007 HTTP 路由 + CR-003 版本显示 都是**独立小功能可 3-5 天做完**,并入 v0.3.1 一起打;④ 复盘 5 项规约要固化落到 CLAUDE.md / skill,借 v0.3.1 一次到位;⑤ v0.3.2 得到 CR-007 前置 = 入站 Socket 直接复用现成路由表,可少 3-5 天工。
>
> 

---

### Q2 · v0.2.5 tag 何时正式打(编码完成 · 待用户测试)

**背景**：v0.2.5 minor(CR-004 FN-12 v2 + CR-008 接口调用体验 + CR-006 TEST-1 v0.1 补齐)2026-07-29 编码完成 · CHG-034/035/036 归档 · 后端 598 用例全绿 + 前端 build 通过 · **待你手工测试并 push tag**。按 memory `feedback_patch_channel_test_driven` = 本地打 tag 可以,push 必须问。

**候选**:

| 方案 | 说明 | Claude 推荐 |
|---|---|:---:|
| **A** | 你先手工过 MT-19-* 12 条(字典管理),没问题后我在下次会话打 tag + push | ⭐ |
| **B** | 我先本地打 tag(不 push),你有空时测,测完你 push | |
| **C** | 直接不 tag v0.2.5 · v0.2.7 + v0.3.0 都做完打一个"合并 minor"tag | 🚫 破坏 semver |
| **D** | 你信任现有测试,直接 push v0.2.5 tag | |

**你的选择**：☐ A ☐ B ☐ C ☐ D ☐ 其他:_________选A，我们在1.0正式版本前主要关注功能迭代，打包我们跳过就行，后续1.0后面的版本要对外发布了，如果跳包就要对外正式说明了。明白了吗___

____

> **推荐 A**:v0.2.5 涉及数据表 scope 拆分 + 缓存 key 变更,手工过一遍 12 条用例最保险;不阻塞 v0.3.0 开工(SOCK-1 后端跟 FN-12 前端解耦)。

---

### Q3 · v0.2.7 分类型 4 页补齐 · 独立打还是并入 v0.3.1

**背景**：v0.2.7 = FB-057 B 方案(保留 QueryConfig/InsertConfig/UpdateConfig/DeleteConfig 四页作"高级用户直达路径")+ 补齐字段加工/分片/字典 + FB-054 edit 模式回填。预估 8-10 人日 ≈ 2 周。

**候选**:

| 方案 | 说明 | 影响 |
|---|---|---|
| **A** | v0.2.7 独立打(在 v0.3.0 之前) | 排期:v0.2.5 → v0.2.7(2 周) → v0.3.0(2.5-3 周) → v0.3.2 |
| **B**（推荐,仅 Q1=B/C 时可选）| 并入 v0.3.1(v0.3.0 之后) | 排期:v0.2.5 → v0.3.0 → v0.3.1(含 v0.2.7)→ v0.3.2 · v0.3.0 提前开工 |
| **C** | 延后到 v0.3.7 · v0.3.0 直接开工 | 用户看到"编辑分类型接口不回填"的时间变长 |
| **D** | 拆:FB-054 edit 归 v0.2.x patch(纯 bug)· FB-057 补能力归 v0.3.1 | 遵循 memory `feedback_patch_vs_minor` |

**你的选择**：☐ A ☐ B ☐ C ☐ D ☐ 其他:_________B___

> **推荐 B(前提是 Q1=B)**:不再单独打 v0.2.7 tag,减少发版噪声;v0.3.1 收官版天然是"补齐+规约+小功能"的场子,和 v0.2.7 语义一致。

---

### Q4 · v0.3.0 完成后是否打"MVP 试点预览 tag"

**背景**：v0.2.x 阶段性复盘 § 4.2 建议"v0.3.2 后打一个 MVP 试点预览 tag(非 v1.0,只标注'金融试点可用'),让销售/试点客户尽早接触"。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| v0.3.2 完成后打 `v0.3.2-preview` 标签 + 产品说明书标注"银行 Socket 场景试点可用",非正式 v1.0 |
| **B** | 不打预览 tag,等 v1.0 · 严格按节奏 B 4-5 月推进 |
| **C** | v0.3.0 完成即打预览 tag(仅出站可用) |

**你的选择**：☐ A ☐ B ☐ C ☐ 其他:____________选A，

> **推荐 A**:v0.3.2 完成 = 出/入站 Socket 全通,能真做"客户已有 lcpt-host 替代"这类演示;不打预览 tag 相当于藏着能力等 4-5 月,机会成本高。

---

## 组 B · SOCK-1 出站细节(5 题)

### Q5 · SOCK-1 分帧策略默认

**背景**：CR-005 原定"4 字节大端定长头"为默认 · 2026-07-28 bank 报文 verify 后复盘发现 bank/host 都用 XML 自闭合边界 · 复盘建议"默认改 XML 自闭合,4 字节定长头改可选支持项"。**该修订未走 CHG,需要你确认。**

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| 默认 `xml_boundary`(XML 自闭合结束)· `length_prefix_be4` 保留作可选项 · 接口配置向导下拉可切换 |
| **B** | 默认 `length_prefix_be4` · `xml_boundary` 作可选 · 保持 CR-005 原设计 |
| **C** | 两个都做完整支持 · 无默认,配置必填 |

**你的选择**：☐ A ☐ B ☐ C  选C，我看实际去发的xml，报文前有8位报文长度，还有编码格式的选项

> **推荐 A**:host + bank 两个实际用例都是 XML 自闭合 · 定长头是"其他系统偶尔用"的样本 · 让常用路径最短。

---

### Q6 · 字符编码 · GBK v0.3.0 就做 or 延后

**背景**：CR-005 定"UTF-8 + GBK 双编码"· host 报文 `<?xml version="1.0" encoding="UTF-8"?>` · bank 报文同样 UTF-8 · GBK 是"经验判断银行核心系统常用"没有实证用例。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| v0.3.0 只做 UTF-8 · GBK 可选延到 v0.3.2(bank 场景实测发现需要再加) | 节省 1-2 天 · 有实证再加更靠谱 |
| **B** | v0.3.0 双编码全做完(CR-005 原范围) | 保险 · 但 GBK 编解码不能实测无法验证正确性 |
| **C** | 延到 v0.3.7 · 全部编码工作合并 | 太远 |

**你的选择**：☐ A ☐ B ☐ C，要选B，实际对不同系统调试时碰到过不同的对接系统需要的编码格式不一样。所以这个还得有个选择的地方。

> **推荐 A**:YAGNI 原则 · CharsetConverter(M1-7 已有字节级转码)可复用,后续加 GBK 只需扩展常量表。

---

### Q7 · 短连接 vs 长连接 · v0.3.0 是否预留长连接接口

**背景**：CR-005 定"短连接先做,长连接留 v0.4.0+"· bank 场景 SOCK-5-A 入站涉及"同步应答通道追踪原 TCP 连接回写",本质是"一交易一连接的短连接语义"· host 场景发起方也是每次新建连接。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| v0.3.0 只做短连接 · SocketClient 内部预留 connectionMode 字段作接口 API 兼容点(不实装长连接分支)· v0.4.0 加长连接池 | 简单 |
| **B** | v0.3.0 直接做完短 + 长(带连接池) · 增加 3-5 天 | 过早优化 |
| **C** | 完全不预留接口 · v0.4.0 需要时重构 API | 兼容性风险 |

**你的选择**：☐ A ☐ B ☐ C，选A，不用连接池，如果需要后续再做。这个连接池可能有点浪费资源。

> **推荐 A**:接口配置 JSON 里加个 `connectionMode: short|long|pooled` 字段,默认 short · 后续加实现不改契约。

---

### Q8 · Eureka 自注册补齐时机

**背景**：CR-005 定"v0.3.0 SOCK-1 内一并补齐 Eureka 自注册"· 但 SOCK-1 出站本身**不依赖** PG 被 Eureka 发现(PG 主动发起 TCP 出站到 host)· 真正需要 PG 自注册是 SOCK-5-B(bank 场景联机通过服务名调 PG)。你 2026-07-28 说"提供 Eureka 实例做联调"。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| v0.3.0 SOCK-1 只 verify Eureka `discover` 可用 · 自注册 `selfRegister` 补齐延到 v0.3.2 SOCK-5-B(真需要时)· v0.3.0 节省 2 天 | 分离关注点 |
| **B** | v0.3.0 SOCK-1 就补齐 selfRegister · 但可能没有实际消费方来验证是否可发现 | 会写死代码但没端到端验证 |
| **C** | 拆到 v0.3.1 单独做一个"Eureka 联调"任务(独立于 SOCK) | 增加协调成本 |

**你的选择**：☐ A ☐ B ☐ C，我觉得选A是可以的，这个注册eureka的注册发现机制跟tcp的xml报文没有太多关系，所以不在一个版本也行，可以先把转接口本体做了之后马上来补这个注册，也不能太晚的版本。我希望尽快见到。

> **推荐 A**:自注册代码本身需要 ApplicationInfoManager + DiscoveryClient 手动装配(见 `EurekaRegistryClient.java` L21-24 注释)· 不与端到端联调绑定容易写完没人验 · 延到 SOCK-5-B 时"边写边联调"更可靠。

---

### Q9 · Netty 依赖引入方式

**背景**：SOCK-1 + SOCK-5-A + SOCK-4 pg-testkit 都需要 Netty · 项目 pom 当前无 Netty 依赖(Spring Boot 2.7 内置 Tomcat servlet,不含 Netty)。

**候选**:

| 方案 | 说明 | 打包体积 |
|---|---|---|
| **A**（推荐）| 直接引入 `io.netty:netty-all:4.1.x` | +3MB · 显式受控 |
| **B** | 引入 `spring-boot-starter-webflux`(自带 Netty · 顺便可用响应式) | +8MB · 引入额外范式,项目暂无 WebFlux 用途 |
| **C** | 只引入需要的子模块 `netty-handler` + `netty-codec` + `netty-transport` | 精细 · 每次加功能要补依赖 |

**你的选择**：☐ A ☐ B ☐ C，选A吧，如果后续遇到了漏洞扫描，spring2.7已经没有办法升级修复。

> **推荐 A**:netty-all 是 Netty 官方推荐的"打包全模块"依赖,3MB 可接受,同一版本管理。

---

## 组 C · SOCK-3/4 归属(2 题)

### Q10 · SOCK-3 MessageDebug 扁平化预览 tab 归哪个版本

**背景**：SOCK-3 = 在报文调试工具追加"XML → 扁平 JSON"预览 tab · 现有 MessageDebug 已支持 JSON↔XML 双向 · 该功能与 SOCK-1 后端不强耦合(独立前端功能)。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| 归 v0.3.0 · 与 SOCK-1 同批 · 让用户 v0.3.0 就有完整"配 SOCK 接口时可先在调试台看扁平化效果" | 完整体验 |
| **B** | 延到 v0.3.2 · 与 SOCK-5-D JSON 骨架 UI 同期做 | 拉长 v0.3.2 |
| **C** | 拆到 v0.3.1 · 前端小改独立发 | 无 |

**你的选择**：☐ A ☐ B ☐ C 选A

> **推荐 A**:1 天工作量 · 无理由拆开。

---

### Q11 · SOCK-4 pg-testkit TCP Mock 兜底方案

**背景**：CR-005 定"pg-testkit Netty ServerSocket + YAML 配 functionId→XML 模板文件 · 兜底 Python socketserver 20 行脚本"。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| **只做 Netty Mock** · 不备 Python 兜底 · 节省 0.5 天 · 保证 pg-testkit 主线一致性 | 依赖 Netty 引入(Q9 相关) |
| **B** | 先做 Python 兜底再做 Netty · 保底可用 | 双写维护成本 |
| **C** | 只做 Python 脚本 · v0.4.0 再补 Netty · v0.3.0 最快 | pg-testkit 走 Python 破坏"pg-testkit 是 Java Spring Boot 工具"的定位 |

**你的选择**：☐ A ☐ B ☐ C  选A，

> **推荐 A**:pg-testkit 是长期演进工具,Python 兜底会成为长期技术债 · Netty 依赖已在 Q9 决策,不额外增加依赖。

---

## 组 D · v0.3.0 增值项(1 题)

### Q12 · 是否随 v0.3.0 出 "lcpt-host demo 接口模板"

**背景**：v0.3.0 完成后,若你需要向客户演示"PG 替代 lcpt-host",还需要手工在 PG 里配一个 FunctionId=181345 接口。若 v0.3.0 内附一个"内置 demo 接口配置",直接导入 SQL 就有完整可运行 demo,加速销售场景。

**候选**:

| 方案 | 说明 |
|---|---|
| **A** | 附内置 demo:`db/demo/host-181345-demo.sql` · 一键 import 就能跑 · +0.5 天 | 便于销售 |
| **B**（推荐）| 提供"部署后手动配置指南"文档 · 不预置 SQL · 保持 demo 数据与生产数据分离清晰 | 灵活 · 无侵入 |
| **C** | 不做 · 你自己按需配 | 演示时需手工现配 |

**你的选择**：☐ A ☐ B ☐ C，不预值demo，我手工测试，完全模拟生产操作。然后后面可能会做一个复制某个接口的功能，但是也是远期功能。

> **推荐 B**:预置 demo 数据涉及"卸载/升级时怎么清"的复杂度 · 一份 markdown 指南(配合 host 报文样本)已足够。

---

## 组 E · v0.3.1 收官版内容(4 题 · **仅 Q1 选 B/C 时相关**)

### Q13 · CR-007 HTTP 侧功能号路由是否前置到 v0.3.1

**背景**：CR-007 有两部分 —— (a) HTTP 侧 `POST /api/route` 按 functionId 转发 · (b) SOCK-5-C 入站 Socket 编排时同表复用。若 HTTP 侧前置到 v0.3.1(3-5 天),渠道方可以立即用 functionId 直调 HTTP · v0.3.2 SOCK-5 直接复用现成路由表。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐,Q1=B 时）| CR-007 HTTP 侧前置到 v0.3.1 · SOCK-5-C 只复用不重造 | v0.3.2 -1 周 · v0.3.1 +3-5 天 |
| **B** | CR-007 全部留 v0.3.2 · 与 SOCK-5-C 同期做 | 集中变更 · 但延迟渠道方可用时间 |
| **C** | CR-007 拆到独立 v0.3.3 · v0.3.1 只做规约与 patch | 增加 tag 数 |

**你的选择**：☐ A ☐ B ☐ C(仅 Q1=B/C 时回答)   选A

---

### Q14 · CR-003 版本显示是否前置到 v0.3.1

**背景**：CR-003 现归 v0.3.7 · 但 SOCK 联调时"客户抱怨看不到版本号"频率会陡增 · CR-003 只需 1 人日(Spring Boot build-info + Controller + 前端 footer 徽章)。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| 前置到 v0.3.1 · 1 天可完成 · SOCK 联调时立即受益 | 简单收益大 |
| **B** | 留 v0.3.7 · 按原路线图 | 4-5 月才见效 |

**你的选择**：☐ A ☐ B(仅 Q1=B/C 时回答)，选A，把

版本号： v0.3.1

作者：光斓

日期：2026年8月2日 

注：当前仅为测试版本

这些东西展示出来，最好放在一个不起眼的地方，或者隐写在屏幕上，又或者在某个地方有个按钮可以点开看到。对于上述这些东西可以存到表里面去，简易版是H2数据库，别人也不好改。日期就获取当前的系统日期就行

---

### Q15 · v0.3.1 装 v0.2.x 攒的 patch(FB-046 亮色主题 + FB-054 wizard edit)

**背景**：v0.2.x 补丁通道当前挂 2 条 patch —— FB-046(亮色弹窗视觉打磨)+ FB-054(wizard edit 回填,后半分类型 4 页归 v0.2.7)· 按 memory `feedback_patch_channel_test_driven` = 攒 3-5 条后打 patch tag,当前只 2 条不够,可归入 v0.3.1 minor 一起打。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| 归 v0.3.1 minor · 2 条 patch 不再单独打 v0.2.2 · 减少发版噪声 | 简单 |
| **B** | 攒够 3-5 条再打 v0.2.2 · v0.3.1 不做 patch · 保留清晰版本语义 | 需持续攒 · 时机不确定 |
| **C** | 单独打 v0.2.2 · 只装这 2 条 | 独立 tag 便于回滚 · 但发版频繁 |

**你的选择**：☐ A ☐ B ☐ C 选a

> **推荐 A**:v0.3.1 定位就是"收官版" · 承接 patch 是合理场景。

---

### Q16 · 复盘 5 项规约调整落地范围

**背景**：v0.2.x 阶段性复盘 § 八 提议 5 项规约调整:

| # | 项 | 影响 | 复盘推荐 |
|---|---|---|:---:|
| 1 | pg-feedback-flow skill 加 Step 3.5 冷静期 · 大范围 CR 强制等 24h | 决策稳定性 ↑ · 节奏 ↓ | 采纳 |
| 2 | AskUserQuestion 软上限 3 次/会话 | 用户疲劳 ↓ | 采纳 |
| 3 | agent 报告 verify 规则 · 用于决策前 verify ≥1 项 | 错报风险 ↓ | 采纳(低成本) |
| 4 | 反馈簿分档拆文件 · 活跃 + 归档拆两文件 | 维护成本 ↓ · 迁移一次性 | 中优先级 |
| 5 | v1.0 加 MVP 试点里程碑规约(见 Q4) | 销售节奏 ↑ | 待销售确认 |

**候选**（可多选）:

☐ 采纳 1(Step 3.5 冷静期)
☐ 采纳 2(AskUserQuestion 上限)
☐ 采纳 3(agent verify 规则)
☐ 采纳 4(反馈簿分档)
☐ 采纳 5(MVP 里程碑)

☐ **推荐:采纳 1 + 2 + 3 · 4 延后 · 5 由 Q4 单独定**

选A，反正我还是一个观点，对文档类的，已经做完的事情都摘出去归档，或者确定完成的都归档，而且如无必要勿增新流程方式。

> **推荐组合**:1/2/3 是低成本高收益,直接落到 CLAUDE.md 和 skill 里;4 是文档结构大迁移,当前反馈簿虽长但认知成本可控,延后到反馈簿破 500 行再动;5 与 Q4 强关联,Q4 拍板即可。

---

## 组 F · SOCK-5 入站(5 题 · v0.3.2 范围)

### Q17 · SOCK-5-D JSON 骨架配置 UI 复杂度

**背景**：CR-005 SOCK-5-D bank 场景 JSON 是数组包 head/body(`[{"head":{},"body":{}}]`),host 场景是扁平单对象 · 需在接口配置期指定"JSON 骨架模板"。

**候选**:

| 方案 | 说明 | 前端工作量 |
|---|---|---|
| **A**（推荐）| 最小 UI:接口配置 SOCKET 步骤加"JSON 骨架"折叠区 · 4 个 JSONPath 文本框(reqBody / reqHead / respBody / respList)· 空则走扁平模式 | 0.5 天 |
| **B** | 完整 wizard 步骤:新增"JSON 骨架"独立步 · 可视化拖拽 · 有骨架预览 | 3-5 天 |
| **C** | 后端支持,前端不做 UI · 用户在 config_json 里直接写 | 灵活 · 用户学习成本高 |

**你的选择**：☐ A ☐ B ☐ C 选A，B的话后面看客户实际业务场景再说

> **推荐 A**:YAGNI · 骨架规则简单,4 个 JSONPath 已覆盖 host + bank 场景 · 可视化拖拽是"未来某个客户可能需要"的过早投入。

---

### Q18 · lcpt-bank 报文样本验收标准(用户已注明"可能不是同一笔交易")

**背景**：`docs/04-测试/bank的报文参考.md` 用户注:"可能不是同一笔交易,拼凑同 FunctionId 的多段日志。我们只要转换功能完善就行"。

**候选验收范围**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| **能跑通** = XML 请求 → PG 入站 → 提取 FunctionId 180345 → 走 HTTP 出到 `lcpt-hstc-online-query` 应用(用 pg-testkit 模拟)→ 收 JSON 应答 → JSON→XML → 回写渠道 · 报文字段完整通过 · 不追求"一模一样重放" | 匹配用户表述 |
| **B** | 字段级完全对齐日志样本 · 每个字段都要 verify | 严 · 但样本本身不一致,难达成 |
| **C** | 端到端 200 OK 就行 · 报文内容可有差异 | 松 · 无质量门槛 |

**你的选择**：☐ A ☐ B ☐ C 选A

> **推荐 A**:用户明说"只要转换功能完善就行" · A 是"能演示 + 转换正确"的合理平衡点。

---

### Q19 · CR-007 功能号路由存储方式

**背景**：CR-007 需要 `function_id → interfaceId` 映射 · 两种存法。

**候选**:

| 方案 | 说明 | 灵活度 |
|---|---|---|
| **A**（推荐 v1.0 前）| 复用 `interface_config` 加字段 `function_id VARCHAR(64) UNIQUE NULL` · 一接口一功能号 · 迁移轻 | 简单 |
| **B** | 独立表 `function_id_route(function_id PK, interface_id FK, priority)` · 支持一功能号多接口按优先级 | 灵活 · 表结构变更 |
| **C** | 先做 A,v1.0 后如需多对多再拆到 B(不破坏 API 契约) | 渐进式 |

**你的选择**：☐ A ☐ B ☐ C，选C 我觉得先做A，后面如果业务量上去了，可能还是得做B,升级成B也放到一个远期版本吧，把背景记录一下就行，后面做到了这个版本就在看要不要做。

> **推荐 C(实施 A)**:目前无一功能号对多接口的实证场景 · 先 A 后 C 是标准演进路径 · CR-007 描述里也是"先复用 interface_config,后续需要再拆"。

---

### Q20 · SOCK-5-A 入站长连接支持范围

**背景**：bank 场景是渠道方"一交易一连接"还是"长连接复用"未明说 · 复盘阶段假设"短连接同步应答通道"够用。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| 只做短连接同步应答(接一个请求就应答一次即断)· 与 Q7 一致 | 简单 · 复用 SOCK-1 编解码器 |
| **B** | 做长连接支持 · Netty ChannelHandler 上下文追踪 · +3-5 天 | 过早优化 |
| **C** | 短连接为默认 · 配置里预留长连接开关(不实装) | 与 Q7 呼应 |

**你的选择**：☐ A ☐ B ☐ C  我觉得选C可能更合适，反正现在先做短连接

> **推荐 C**:与 Q7 保持一致 · 接口预留,实装留 v0.4.0+。

---

### Q21 · FB-053 pg-testkit 多应用名注册 · 并入 SOCK-5-B or 独立 SOCK-4-B

**背景**：FB-053 = pg-testkit 应支持模拟注册多个不同应用名到 Eureka · 直接支撑 SOCK-5-B HTTP 出站 + Eureka discover 联调 · 现归 v0.3.2。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| 并入 SOCK-5-B 单元:SOCK-5-B 交付时同步在 pg-testkit 加"Eureka Mock 客户端"能同时注册多个应用名 | 单元职责紧凑 |
| **B** | 独立 SOCK-4-B pg-testkit 增强单元 · 与 SOCK-5-B 解耦 | 单元更细粒度 · 但联调时机同 |
| **C** | 归 v0.3.1(若 Q1=B/C)· pg-testkit 侧先做好 · v0.3.2 直接用 | 前置基建 |

**你的选择**：☐ A ☐ B ☐ C  选A

> **推荐 A**:两者严格绑定 · 拆开会造成"pg-testkit 改完 SOCK-5-B 还没改"的中间状态 · 一并做减少断层。

---

## 组 G · 横跨性(3 题)

### Q22 · SOCK 场景手工用例 MT-XX-* 写作时机

**背景**：`docs/04-测试/v0.1.0-手工测试指南.md` 当前 ~85 条 MT-XX-* 覆盖 v0.1.0 全阶段 + FN-12 补齐。SOCK 是新协议,需新增大批用例(预估 MT-20-* 出站 8-10 条 + MT-21-* 入站 8-10 条 + MT-22-* 路由 4-6 条)。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| TDD 同步写:每个 SOCK-N 单元交付时同批附对应 MT-XX-* 用例(不集中)· CHG 归档一起 | 用例与代码同步 |
| **B** | v0.3.2 收官前统一补齐(方便一次跑完) | 集中 · 但可能补漏 |
| **C** | 只写后端单元测试 · 不补手工用例 · 后续用户测试驱动 | 违背当前 TDD 规范 |

**你的选择**：☐ A ☐ B ☐ C  选A

> **推荐 A**:与项目 TDD 规范一致 · SOCK 是新协议,单元交付时同步用例最保险。

---

### Q23 · trace_id 全串是否提前到 v0.3.0/v0.3.2(现归 v0.5.0)

**背景**：GAP 梳理维度 6 · trace_id 未跨表串 · 现归 v0.5.0 FN-BIZ 新增 business_op_log 时一并做 · **但 SOCK 场景涉及 3-4 层链路(渠道→PG入站→PG 出站→联机),没有 trace_id 排查问题会非常痛**。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐,若 Q1=B）| v0.3.1 提前做"MDC + trace_id 三表补齐"(sys_log / sql_audit_log / perf_stat 加 trace_id 字段 + AOP MDC 传递)· v0.5.0 只补 business_op_log 第四表 · 3-4 天工作量 | SOCK 联调受益 |
| **B** | 严格按现规划留 v0.5.0 · SOCK 联调靠日志时间戳凑 | 排查成本高 |
| **C** | 只做后端 MDC · 不改三表结构 · 日志里带 trace_id 但查不了 | 半成品 |

**你的选择**：☐ A ☐ B ☐ C  选A

> **推荐 A**:三表加 trace_id 字段迁移 SQL 简单 · MDC 是 Spring Boot 标准套路 · SOCK 场景不做 trace_id 后期维护成本非常高。

---

### Q24 · 老库客户对接方式(v0.2.5 scope 迁移已给脚本 · 是否统一策略)

**背景**：v0.2.0 → v0.2.5 已提供 `migration-fn-12-scope.sql` 幂等迁移脚本 · v0.3.0 SOCK-1 会新增 `interface_config.config_json` socket 段 · v0.3.2 CR-007 会加 `interface_config.function_id` 字段 · 老库客户升级需持续维护迁移脚本。

**候选**:

| 方案 | 说明 |
|---|---|
| **A**（推荐）| 每 minor 交付若涉及表结构变更,同步产出 `db/migration-vX.Y.Z-*.sql` 幂等脚本 · 累积一份 `db/migration/README.md` 索引记录顺序 | 现有做法制度化 |
| **B** | 引入 Flyway/Liquibase 迁移框架 · 自动化管理 | 引入新依赖 · 前期投入大 |
| **C** | 不管 · 老客户按发版记录手工升级 | 客户成本高 |

**你的选择**：☐ A ☐ B ☐ C  现在还没有实际使用的客户，

> **推荐 A**:项目当前只有 1 个老库客户(你自己),Flyway 是过早引入 · 每次 SQL + 索引足够。

---

## 已知的次要决策(可默认走 Claude 建议 · 你只需确认不反对)

以下事项相对小,若不反对我按推荐直接执行 · 不做主题题目,防清单过长:

| # | 事项 | 默认执行 |
|---|---|---|
| M1 | v0.3.0 任务计划文件命名沿用 `2026-07-28-FB-052-part1-lcpt-host-outbound.md`(现路线图已引用) | 遵循 |
| M2 | v0.3.2 任务计划文件命名沿用 `2026-07-28-FB-052-part2-lcpt-bank-inbound.md`(现路线图已引用) | 遵循 |
| M3 | 若 Q1=B,v0.3.1 任务计划命名 `2026-08-01-v0.3.1-中间收官.md` | 建议 |
| M4 | v0.3.0 SOCK-1 后端目录 `com.powergateway.socket.*`(CR-005 已定) | 遵循 |
| M5 | v0.3.0 编解码器命名 `LengthPrefixCodec` + `XmlBoundaryCodec`(CR-005 已定) | 遵循 |
| M6 | v0.3.0 前端 SOCKET 目标类型 icon 沿用 element-plus `Connection` 图标 | 简 |
| M7 | v0.3.2 CR-007 `POST /api/route` 免登(与 `/api/exec/{id}` 一致,渠道方直接调) | 遵循 M2-7 惯例 |
| M8 | v0.3.0/v0.3.2 CHG 编号:v0.3.0 ≈ CHG-037/038(SOCK-1 + SOCK-2/3/4) · v0.3.2 ≈ CHG-039/040/041(SOCK-5-A + B/C + D+E) | 遵循 |
| M9 | v0.3.0 打 tag 前 P0+P1 用例 = 现有 85 条 + 新增 MT-20-* SOCK 出站用例 | 遵循 |
| M10 | 复盘 § 7.3 "反馈簿分档拆文件" · 与 Q16.4 一致 · 若 Q16.4 不采纳则不做 | 遵循 |

☐ **全部同意 M1-M10 默认执行** 全部同意

---

## 你的整体回复模板(可复制回来直接填)

```
Q1  (三 minor 序列)              : A/B/C
Q2  (v0.2.5 tag 时机)            : A/B/C/D
Q3  (v0.2.7 归属)                : A/B/C/D
Q4  (MVP 预览 tag)               : A/B/C
Q5  (SOCK-1 分帧默认)             : A/B/C
Q6  (GBK 编码时机)                : A/B/C
Q7  (短连接/长连接)               : A/B/C
Q8  (Eureka 自注册时机)           : A/B/C
Q9  (Netty 依赖)                 : A/B/C
Q10 (SOCK-3 归属)                : A/B/C
Q11 (SOCK-4 兜底)                : A/B/C
Q12 (host demo 模板)             : A/B/C
Q13 (CR-007 前置)                : A/B/C  [仅 Q1=B/C]
Q14 (CR-003 前置)                : A/B    [仅 Q1=B/C]
Q15 (v0.2.x patch)               : A/B/C
Q16 (规约 5 项)                  : 采纳 __ / 全部推荐组合
Q17 (JSON 骨架 UI)                : A/B/C
Q18 (bank 验收标准)               : A/B/C
Q19 (CR-007 存储)                 : A/B/C
Q20 (SOCK-5-A 长连接)             : A/B/C
Q21 (FB-053 归属)                 : A/B/C
Q22 (SOCK 手工用例时机)           : A/B/C
Q23 (trace_id 前置)               : A/B/C
Q24 (老库迁移策略)                : A/B/C
M1-M10 默认执行                  : 全部同意 / 需要讨论(标注哪些)
```

---

## 归档

回复完毕后:
- ✅ / 🚫 逐条 → Claude 同步到 [路线图](./路线图.md) / [待办与缺陷池](./待办与缺陷池.md) / CLAUDE.md / CHG 记录 / 任务计划
- 本文件迁移到 `docs/06-项目管理/archived/2026-08-01-待确认问题清单.md`
- 若 Q1=B/C · 同步新建 v0.3.1 章节到路线图 + 待办池 + 建 v0.3.1 任务计划文件
- Q16 采纳的规约调整 → 更新 CLAUDE.md + skill 定义

---

## 相关文档

- [路线图](./路线图.md)(v0.3.0/v0.3.2 章节现状)
- [待办与缺陷池](./待办与缺陷池.md)(CR-005 / CR-007 / CR-003 / CR-004 / CR-006 / CR-008 详情)
- [反馈簿](./反馈簿.md)(FB-046 / FB-052 / FB-053 / FB-054 / FB-057 / FB-058 详情)
- [能力 GAP 梳理](./复盘诊断/能力GAP梳理-2026-07-28.md)(节奏 B 拍板依据)
- [v0.2.x 阶段性复盘](./复盘诊断/v0.2.x-阶段性复盘-2026-07-28.md)(Q16 规约 5 项出处)
- [模拟host.md](../04-测试/模拟host.md)(v0.3.0 验收依据)
- [bank的报文参考.md](../04-测试/bank的报文参考.md)(v0.3.2 验收依据)
