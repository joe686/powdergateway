# PowerGateway 可视化接口程序测试案例

---

## 目录
1. [接口转换配置测试](#1-接口转换配置测试)
   - 1.1 报文格式转换测试
   - 1.2 字段转换配置测试
   - 1.3 字段加工配置测试
   - 1.4 渠道模板管理测试
   - 1.5 端口分发路由测试

2. [可视化接口开发测试](#2-可视化接口开发测试)
   - 2.1 数据库连接管理测试
   - 2.2 表结构管理测试
   - 2.3 查询接口配置测试（单表/多表）
   - 2.4 插入接口配置测试
   - 2.5 修改接口配置测试
   - 2.6 删除接口配置测试
   - 2.7 分库分表配置测试
   - 2.8 常用字段公式管理测试
   - 2.9 缓存查询管理测试

3. [系统管理测试](#3-系统管理测试)
   - 3.1 SQL留存管理测试
   - 3.2 性能统计测试
   - 3.3 日志管理测试
   - 3.4 用户权限管理测试

---

## 1. 接口转换配置测试

### 1.1 报文格式转换测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M1-1-01 | JSON转XML | 1. 进入报文格式转换页面<br>2. 选择源格式为JSON，目标格式为XML<br>3. 输入测试JSON报文<br>4. 点击转换 | 转换结果为合法XML格式，字段值正确映射 | 高 |
| M1-1-02 | XML转JSON | 1. 进入报文格式转换页面<br>2. 选择源格式为XML，目标格式为JSON<br>3. 输入测试XML报文<br>4. 点击转换 | 转换结果为合法JSON格式，字段值正确映射 | 高 |
| M1-1-03 | JSON转CSV | 1. 进入报文格式转换页面<br>2. 选择源格式为JSON，目标格式为CSV<br>3. 输入测试JSON报文<br>4. 点击转换 | 转换结果为合法CSV格式，字段值正确映射 | 中 |
| M1-1-04 | CSV转JSON | 1. 进入报文格式转换页面<br>2. 选择源格式为CSV，目标格式为JSON<br>3. 输入测试CSV内容<br>4. 点击转换 | 转换结果为合法JSON格式，字段值正确映射 | 中 |
| M1-1-05 | FormData转JSON | 1. 进入报文格式转换页面<br>2. 选择源格式为FormData，目标格式为JSON<br>3. 输入测试FormData<br>4. 点击转换 | 转换结果为合法JSON格式，字段值正确映射 | 中 |
| M1-1-06 | 格式校验失败 | 1. 进入报文格式转换页面<br>2. 选择JSON转XML<br>3. 输入非法JSON（缺少闭合括号）<br>4. 点击转换 | 提示格式校验失败，显示具体错误位置 | 高 |
| M1-1-07 | 转换预览功能 | 1. 配置JSON转XML模板<br>2. 输入测试JSON报文<br>3. 点击预览 | 实时显示转换后的XML结果 | 高 |
| M1-1-08 | 模板保存与复用 | 1. 配置JSON转XML转换规则<br>2. 保存为模板<br>3. 新建转换任务，选择该模板<br>4. 输入测试报文转换 | 模板正确加载，转换结果符合预期 | 高 |

### 1.2 字段转换配置测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M1-2-01 | 字段映射配置（拖拽） | 1. 进入字段转换配置页面<br>2. 选择源报文和目标报文<br>3. 从源字段树拖拽字段到目标字段树 | 字段映射关系正确建立 | 高 |
| M1-2-02 | 固定值填入 | 1. 进入字段转换配置页面<br>2. 选择目标字段<br>3. 配置固定值（字符串/数字/日期）<br>4. 预览转换结果 | 目标字段使用固定值，忽略源字段 | 高 |
| M1-3-03 | 字段新增 | 1. 进入字段转换配置页面<br>2. 在目标报文中新增字段<br>3. 选择数据来源（映射源字段/固定值/加工生成） | 新增字段配置成功，预览时显示新增字段 | 高 |
| M1-2-04 | 字段删除/编辑 | 1. 进入字段转换配置页面<br>2. 删除目标报文中的一个字段<br>3. 编辑另一个字段的名称和类型 | 删除的字段不再出现，编辑后的字段属性更新 | 高 |
| M1-2-05 | 批量映射 | 1. 进入字段转换配置页面<br>2. 批量选择多个源字段<br>3. 点击自动匹配 | 字段名相同的源字段自动映射到目标字段 | 中 |
| M1-2-06 | 批量清除映射 | 1. 配置多个字段映射<br>2. 选择多个已映射字段<br>3. 点击批量清除 | 所选字段的映射关系被清除 | 中 |

### 1.3 字段加工配置测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M1-3-01 | 截位加工（左截） | 1. 进入字段加工配置页面<br>2. 选择字段，配置左截位，长度为5<br>3. 输入测试值"abcdefghij"<br>4. 预览加工结果 | 加工结果为"fghij" | 高 |
| M1-3-02 | 截位加工（右截） | 1. 进入字段加工配置页面<br>2. 选择字段，配置右截位，长度为5<br>3. 输入测试值"abcdefghij"<br>4. 预览加工结果 | 加工结果为"abcde" | 高 |
| M1-3-03 | 补齐加工（左补齐） | 1. 进入字段加工配置页面<br>2. 选择字段，配置左补齐，补齐字符"0"，总长度8<br>3. 输入测试值"1234"<br>4. 预览加工结果 | 加工结果为"00001234" | 高 |
| M1-3-04 | 补齐加工（右补齐） | 1. 进入字段加工配置页面<br>2. 选择字段，配置右补齐，补齐字符" "，总长度8<br>3. 输入测试值"abcd"<br>4. 预览加工结果 | 加工结果为"abcd    " | 高 |
| M1-3-05 | 格式转换（字符串转数字） | 1. 进入字段加工配置页面<br>2. 选择字段，配置字符串转数字<br>3. 输入测试值"12345"<br>4. 预览加工结果 | 加工结果为数字类型12345 | 高 |
| M1-3-06 | 格式转换（日期格式） | 1. 进入字段加工配置页面<br>2. 选择字段，配置日期格式转换（yyyy-MM-dd → MM/dd/yyyy）<br>3. 输入测试值"2024-01-15"<br>4. 预览加工结果 | 加工结果为"01/15/2024" | 高 |
| M1-3-07 | 大小写转换 | 1. 进入字段加工配置页面<br>2. 选择字段，配置全部大写<br>3. 输入测试值"hello world"<br>4. 预览加工结果 | 加工结果为"HELLO WORLD" | 中 |
| M1-3-08 | 去空格 | 1. 进入字段加工配置页面<br>2. 选择字段，配置去前后空格<br>3. 输入测试值"  hello world  "<br>4. 预览加工结果 | 加工结果为"hello world" | 中 |
| M1-3-09 | 多规则叠加 | 1. 进入字段加工配置页面<br>2. 配置规则1：去空格<br>3. 配置规则2：首字母大写<br>4. 配置规则3：截取前5位<br>5. 输入测试值"  hello world  "<br>6. 预览加工结果 | 加工结果为"Hello" | 高 |

### 1.4 渠道模板管理测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M1-4-01 | 渠道新增 | 1. 进入渠道模板管理页面<br>2. 点击新增渠道<br>3. 填写渠道编码、名称、识别字段<br>4. 关联转换模板<br>5. 保存 | 渠道配置成功保存 | 高 |
| M1-4-02 | 渠道编辑 | 1. 进入渠道模板管理页面<br>2. 选择已有渠道<br>3. 修改渠道名称和关联模板<br>4. 保存 | 渠道配置更新成功 | 高 |
| M1-4-03 | 渠道删除 | 1. 进入渠道模板管理页面<br>2. 选择已有渠道<br>3. 点击删除<br>4. 确认删除 | 渠道被删除，列表中不再显示 | 高 |
| M1-4-04 | 渠道自动匹配 | 1. 配置渠道：识别字段为"channel"，值"APP"关联移动端模板<br>2. 调用转换接口，传入含channel=APP的报文<br>3. 不指定模板ID | 系统自动识别渠道，使用移动端模板转换 | 高 |
| M1-4-05 | 渠道手动选择 | 1. 配置多个渠道模板<br>2. 进入报文转换页面<br>3. 手动选择渠道<br>4. 输入测试报文转换 | 使用所选渠道的模板进行转换 | 中 |
| M1-4-06 | 模板复制 | 1. 选择已有渠道模板<br>2. 点击复制<br>3. 修改关键信息后保存 | 新模板创建成功，与原模板配置一致 | 中 |

### 1.5 端口分发路由测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M1-5-01 | 端口路由新增 | 1. 进入端口分发路由页面<br>2. 点击新增路由<br>3. 选择渠道编码<br>4. 配置端口地址、请求方式、超时时间<br>5. 绑定请求/应答转换模板<br>6. 保存 | 路由配置成功保存 | 高 |
| M1-5-02 | 端口连通性测试 | 1. 配置端口路由<br>2. 点击连通性测试按钮 | 显示端口可达/不可达状态 | 高 |
| M1-5-03 | 完整双向调用 | 1. 配置渠道、转换模板、端口路由<br>2. 调用POST /api/dispatch<br>3. 传入源报文 | 请求经转换后转发到目标端口，应答经转换后返回 | 高 |
| M1-5-04 | 应答模板透传 | 1. 配置端口路由，不设置应答转换模板<br>2. 调用POST /api/dispatch<br>3. 传入源报文 | B系统原始应答直接透传返回 | 高 |
| M1-5-05 | 请求超时重试 | 1. 配置端口路由，设置超时时间1秒，重试次数3次<br>2. 调用dispatch接口，目标服务超时<br>3. 查看日志 | 系统重试3次后返回错误，日志记录重试过程 | 中 |

---

## 2. 可视化接口开发测试

### 2.1 数据库连接管理测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M2-1-01 | MySQL连接配置 | 1. 进入数据库连接管理页面<br>2. 点击新建连接<br>3. 选择数据库类型MySQL<br>4. 填写连接信息<br>5. 点击测试连接 | 连接测试成功，提示"连接成功" | 高 |
| M2-1-02 | Oracle连接配置 | 1. 进入数据库连接管理页面<br>2. 点击新建连接<br>3. 选择数据库类型Oracle<br>4. 填写连接信息<br>5. 点击测试连接 | 连接测试成功，提示"连接成功" | 高 |
| M2-1-03 | PostgreSQL连接配置 | 1. 进入数据库连接管理页面<br>2. 点击新建连接<br>3. 选择数据库类型PostgreSQL<br>4. 填写连接信息<br>5. 点击测试连接 | 连接测试成功，提示"连接成功" | 高 |
| M2-1-04 | 连接测试失败 | 1. 进入数据库连接管理页面<br>2. 新建连接，填写错误的密码<br>3. 点击测试连接 | 提示连接失败，显示具体错误信息 | 高 |
| M2-1-05 | 连接编辑/删除 | 1. 选择已有连接<br>2. 修改连接名称<br>3. 保存<br>4. 删除该连接 | 编辑后配置更新，删除后连接从列表消失 | 高 |
| M2-1-06 | 环境区分配置 | 1. 新建3个连接，分别选择dev/test/prod环境<br>2. 查看连接列表 | 连接按环境正确标识 | 中 |

### 2.2 表结构管理测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M2-2-01 | 表结构查询 | 1. 选择数据库连接<br>2. 进入表结构管理页面 | 树形展示数据库→表→字段，显示表注释和字段注释 | 高 |
| M2-2-02 | 缓存存储 | 1. 查询表结构<br>2. 查看Redis缓存 | 表结构信息缓存到Redis，TTL为24小时 | 高 |
| M2-2-03 | 手动刷新缓存 | 1. 修改数据库表结构（新增字段）<br>2. 在系统中点击刷新缓存<br>3. 查看表结构 | 表结构更新为最新状态 | 高 |
| M2-2-04 | 表结构模糊查询 | 1. 进入表结构管理页面<br>2. 在搜索框输入表名关键词<br>3. 点击搜索 | 显示匹配的表结构 | 中 |
| M2-2-05 | 表结构导出 | 1. 进入表结构管理页面<br>2. 点击导出Excel | 下载Excel文件，包含完整表结构信息 | 中 |

### 2.3 查询接口配置测试（单表/多表）

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M2-3-01 | 单表查询配置 | 1. 进入接口开发页面<br>2. 选择接口类型为查询<br>3. 选择数据库和表<br>4. 勾选返回字段<br>5. 配置查询条件<br>6. 预览查询结果 | 预览显示正确的查询结果 | 高 |
| M2-3-02 | 多表查询配置（2表关联） | 1. 进入接口开发页面<br>2. 选择接口类型为查询<br>3. 选择两个表<br>4. 配置主键关联关系（如A.id关联B.aid）<br>5. 勾选返回字段<br>6. 预览查询结果 | 预览显示正确的多表关联查询结果 | 高 |
| M2-3-03 | 多表查询配置（3表关联） | 1. 进入接口开发页面<br>2. 选择接口类型为查询<br>3. 选择三个表<br>4. 配置多级关联关系<br>5. 勾选返回字段<br>6. 预览查询结果 | 预览显示正确的多表关联查询结果 | 高 |
| M2-3-04 | 字段重命名 | 1. 配置查询接口<br>2. 对返回字段设置别名<br>3. 预览查询结果 | 结果集中字段使用别名 | 中 |
| M2-3-05 | 查询条件配置 | 1. 配置查询接口<br>2. 添加多个查询条件（等于、大于、包含等）<br>3. 设置条件组合（且/或）<br>4. 预览查询结果 | 查询结果符合条件组合 | 高 |
| M2-3-06 | 字段加工（查询结果） | 1. 配置查询接口<br>2. 对返回字段配置加工规则（如截位、格式转换）<br>3. 预览查询结果 | 查询结果中字段已按规则加工 | 高 |
| M2-3-07 | 数据加工（运算） | 1. 配置查询接口<br>2. 添加数据加工规则（字段A+字段B）<br>3. 预览查询结果 | 查询结果包含运算生成的新字段 | 高 |
| M2-3-08 | 动态参数赋值 | 1. 配置查询接口<br>2. 设置查询条件参数为请求字段<br>3. 传入测试请求参数<br>4. 预览查询结果 | 查询结果符合动态参数条件 | 高 |

### 2.4 插入接口配置测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M2-4-01 | 单表插入配置 | 1. 进入接口开发页面<br>2. 选择接口类型为插入<br>3. 选择数据库和表<br>4. 配置字段数据来源（请求报文/固定值/运算）<br>5. 预览插入结果 | 数据成功插入，预览显示插入成功 | 高 |
| M2-4-02 | 多表插入配置（2表） | 1. 进入接口开发页面<br>2. 选择接口类型为插入<br>3. 添加两个表<br>4. 分别配置每个表的字段数据来源<br>5. 预览插入结果 | 两个表的数据都成功插入 | 高 |
| M2-4-03 | 多表插入配置（3表） | 1. 进入接口开发页面<br>2. 选择接口类型为插入<br>3. 添加三个表<br>4. 分别配置每个表的字段数据来源<br>5. 预览插入结果 | 三个表的数据都成功插入 | 高 |
| M2-4-04 | 数据来源-请求报文 | 1. 配置插入接口<br>2. 设置字段数据来源为请求报文<br>3. 传入测试请求参数<br>4. 预览插入结果 | 字段值来自请求报文 | 高 |
| M2-4-05 | 数据来源-自定义字段 | 1. 配置插入接口<br>2. 设置字段数据来源为自定义固定值<br>3. 预览插入结果 | 字段值为配置的固定值 | 高 |
| M2-4-06 | 数据来源-字段运算 | 1. 配置插入接口<br>2. 设置字段数据来源为字段运算（如请求字段+固定值）<br>3. 预览插入结果 | 字段值为运算结果 | 高 |
| M2-4-07 | 数据校验失败 | 1. 配置插入接口<br>2. 遗漏必填字段配置<br>3. 预览插入结果 | 提示校验失败，显示具体错误信息 | 高 |
| M2-4-08 | 事务回滚 | 1. 配置多表插入接口<br>2. 设置第二张表插入失败条件<br>3. 预览插入结果 | 第一张表插入被回滚，数据保持一致 | 高 |

### 2.5 修改接口配置测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M2-5-01 | 修改接口配置 | 1. 进入接口开发页面<br>2. 选择接口类型为修改<br>3. 选择数据库和表<br>4. 配置修改字段和数据来源<br>5. 配置修改条件（主键）<br>6. 预览修改结果 | 数据成功修改，预览显示修改前后对比 | 高 |
| M2-5-02 | 唯一条件校验 | 1. 配置修改接口<br>2. 设置非唯一字段作为修改条件<br>3. 保存配置 | 提示条件不唯一，拒绝保存 | 高 |
| M2-5-03 | 数据来源复用 | 1. 配置修改接口<br>2. 使用请求报文、自定义字段、字段运算三种数据来源<br>3. 预览修改结果 | 修改后数据符合配置的数据源 | 高 |
| M2-5-04 | 修改预览对比 | 1. 配置修改接口<br>2. 填入测试参数<br>3. 点击预览 | 显示修改前和修改后的数据对比 | 高 |

### 2.6 删除接口配置测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M2-6-01 | 删除接口配置 | 1. 进入接口开发页面<br>2. 选择接口类型为删除<br>3. 选择数据库和表<br>4. 配置删除条件（主键）<br>5. 预览删除结果 | 数据成功删除，预览显示删除记录 | 高 |
| M2-6-02 | 删除条件唯一性校验 | 1. 配置删除接口<br>2. 设置非唯一字段作为删除条件<br>3. 预览删除 | 提示条件不唯一，要求确认 | 高 |
| M2-6-03 | 删除预览 | 1. 配置删除接口<br>2. 点击预览待删数据 | 显示将被删除的前10条数据 | 高 |
| M2-6-04 | 批量删除保护 | 1. 配置删除接口<br>2. 不启用批量删除开关<br>3. 设置可能导致批量删除的条件<br>4. 预览删除 | 拒绝执行，提示禁止批量删除 | 高 |
| M2-6-05 | 批量删除授权 | 1. 配置删除接口<br>2. 启用批量删除开关<br>3. 输入操作密码<br>4. 执行删除 | 批量删除成功 | 中 |

### 2.7 分库分表配置测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M2-7-01 | 分库分表规则配置 | 1. 进入分库分表配置页面<br>2. 点击新增规则<br>3. 配置业务模块名称<br>4. 选择请求字段<br>5. 配置库表映射规则<br>6. 保存 | 分库分表规则配置成功 | 高 |
| M2-7-02 | 规则校验预览 | 1. 配置分库分表规则<br>2. 输入测试请求字段值<br>3. 点击预览 | 显示匹配到的数据库和表 | 高 |
| M2-7-03 | 接口关联分库分表 | 1. 配置查询/插入/修改/删除接口<br>2. 选择已配置的分库分表规则<br>3. 保存接口配置 | 接口成功关联分库分表规则 | 高 |
| M2-7-04 | 分库分表执行 | 1. 调用关联了分库分表规则的接口<br>2. 传入不同的请求字段值<br>3. 查看执行结果 | 数据正确路由到目标库表 | 高 |
| M2-7-05 | 规则复制 | 1. 选择已有分库分表规则<br>2. 点击复制<br>3. 修改关键配置后保存 | 新规则创建成功 | 中 |

### 2.8 常用字段公式管理测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M2-8-01 | 公式新增 | 1. 进入常用字段公式管理页面<br>2. 点击新增公式<br>3. 填写公式名称、业务场景<br>4. 关联数据库<br>5. 配置条件和运算规则<br>6. 关联接口字段（可选）<br>7. 保存 | 公式配置成功保存 | 高 |
| M2-8-02 | 公式编辑 | 1. 选择已有公式<br>2. 修改条件配置<br>3. 保存 | 公式配置更新成功 | 高 |
| M2-8-03 | 公式删除 | 1. 选择已有公式<br>2. 点击删除<br>3. 确认删除 | 公式被删除，列表中不再显示 | 高 |
| M2-8-04 | 公式复制 | 1. 选择已有公式<br>2. 点击复制<br>3. 修改公式名称后保存 | 新公式创建成功，与原公式配置一致 | 中 |
| M2-8-05 | 公式调用（查询条件） | 1. 配置查询接口<br>2. 在条件配置中选择常用公式<br>3. 预览查询结果 | 查询条件应用了公式规则 | 高 |
| M2-8-06 | 公式调用（数据来源） | 1. 配置插入/修改接口<br>2. 在数据来源中选择常用公式<br>3. 预览执行结果 | 字段值按公式计算 | 高 |
| M2-8-07 | 公式校验 | 1. 配置公式，设置非法条件<br>2. 点击保存 | 提示校验失败，显示具体错误信息 | 高 |

### 2.9 缓存查询管理测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| M2-9-01 | 缓存查询创建 | 1. 进入缓存查询管理页面<br>2. 选择已发布的查询接口<br>3. 配置缓存key模板<br>4. 设置缓存有效期<br>5. 保存配置 | 缓存查询配置成功 | 高 |
| M2-9-02 | 缓存命中测试 | 1. 创建缓存查询配置<br>2. 第一次调用接口（冷查询）<br>3. 第二次调用接口（相同参数）<br>4. 查看响应时间 | 第一次响应时间较长，第二次响应时间显著缩短 | 高 |
| M2-9-03 | 缓存手动清除 | 1. 调用接口生成缓存<br>2. 在缓存管理页面点击清除缓存<br>3. 再次调用接口 | 缓存被清除，重新查询数据库 | 高 |
| M2-9-04 | 缓存更新 | 1. 调用接口生成缓存<br>2. 修改数据库数据<br>3. 在缓存管理页面点击更新缓存<br>4. 调用接口 | 缓存更新为最新数据 | 高 |
| M2-9-05 | 缓存命中统计 | 1. 多次调用接口（部分相同参数）<br>2. 查看缓存命中统计 | 命中次数和未命中次数正确统计 | 中 |
| M2-9-06 | 缓存高并发测试 | 1. 创建缓存查询配置<br>2. 使用压测工具并发调用接口<br>3. 查看响应时间和成功率 | 高并发场景下响应时间稳定，成功率≥99% | 高 |

---

## 3. 系统管理测试

### 3.1 SQL留存管理测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| SYS-1-01 | SQL留存记录 | 1. 执行插入/修改/删除接口<br>2. 进入SQL留存管理页面<br>3. 查询执行记录 | 显示对应的SQL语句和操作信息 | 高 |
| SYS-1-02 | SQL查询条件 | 1. 进入SQL留存管理页面<br>2. 按操作人、时间范围、操作类型查询<br>3. 点击搜索 | 显示符合条件的SQL记录 | 高 |
| SYS-1-03 | SQL导出 | 1. 查询SQL留存记录<br>2. 点击导出Excel | 下载Excel文件，包含完整SQL记录 | 中 |
| SYS-1-04 | 留存期限配置 | 1. 进入系统配置页面<br>2. 修改SQL留存期限<br>3. 保存配置 | 配置更新成功，过期记录自动清理 | 中 |

### 3.2 性能统计测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| SYS-2-01 | 统计数据展示 | 1. 执行多次增删改查操作<br>2. 进入性能统计页面<br>3. 查看统计图表 | 图表显示正确的执行次数、成功率、响应时间 | 高 |
| SYS-2-02 | 时间维度切换 | 1. 进入性能统计页面<br>2. 切换时间维度（小时/天/周/月）<br>3. 查看统计数据 | 数据按所选时间维度正确展示 | 高 |
| SYS-2-03 | 异常告警触发 | 1. 模拟大量失败请求<br>2. 进入性能统计页面<br>3. 查看告警信息 | 失败率超过阈值时触发告警提示 | 高 |
| SYS-2-04 | 告警阈值配置 | 1. 进入性能统计页面<br>2. 修改告警阈值<br>3. 保存配置 | 配置更新成功，新阈值生效 | 中 |

### 3.3 日志管理测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| SYS-3-01 | 日志查询 | 1. 执行接口操作<br>2. 进入日志管理页面<br>3. 按条件查询日志 | 显示符合条件的日志记录 | 高 |
| SYS-3-02 | 日志筛选 | 1. 进入日志管理页面<br>2. 按操作人、时间范围、日志级别筛选<br>3. 查看结果 | 日志按条件筛选正确 | 中 |
| SYS-3-03 | 日志导出 | 1. 查询日志记录<br>2. 点击导出Excel/TXT | 下载日志文件 | 中 |
| SYS-3-04 | 日志菜单开关 | 1. 进入系统配置页面<br>2. 关闭日志管理菜单开关<br>3. 查看侧边栏 | 日志管理菜单隐藏 | 中 |

### 3.4 用户权限管理测试

| 测试编号 | 测试场景 | 测试步骤 | 预期结果 | 优先级 |
| :--- | :--- | :--- | :--- | :--- |
| SYS-4-01 | 用户新增 | 1. 进入用户权限管理页面<br>2. 点击新增用户<br>3. 填写用户名、密码、角色<br>4. 保存 | 用户创建成功 | 高 |
| SYS-4-02 | 用户编辑 | 1. 选择已有用户<br>2. 修改角色<br>3. 保存 | 用户信息更新成功 | 高 |
| SYS-4-03 | 用户删除 | 1. 选择已有用户<br>2. 点击删除<br>3. 确认删除 | 用户被删除 | 高 |
| SYS-4-04 | 角色权限控制（admin） | 1. 使用admin角色登录<br>2. 查看所有菜单 | 所有菜单项可见 | 高 |
| SYS-4-05 | 角色权限控制（user） | 1. 使用user角色登录<br>2. 查看菜单 | 部分敏感菜单（如分库分表配置）不可见 | 高 |
| SYS-4-06 | 角色权限控制（readonly） | 1. 使用readonly角色登录<br>2. 尝试执行删除操作 | 提示无权限，操作被拒绝 | 高 |

---

## 测试优先级说明

| 优先级 | 说明 |
| :--- | :--- |
| 高 | 核心功能，必须通过测试，不通过则影响系统正常使用 |
| 中 | 重要功能，建议通过测试，不通过可能影响部分功能 |

---

**文档版本**: v1.0  
**创建日期**: 2026-05-06  
**适用版本**: PowerGateway 可视化接口程序